# v0.14 — Distributional / Variational Grammar Primitives (Design Note)

Implements the standing roadmap item "Grammar extensions for genuinely-deferred
primitives": two new scalar `ExprNode` arms that let the dimensional validator
EXPRESS the Model-A Langevin / fluctuation-dissipation relation the BE-15
docstring documents as un-encodable —

  ∂φ/∂t = −Γ δH/δφ + ζ ,   ⟨ζ(x,t)ζ(x',t')⟩ = 2Γk_BT δ(x−x') δ(t−t')

- **`dirac-delta`** — the Dirac-δ correlator primitive (barrier 1).
- **`variational-derivative`** — the functional derivative δF/δφ (barrier 2).

(Barrier 3, functional integration `H[φ] = ∫Dφ …` over field configurations, is
a measure over function space — NOT in scope; the roadmap names only the two
above.)

## The dimensional rules (the whole point)

Both are SCALAR-producing inline arms, exactly like the existing `integral` /
`derivative` arms (shape-guarded, `inferArgLocal` children, a pure dimensional
rule). They are NOT numerically evaluable (δ(0)=∞; a functional derivative needs
the functional's form) — `lowering` throws for them alongside `integral` /
`derivative`, and `evalExpr` / `substitute` fall to their throwing defaults.

1. **`{ kind: 'dirac-delta', arg: ExprNode }`** → `dim = power(dim(arg), −1)`.
   Because `∫ δ(x) dx = 1` (dimensionless) ⟹ `[δ(x)] = [x]⁻¹`. A dimensionless
   arg gives a dimensionless δ. Multi-argument deltas `δ(x)δ(t)` are an `op '*'`
   of single-arg `dirac-delta` nodes — no special multi-arg node needed.
   **Null short-circuit (Eve catch):** guard `if (argProbe.dim === null) return
   null;` BEFORE calling `power` — `algebra.ts` `power`/`divide`/`multiply` have
   no null guard and would emit a NaN-laden Dimension. Mirrors `validator.ts:535`.

2. **`{ kind: 'variational-derivative', functional, field, over }`** →
   `dim = divide(divide(dim(functional), dim(field)), dim(over))`, i.e.
   `[F]/([φ]·[μ])`. Because `δF = ∫ (δF/δφ) δφ dμ` ⟹
   `[F] = [δF/δφ]·[φ]·[μ]`. `over` carries the integration-measure dimension
   (the `dμ` whose one integration the variational derivative "undoes"),
   symmetric with `integral { integrand, over }` (which MULTIPLIES by `[over]`;
   this DIVIDES by it and by `[field]`). **Three-child shape guard (Eve catch):**
   `if (!node.functional || !node.field || !node.over) { push violation; return
   null; }` (cf. `integral`'s TWO-child guard `validator.ts:518`), THEN null
   short-circuit on all three child probes before `divide`.

Both arms are SCALAR: a child carrying free indices is rejected with
`TensorInScalarOpError` (mirroring the scalar `*`/`/`/`^` arms), and the node
contributes no free indices to the parent.

## Integration points (grounded against HEAD)

- `validator.ts`: add the two inline arms to the `ExprNode` union; add the two
  `case` arms to `infer` (the union has a `_exhaustive: never` default, so the
  arms are mandatory — the compiler enforces it). Update the header scope note
  (barriers 1+2 now expressible).
- `README.md:250` (Eve MUST-FIX): bump "Dimensional AST validator (**21 node
  kinds**; …)" → **23 node kinds**. Verified: 21 distinct `case` arms in
  validator.ts today; +2 = 23. (No TEST asserts the count — doc-only. Archived
  docs e.g. `batch-E-findings.md` keep their historical 21.) The "Zero
  public-surface change" claim below is still true for `ALL_TYPE_EXPORTS` /
  runtime snapshot — this README count is a separate documented invariant.
- `lowering.ts`: add `case 'dirac-delta'` / `case 'variational-derivative'` to
  the existing `integral`/`derivative` throw group (`NumericalBackendError`,
  "not numerically evaluated") — its `default` arm has a `never` exhaustiveness
  that would otherwise fail at tsc.
- `evalExpr` (expr-eval.ts), `substitute` (expr-subst.ts), `collectSymbols`
  (compose-symbolic.ts), `renderGensym` (expr-simplify.ts, switch at ~:165):
  throwing/`return` DEFAULT arms already tolerate new kinds — verified no
  `never` there. (`renderGensym`'s throwing `default` is caught as a no-op in
  `simplifyExpr`; `substitute` RECURSES into integral/derivative children but
  will THROW on the new arms — correct while catalog re-encoding is deferred,
  since these never appear in a composable `symbolic` form. NB: wiring the new
  arms into any bridge `symbolic` form LATER would require adding explicit
  `substitute`/`collectSymbols`/`renderGensym` arms.)
- The MathTS / Path-B formula transpilers BUILD ExprNode (don't switch on it) —
  unaffected.

## Public surface

The two arms are INLINE object types in the `ExprNode` union (like `integral` /
`derivative`), NOT named exported interfaces — so NO new entries in
`ALL_TYPE_EXPORTS` and NO runtime-snapshot change. Zero public-surface change.

## Demonstration (uncontested physics; catalog re-encoding deferred)

A test validates the FULL Model-A FDT relation, exercising both primitives, with
dimensions chosen consistently:
- `[φ]` a field dimension; `H` an energy `[F]=ENERGY`; the spatial measure
  `over = d³x` with `[μ]=L³`.
- `δH/δφ over d³x` → `ENERGY·[φ]⁻¹·L⁻³`.
- The Model-A Langevin `∂φ/∂t = −Γ·δH/δφ` fixes `[Γ] = [φ]²·L³·T⁻¹·ENERGY⁻¹`.
- FDT RHS `2·Γ·k_BT·δ³(x)·δ(t)` with `k_BT=ENERGY`, `δ³(x)=L⁻³`, `δ(t)=T⁻¹`
  then evaluates to `[φ]²·T⁻²` = `[ζ]²` = `[∂φ/∂t]²` — the LHS. **The FDT
  correlator equation is dimensionally homogeneous through the new grammar.**

The CATALOG re-encoding — replacing BE-15's deliberate Kawasaki-Gunton
`L²=Γt` reduction with the faithful Langevin form, and BE-28's contested Onsager
σ with a faithful MEPP variational maximization — is a PHYSICS-CURATION decision
(BE-28 in particular carries a ⚠ CRITICAL WARNING that its σ=ΣJX is the
definiendum, not the variational principle). That is a physicist's call
(CONTRIBUTING.md), deferred; this increment delivers the GRAMMAR + proves it
expresses the forms. BE-15's docstring is updated to record that barriers 1 and 2
are now lifted.

**Γ-disambiguation caveat (Adam catch).** The demonstration's bare-Langevin
kinetic coefficient `[Γ] = [φ]²·L³·T⁻¹·ENERGY⁻¹` is a DIFFERENT object from the
Γ already in BE-15's docstring (`be-15-emergence.ts` documents the
Kawasaki-Gunton coarsening diffusion constant with `[Γ] = L²/T`). Both are
legitimate — they coincide only under a specific `[φ]` normalization. The
docstring edit must therefore DISAMBIGUATE the two (label them "bare Langevin Γ"
vs "coarsening Γ"), NOT silently overwrite the existing `[L²/T]` with the new
demonstration value, or BE-15 would self-contradict.

**Docstring scope (Eve catch).** BE-15's barriers paragraph
(`be-15-emergence.ts:11-16`) asserts BOTH the "three grammar barriers" line AND
"The UPT AST grammar (`symbol | op … | integral | derivative`) provides none of
these primitives … cannot be encoded." The edit must refresh BOTH: record that
barriers 1 (Dirac-δ correlators) and 2 (functional derivative) are now lifted by
the new arms, while barrier 3 (functional integration over field configurations)
remains out of scope — not merely amend the barrier-count, or the docstring keeps
a now-false blanket "provides none of these primitives."

## Test plan

- `dirac-delta`: `δ(length)` → `[L]⁻¹`; `δ(time)` → `[T]⁻¹`; `δ(dimensionless)` →
  dimensionless; `δ(x)δ(t)` product → `[L]⁻¹[T]⁻¹`; missing `arg` → violation;
  tensor arg → throws.
- `variational-derivative`: `δH/δφ over d³x` → `[F]/[φ]/[μ]`; missing any field →
  violation; tensor child → throws.
- Equation-level: the full Model-A FDT relation validates homogeneous via
  `validateEquation` (the marquee).
- Equation-level NEGATIVE: a deliberately non-homogeneous FDT variant (e.g. drop
  a `δ(t)` from the RHS) is REJECTED by `validateEquation` — proves the grammar
  can reject, not only accept (Adam NICE-TO-HAVE #5).
- Serialization (Eve NICE-TO-HAVE #4): a JSON round-trip test for both new kinds
  (matching `tests/.../metric-ast-serialization.test.ts`, which round-trips each
  prior new kind) — build node → `JSON.parse(JSON.stringify(node))` → re-`infer`
  → same dim. Confirms no display/serialization path drops the new arms.
- Regression: `validate`/`lowering` exhaustiveness compiles; full suite green.

## Resolved questions (Adam vet — all leans endorsed)

1. `variational-derivative` parameterization: **`over` (measure dim)** — keeps the
   dimensional rule value-independent (no numeric `d` smuggled into the
   purely-dimensional layer), symmetric with `integral { over }`. A numeric
   `spatialDimension` would be an anti-pattern; `δ³(x)=L⁻³` is already handled by
   the `op '*'`-of-single-arg-deltas approach without a numeric field.
2. `dirac-delta` of a DIMENSIONLESS arg: **allowed (→ dimensionless)** — falls out
   of `power(DIMENSIONLESS, −1) = DIMENSIONLESS` for free; δ of a dimensionless
   variable (angle, phase) is physically legitimate. No special-case.
3. Tensor-child rejection: **reuse `TensorInScalarOpError`** — same "scalar
   operator got a tensor child" class, keeps `instanceof` discrimination uniform.
   Its constructor takes an `op` string; pass a descriptive label
   (`'dirac-delta'` / `'variational-derivative'`) so the message reads sensibly
   despite its `tensor-product`-oriented tail (Adam NICE-TO-HAVE #3).

## Adam verdict: GREEN

Dimensional rules correct (`power`/`divide` semantics verified against
`algebra.ts`); integral/derivative precedent faithfully mirrored; exhaustiveness
integration accurate; FDT algebra balances exactly; zero public-surface change
confirmed. Two MUST-FIX note corrections folded above (expr-simplify.ts audit
gap; Γ-disambiguation caveat). Cleared to proceed pending Eve.

## Eve verdict: YELLOW → folded GREEN

Confirmed Adam's GREEN on the core (dim rules, exhaustiveness wiring at
`validator.ts:663` / `lowering.ts:611`, error reuse, FDT physics — independently
re-derived, no spatial/spacetime delta conflation). Eve's negative-test check
CONFIRMED the mechanism (`validateEquation` `validator.ts:724` `!equals` catches
the `T⁻¹` vs `T⁻²` discrepancy). Display/serialization sweep CONFIRMED no silent
drop (all `ExprNode.kind` switches have non-`never` defaults; no pretty-printer
over ExprNode). One MUST-FIX (README:250 21→23 node-kind count) + four
nice-to-haves (null short-circuit; three-child shape guard; JSON round-trip test;
BE-15 docstring "provides none" sentence) — all folded above. Cleared to
implement.
