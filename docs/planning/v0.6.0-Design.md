# UPT v0.6.0 — GR Theoretical Closure + Shapiro Investigation — Design

**Generated**: 2026-05-19 (post-v0.5.1 ship).
**Source**: `docs/planning/v0.6.0-Brainstorm.md` (carry-forward seed) + v0.5.1 carry-forward in `todo.md` + theoretical-completeness audit performed during the brainstorming pass.
**HEAD at design time**: `3d73a52` (v0.5.1 closure commit).
**Pre-vet count**: N/A (this is the initial design — Adam+Eve vet runs against this document next).

> **Sister docs**: `docs/planning/v0.6.0-Brainstorm.md` (seed). `docs/planning/v0.5.1-Design.md` (template for this file's shape). `docs/architecture/v0.5.1-audit.md` (precedent for finding-driven release scope).

---

## Goal

Ship v0.6.0 as a **GR theoretical closure** minor bump. Three interlocking themes:

1. **Close the Einstein field equation as a structurally-validated AST equation.** v0.5.0 shipped the LHS (`EinsteinTensorNode`, `MetricTensorNode`). The RHS (`T_μν`), the cosmological term (`Λ`), and the full equality predicate (`G_μν + Λg_μν = (8πG/c⁴) T_μν`) are still absent. BE-13, BE-17, BE-20 explicitly document this gap. After v0.6.0, that statement is false and those bridges become structurally encodable.
2. **Add the conserved-charge machinery (Killing vectors)** required for a principled diagnostic of the BE-37 Shapiro residual floor (PC-1.5 from v0.5.1 carry-forward). In Schwarzschild, `∂_t` and `∂_φ` are Killing vectors → `E = -p_t` and `L = p_φ` are exact conserved quantities. A symplectic integrator should preserve them to machine precision; deviation pinpoints integrator vs null-IC reconstruction.
3. **Complete the classical-GR curvature surface**: Weyl tensor (trace-free Riemann; vacuum classification object), Kretschmann scalar (full curvature invariant; horizon-piercing diagnostic), and the PD-6-triggered `CurvatureCompositeNode<K,S>` factory extraction (Weyl is the 5th curvature primitive; PD-6 fires).

Plus two breaking-change carry-forwards bundled because the user authorized "don't worry about breaking API":

- **BR-2**: `christoffelFn` returns `Float64Array(64)` (was nested `number[4][4][4]`). No backwards-compat shim. Gate at ≥30% GL4+RK4 speedup.
- **FD default flip**: `pderivNumericalFn` default `order: 2 → 4` (was opt-in via PD-7 in v0.5.1).

**Non-goals** (deferred to v0.7.0+):
- Higher-dimensional spacetime support (Weyl formula is `n=4`-hardcoded).
- Lie derivative `ℒ_X` AST primitive (Killing equation uses `∇_(μ ξ_ν)` form; pure `ℒ` is broader).
- Spinor structure / Dirac equation (quantum-side primitive).
- Stress-energy raised to upper-upper variance `T^μν` (locked to lower-lower in v0.6.0).
- Torsion `T^ρ_{μν}` as a first-class AST kind (BE-17 still uses tensor-product scalar reduction).
- Path-integral / functional measure (quantum-gravity side).

---

## Tag Decision

**v0.6.0 (minor bump, breaking)**. SemVer applies from v0.1.0 (per CLAUDE.md). Two breaking changes are intentionally bundled (BR-2, FD-default). Both touch the numerical/lowering surface, so the migration tax is amortized into one consumer-update cycle.

---

## Architectural Decisions (locked)

### Decision #1 — `KillingVectorNode` validates symbolically; verification is numerical

The Killing equation `∇_(μ ξ_ν) = 0` is operator-valued (a tensor identity that must hold pointwise on the manifold). The validator returns `freeIndices` and dimension; the *numerical* check `verifyKillingEquation(killingFn, metricFn, x)` lives in `src/numerical/killing.ts` and uses 4th-order FD covariant derivatives. Default tolerance `1e-12`, parameterizable. Pattern mirrors `bianchiResidual()` from v0.5.0.

### Decision #2 — `StressEnergyTensorNode` locks to lower-lower variance

Canonical form is `T_μν` (lower-lower). Upper-upper `T^μν` requires metric-raising machinery that introduces no theoretical content. v0.7.0 may add the raised variant if a bridge needs it directly. Symmetry `T_μν = T_νμ` is enforced via a new `symmetry: 'symmetric' | undefined` field on the AST node (minimal symmetry-tag system; BE-17 antisymmetry stays deferred).

### Decision #3 — `EinsteinFieldEquationNode` is a first-class predicate, not an `op '='`

Rather than encoding `G + Λg = κT` as nested `op '+'` and an outer equality, we introduce `EinsteinFieldEquationNode` as a discriminated-union variant whose validator checks (a) free-index agreement (b) per-component dim equality (c) symmetry agreement on both sides. This is the natural shape for *every* rank-2 tensor equation in the catalog; v0.6.0 introduces it for Einstein and the pattern generalizes in v0.7.0.

### Decision #4 — `WeylTensorNode` hardcodes `n=4`

Spacetime dimension is `n=4` throughout UPT. The Weyl formula has explicit `1/(n-2)` and `1/((n-1)(n-2))` factors. Hardcode `n=4` with an explicit comment block; future-dimension parameterization is out of scope for v0.6.0 and would touch every curvature kind, not just Weyl.

### Decision #5 — `CurvatureCompositeNode<K,S>` factory extraction happens NOW

v0.5.1's PD-6 doc note pinned the extraction trigger as "first 5th curvature primitive." Weyl is that primitive. The migration applies to all six kinds: Riemann, Ricci, Einstein, BianchiResidual, Weyl, Kretschmann. Net LOC estimate **−180** (current parallel-validator duplication ~30 LOC × 4 + new factory ~60). Existing six-kind tests pass unchanged (refactor-only invariant).

### Decision #6 — BR-2 ships without backwards-compat shim

User directive: "don't worry about breaking API." No `christoffelFnFlatToNested(fn)` adapter. Migrate `tests/fixtures/schwarzschild.ts`, `computeChristoffelTensor`, and the RK4 + GL4 integrators in one coordinated commit. Bench gate ≥30% on `bench/gl4-mercury-1000step.bench.ts` is mandatory — failure reverts the BR-2 commits and ships v0.6.0 without it (negative-result documented).

### Decision #7 — FD default flip 2→4 is unconditional

`pderivNumericalFn` default `order` flips from 2 to 4. v0.5.1's PD-7 introduced the opt-in path; the curvature-lowering helpers already migrated. Any v0.5.1 test that *needs* `{order: 2}` (numerical noise sensitivity) gets explicit annotation. No deprecation warning; the user-directive scope explicitly accepts breaking changes.

### Decision #8 — PC-1.5 is measure-and-document, not measure-and-fix

User-confirmed gate. Phase 1 delivers a written finding (`docs/architecture/pc-1.5-shapiro-residual-floor.md`) backed by three benches (step-count sweep, null-IC reconstruction variance, conserved-charge drift). No numeric residual commitment; the diagnostic surfaces the root cause and v0.7.0 may implement a fix.

### Decision #9 — Bridge-status promotions remain deliberate

After Phase 2/3 enable structural encoding of BE-13/17/19/20/39/50, the temptation is to promote `speculative → established` automatically. Decline. Status promotion is a per-bridge **physics-validation** decision separate from AST closure; document the gap in CHANGELOG ("bridge X is now structurally encodable; status pin unchanged pending physics validation") and let v0.7.0 audit revisit.

---

## Phase Architecture

```
v0.6.0 — GR theoretical closure (estimated ~34 tasks)
┌──────────────────────────────────────────────────────────────────────┐
│ Phase 1 — PC-1.5 + Killing-vector machinery (~ 8 tasks)              │
│   THEORETICAL: ξ^μ satisfying ∇_(μ ξ_ν) = 0; conserved Q = ξ^μ p_μ;  │
│   diagnostic for BE-37 Shapiro residual via E/L drift along geodesic │
├──────────────────────────────────────────────────────────────────────┤
│ Phase 2 — Einstein-equation closure + BR-2 + FD-flip (~ 12 tasks)    │
│   THEORETICAL: T_μν, Λ, full G+Λg = κT predicate. Closes BE-13/17/20 │
│   BREAKING: christoffelFn → Float64Array(64); pderiv default 2→4     │
├──────────────────────────────────────────────────────────────────────┤
│ Phase 3 — Curvature invariants + composite-node abstraction (~ 9)    │
│   THEORETICAL: Weyl (vacuum class.), Kretschmann (horizon diag.);    │
│   PD-6 fires → CurvatureCompositeNode<K,S> factory consolidates 6    │
├──────────────────────────────────────────────────────────────────────┤
│ Phase 4 — Polish + bridge re-encoding + release prep (~ 5)           │
│   NEW-3 JSDoc; schwarzschildRiemannFn 8→16 components; BE-* re-      │
│   encoding; audit/outdated/build/bench; version bump + tag + publish │
└──────────────────────────────────────────────────────────────────────┘

Adam+Eve adversarial review gates:
  - Design vet (this doc — runs next)
  - Plan vet (after writing-plans skill drafts implementation plan)
```

---

## Phase 1 — PC-1.5 + Killing-vector machinery

### Theoretical motivation

In a spacetime admitting a Killing vector `ξ^μ` satisfying the Killing equation
`∇_μ ξ_ν + ∇_ν ξ_μ = 0`, the contraction `Q = ξ^μ p_μ` is exactly conserved
along any geodesic (Carroll §3.8, eq. 3.174–3.175). Schwarzschild has two
manifest Killing vectors — `∂_t` (time-translation) → conserved energy `E = -p_t`,
and `∂_φ` (axial rotation) → conserved angular momentum `L = p_φ`. A symplectic
integrator (GL4) on a Hamiltonian with these symmetries preserves the conserved
charges to machine precision modulo the integrator's truncation floor. **Drift
in `E` therefore localizes the BE-37 Shapiro residual floor**:

- If `max|ΔE/E|` over the Earth-Mars geodesic ≥ 2.5e-4: integrator-driven (step
  count too coarse).
- If `max|ΔE/E|` < 1e-10 while the BE-37 relErr stays at 2.51e-4: null-IC
  reconstruction noise (the sqrt + sign-choice on `p^μ p_μ = 0` produces an
  initial condition off by ~1e-15 absolute that compounds across the affine
  parameter range, not the integrator).

This is a *theoretically-grounded* diagnostic — the v0.5.1 audit's PC-1
hypothesis (constants drift) had no such grounding, which is part of why it
was refuted.

### Components

- **`src/dimensional/killing-validators.ts`** — `KillingVectorNode` (rank-1
  upper tensor + metric reference), `ConservedChargeNode` (inner contraction
  with rank-1 lower momentum), validators returning freeIndices + dim.
- **`src/numerical/killing.ts`** — `evaluateConservedCharge(killing, momentum, x)`,
  `verifyKillingEquation(killingFn, metricFn, x, { order = 4, tolerance = 1e-12 })`
  using 4th-order FD covariant derivatives.
- **`tests/fixtures/schwarzschild.ts`** — add `schwarzschildKillingT()` returning
  the rank-1 upper `[1, 0, 0, 0]` field; `schwarzschildKillingPhi()` returning
  `[0, 0, 0, 1]`. Pin both against `verifyKillingEquation` residual < 1e-12.
- **`bench/be37-step-count-sweep.bench.ts`** — BE-37 Earth-Mars at step counts
  `{256, 1024, 2048, 4096, 8192}`. Record relErr, max|ΔE/E|, max|ΔL/L|.
- **`bench/null-ic-reconstruction.bench.ts`** — reconstruct null IC 1000× with
  perturbed seed metric components; record variance in `p^μ` components.
- **`bench/geodesic-conservation.bench.ts`** — Mercury 100-orbit and BE-37
  Earth-Mars; record per-orbit max|ΔE/E| and max|ΔL/L|.
- **`docs/architecture/pc-1.5-shapiro-residual-floor.md`** — written finding
  with the three benches' outputs, a verdict (integrator vs null-IC), and a
  recommendation for v0.7.0+. Honest about uncertainty.

### Tests

- `tests/dimensional/killing-validators.test.ts` — node validation, dim, freeIndices.
- `tests/numerical/killing-schwarzschild.test.ts` — `verifyKillingEquation`
  residual < 1e-12 at 5+ sample points for both Killing vectors.
- `tests/numerical/conserved-charge-mercury.test.ts` — along Mercury 10-orbit
  GL4 integration, `max|ΔE/E|` and `max|ΔL/L|` < 1e-10.

---

## Phase 2 — Einstein-equation closure + BR-2 + FD-flip

### Theoretical motivation

The Einstein field equation

```
G_μν + Λ g_μν = (8πG / c⁴) T_μν
```

is the defining equation of GR. v0.5.0 shipped the LHS structurally
(`EinsteinTensorNode`, `MetricTensorNode`). The RHS (`T_μν`), the
cosmological term (`Λ g_μν`), and the equation itself as a structural
predicate are absent. BE-17 (Einstein-Cartan) `src/bridges/equations/be-17-einstein-cartan.ts:13-20`
explicitly documents this gap. Phase 2 closes it.

### Components

- **`src/dimensional/stress-energy-validators.ts`** —
  - `StressEnergyTensorNode { kind: 'stress-energy'; symbol: 'T'; indices: [LowerIndex, LowerIndex]; symmetry: 'symmetric'; componentDim: Dimension }`
  - `CosmologicalConstantNode { kind: 'cosmological-constant'; symbol: 'Λ'; dim: Dimension }` (dim = `[L⁻²]`)
- **`src/dimensional/einstein-equation.ts`** —
  - `EinsteinFieldEquationNode { kind: 'einstein-equation'; lhs: EinsteinTensorNode; cosmological: CosmologicalConstantNode | null; metric: MetricTensorNode; rhs: StressEnergyTensorNode; coupling: 'einstein' }`
  - Validator checks free-index agreement, per-component dim equality, symmetry agreement.
- **`src/numerical/einstein-equation.ts`** — numerical closure evaluator for
  `G_μν[g] + Λ g_μν - (8πG/c⁴) T_μν` at a point, returning residual tensor.
- **`tests/fixtures/perfect-fluid.ts`** —
  `perfectFluidStressEnergy(rho, p, fourVelocity, metric)` returning the
  `T_μν` array for `T_μν = (ρ + p/c²) u_μ u_ν + p g_μν`.
- **`src/numerical/christoffel-flat.ts`** — `christoffelFnFlat` returning
  `Float64Array(64)` with index encoding `(λ,μ,ν) → 16λ + 4μ + ν`. **No
  nested-shape shim.**
- **`src/numerical/pderiv.ts`** — flip default `order: 2 → 4`. Document the
  precision/cost trade in JSDoc (4th-order is ~2× FD evaluations but ~10⁴×
  lower truncation error). Sites that need `{order: 2}` get explicit annotation.

### Migrations (no shim, breaking)

- `tests/fixtures/schwarzschild.ts` — `schwarzschildChristoffelFn` returns
  flat shape.
- `src/numerical/connection-lowering-helpers.ts` — `computeChristoffelTensor`
  internals consume flat shape.
- `src/numerical/geodesic-integrator.ts` — RK4 + GL4 consume flat shape.
- All v0.5.1 callers (~10 sites) migrated.

### Tests

- `tests/numerical/einstein-vacuum-schwarzschild.test.ts` — `G_μν` numerical
  residual < 1e-9 at 10 sample `(r, θ)` points (`T = 0`, `Λ = 0`).
- `tests/numerical/einstein-desitter.test.ts` — `R_μν - Λ g_μν = 0` to
  relErr < 1e-9 on closed-form de Sitter metric.
- `tests/numerical/einstein-flrw.test.ts` — perfect-fluid closure recovers
  Friedmann `H² = 8πGρ/3` to relErr < 1e-7.
- `tests/numerical/stress-energy-validators.test.ts` — `StressEnergyTensorNode`
  validation, symmetry enforcement.
- `tests/numerical/cosmological-constant.test.ts` — dim and basic ops.
- `bench/gl4-mercury-1000step.bench.ts` — BR-2 bench gate ≥30% reduction
  vs v0.5.1 baseline (commit `bd98028`).
- `tests/numerical/pderiv-order-default.test.ts` — explicit pin that default
  order is now 4.

### Bench gate (BR-2)

If `bench/gl4-mercury-1000step.bench.ts` does NOT show ≥30% wall-time
reduction:
1. Revert BR-2 commits (christoffel-flat + integrator + fixture migrations).
2. Ship v0.6.0 without BR-2; carry forward to v0.7.0 brainstorm.
3. Document negative result in `docs/architecture/benchmarks.md` with hardware
   profile and bench output.

This protects the release — the breaking change is justified only by the
speedup; absent the speedup, the breaking change is debt.

---

## Phase 3 — Weyl + Kretschmann + CurvatureCompositeNode<K,S>

### Theoretical motivation

**Weyl tensor `C^ρ_{σμν}`** is the trace-free part of Riemann:

```
C^ρ_{σμν} = R^ρ_{σμν}
          − (1/(n-2)) (δ^ρ_μ R_{σν} − δ^ρ_ν R_{σμ} − g_{σμ} R^ρ_ν + g_{σν} R^ρ_μ)
          + (1/((n-1)(n-2))) R (δ^ρ_μ g_{σν} − δ^ρ_ν g_{σμ})
```

(n=4 hardcoded per Decision #4.) Weyl encodes purely gravitational
degrees of freedom — the part of curvature that survives in vacuum.
Schwarzschild and Kerr have `R_μν = 0` but `C^ρ_{σμν} ≠ 0`; this is
why tidal forces exist outside a black hole. Without `WeylTensorNode`,
UPT cannot distinguish Ricci-curvature (matter-coupled) from
Weyl-curvature (vacuum/gravitational-wave) — a core GR classification
that no unification claim can omit.

**Kretschmann scalar `K = R_{ρσμν} R^{ρσμν}`** is the full quadratic
curvature invariant. For Schwarzschild,
`K(r) = 48 G² M² / (c⁴ r⁶)` — diverges at `r = 0` (true singularity),
finite at `r = r_s` (coordinate singularity). Canonical horizon-piercing
diagnostic.

**`CurvatureCompositeNode<K, S>` factory**: v0.5.0 shipped four parallel
curvature-composite AST kinds. v0.5.1's PD-6 pinned the extraction
trigger as "first 5th curvature primitive." Weyl is that primitive.

### Components

- **`src/dimensional/weyl-validators.ts`** — `WeylTensorNode` (one upper +
  three lower indices, componentDim `[L⁻²]`).
- **`src/numerical/weyl-lowering.ts`** — Weyl from Riemann + Ricci +
  Ricci-scalar using the n=4 formula.
- **`src/dimensional/curvature-invariants.ts`** — `KretschmannScalarNode`
  (componentDim `[L⁻⁴]`).
- **`src/numerical/kretschmann.ts`** — `R_{ρσμν} R^{ρσμν}` numerical
  contraction.
- **`src/dimensional/curvature-composite.ts`** — `CurvatureCompositeNode<K, S>`
  factory + `CurvatureKindRegistry` mapping. Migrate
  Riemann/Ricci/Einstein/Bianchi/Weyl/Kretschmann onto it. `lowering.ts`
  consolidates 6 switch arms behind `lowerCurvature(node, ctx)`.

### Tests

- `tests/numerical/weyl-schwarzschild.test.ts` — vacuum check
  `C^ρ_{σμν} = R^ρ_{σμν}` (Weyl = Riemann when Ricci = 0) to relErr
  < 1e-9 at 5+ points.
- `tests/numerical/kretschmann-schwarzschild.test.ts` — `K(r) = 48G²M²/(c⁴r⁶)`
  to relErr < 1e-9 at 10 r-values from `r_s` to `100·r_s`.
- `tests/numerical/kretschmann-horizon.test.ts` — `K(r_s) = 3/(4G²M²)` is
  finite (coordinate-singularity diagnostic).
- `tests/dimensional/weyl-validators.test.ts` — node validation, dim,
  freeIndices, n=4 prefactor pins.
- `tests/dimensional/curvature-composite-factory.test.ts` — refactor-only
  invariant: every existing Riemann/Ricci/Einstein/Bianchi test passes
  unchanged. Pre-commit verification gate.

---

## Phase 4 — Polish + bridge re-encoding + release prep

### Components

- **NEW-3 JSDoc backfill** in `src/index.ts` and all new Phase 1–3 public exports.
- **`schwarzschildRiemannFn` fixture backfill** — v0.5.0 shipped 8/256 entries
  (pragmatic-minimum). Backfill all 16 distinct non-zero components (the
  remaining 240 are zero or related by index symmetries). Cross-pin against
  numerical lowering to relErr < 1e-12.
- **Bridge re-encoding using new primitives** (status pins UNCHANGED per
  Decision #9):
  - BE-13 Einstein-trace — use `EinsteinFieldEquationNode` for full LHS=RHS
  - BE-17 Einstein-Cartan — close the field-equation half (algebraic
    torsion-spin stays scalar-reduced; antisymmetry tag deferred)
  - BE-19 quantum bounce — use `KretschmannScalarNode` for horizon diagnostic
  - BE-20 vacuum energy — use `CosmologicalConstantNode` and
    `EinsteinFieldEquationNode`
  - BE-39 asymptotic safety — use `WeylTensorNode` for the C² coupling
  - BE-50 Wheeler-Feynman — Weyl-vacuum classification
- **Release pre-flight** per CLAUDE.md repo invariant: `npm audit`, `npm outdated`,
  full vitest, `npm run build`, `npm run smoke`, `npm run bench:ci`.
- **Version bump** 0.5.1 → 0.6.0 in `package.json`; CHANGELOG entry with
  summary + BREAKING-changes section (BR-2, FD-default) + theoretical-closure
  summary. `todo.md` roll-forward.
- **Direct-push** to `master`; tag `v0.6.0`; push tag; `npm publish
  --ignore-scripts --access public`.

---

## Cross-Phase Invariants

1. **Suite green** at every task commit. Baseline 1595/1597 (v0.5.1).
   Target ~1700+ after Phase 1–3 additions.
2. **tsc clean** at every task commit (`npm run build` exits 0).
3. **No new `@ts-ignore` / `as any`** — v0.5.1 TS-1 set the zero-bar. New
   `import type` chains preferred.
4. **BE-37 Shapiro residual tracked** (NOT gated until release). Final
   v0.6.0 BE-37 relErr must not exceed v0.5.1's 2.51e-4 *unless* PC-1.5
   finding explicitly justifies a known-cause regression.
5. **Bridge-status integrity test pinned** —
   `tests/bridges/dimensional-signature-catalog.test.ts` passes. Status
   promotions remain deliberate per Decision #9.

---

## Subagent Parallelism Boundaries

File-disjoint partitioning per v0.5.1 "Multi-agent file-disjoint parallelism"
lesson. CHANGELOG.md is the single read-modify-write contention point
(sequential).

| Phase | Parallel partition |
|---|---|
| 1 | Killing AST/validators (A) ∥ PC-1.5 benches (B). Both read `tests/fixtures/schwarzschild.ts`. |
| 2 | StressEnergy + Λ + EinsteinFieldEquation AST (A) ∥ BR-2 flat-Γ migration (B) ∥ FD-flip (parent). |
| 3 | Weyl AST + numerical (A) ∥ Kretschmann AST + numerical (B) ∥ CurvatureComposite factory (parent post-merge). |
| 4 | JSDoc backfill (A) ∥ fixture backfill (parent) ∥ BE-bridge re-encoding (parallel per-bridge). |

---

## Risks

1. **Phase 2 scope explosion**. Heaviest phase: 5 distinct theoretical
   additions + 2 breaking-change migrations. Mitigation: BR-2 last in
   Phase 2; Einstein-closure pins land first.
2. **Killing-equation sign convention**. Carroll vs MTW differ in
   convention details. UPT follows Carroll throughout. Mitigation:
   pre-execution verification gate on Phase 1 Task 1 (read Carroll
   Eq. 3.174 before writing validator).
3. **Weyl-formula prefactor dim-dependence**. Hardcode `n=4` with explicit
   comment block (Decision #4).
4. **FD-flip regression on existing v0.5.1 tests**. Mitigation: rerun
   full suite after flip; explicit `{order: 2}` annotation on noise-sensitive
   tests.
5. **CurvatureComposite migration churn**. 6 kinds × 3 files each ≈ 18
   file edits. Mitigation: refactor-only invariant (existing tests pass
   unchanged); land Weyl + Kretschmann on factory first, then migrate
   existing four incrementally.
6. **BR-2 bench gate fails**. Mitigation: full revert path documented;
   negative-result documented in `docs/architecture/benchmarks.md`;
   release ships without BR-2.

---

## Open Questions (defer to Adam+Eve vet)

1. Should `verifyKillingEquation` accept an `engine: TensorEngine` parameter
   for consistency with `bianchiResidual`?
2. Should `EinsteinFieldEquationNode` allow `cosmological: null` to mean
   "Λ-omitted" or require an explicit `Λ = 0` `CosmologicalConstantNode`?
   Recommend `null` semantic.
3. Should `StressEnergyTensorNode` require a `symmetry: 'symmetric'` field
   (locked) or treat it as implicit at the node-kind level? Recommend
   explicit field for future-extensibility (Tμν⁺ Belinfante-Rosenfeld
   form is non-symmetric).
4. Should `WeylTensorNode` accept a `dimension: 4` field for future-proofing
   (decision #4 caveat) or stay hardcoded? Recommend hardcoded; the field
   would be cargo-culted until used.
5. **Eve-hallucination prevention**: every Eve-vet specific-fact claim
   gets grep-verified per `feedback_eve_o3_hallucinations.md` (55% rate on
   v0.5.1 audit).

---

## Public-API Additions

Final list:

- `KillingVectorNode`, `ConservedChargeNode`
- `evaluateConservedCharge`, `verifyKillingEquation`
- `StressEnergyTensorNode`, `CosmologicalConstantNode`
- `EinsteinFieldEquationNode`, `evaluateEinsteinEquation`
- `WeylTensorNode`, `KretschmannScalarNode`
- `CurvatureCompositeNode<K, S>`, `CurvatureKindRegistry`
- `christoffelFnFlat` (BR-2 BREAKING — replaces nested shape)
- Schwarzschild Killing-vector fixtures (`schwarzschildKillingT`,
  `schwarzschildKillingPhi`)
- Perfect-fluid stress-energy fixture (`perfectFluidStressEnergy`)

---

## Breaking Changes (CHANGELOG-bound)

- **BR-2**: `christoffelFn` and `schwarzschildChristoffelFn` now return
  `Float64Array(64)` (was nested `number[4][4][4]`). Index encoding
  `(λ,μ,ν) → 16λ + 4μ + ν`. No shim. Migration: replace
  `christoffel[λ][μ][ν]` with `christoffel[16*λ + 4*μ + ν]` or use the
  new `getChristoffel(arr, lambda, mu, nu)` helper.
- **FD default**: `pderivNumericalFn` default `order` is now 4 (was 2).
  Sites that need 2nd-order get explicit `{order: 2}` annotation.
- **Internal**: 6 curvature kinds share `CurvatureCompositeNode<K, S>`.
  Transparent if accessed via existing names (Riemann/Ricci/Einstein/
  Bianchi/Weyl/Kretschmann); breaking if accessed by structural pattern
  matching on the AST discriminator (none in catalog).

---

## Task Count

Estimated **~34 tasks** across 4 phases (8 + 12 + 9 + 5). Comparable to
v0.5.0's 25 and v0.5.1's 22. Heavier than typical patch because of the
theoretical-closure scope.
