# UPT v0.4.0 Design — Connection Layer + Two New Einstein-GR Bridges

**Date:** 2026-05-15
**Status:** Design (brainstorming approved interactively 2026-05-15; Adam + Eve adversarial review completed at lock point; reshaped post-review around the north-star pull — bridge encoding + first-class MathTS positioning; awaiting written-spec review before implementation-plan handoff)
**Author:** Daniel Simon Jr., with brainstorming-skill agent (Opus 4.7 1M); cross-validated by Adam (Gemini 2.5 Pro) `COHERENT 0.90` + Eve (OpenAI o3, full) `COHERENT-WITH-GAPS 0.65`, single-pass consolidated lock-point review with all 3 HIGH findings folded in inline. Post-review reshape (bridge encoding + `mathts-autograd` sister package) is **scope + packaging**; the Adam/Eve-validated architectural decisions (covariant-derivative node shape, validator case, AD interface shapes, Schwarzschild domain restriction) are unchanged.
**Companion runbooks:** [`v0.2.0-Design.md`](v0.2.0-Design.md), [`v0.3.0-Design.md`](v0.3.0-Design.md), [`v0.3.5-Design.md`](v0.3.5-Design.md)
**Spec foundation:** [`docs/specification/Part-VIII-Metric-Layer.md`](../specification/Part-VIII-Metric-Layer.md) §VIII.8, §VIII.9, §VIII.10; v0.4.0 introduces `Part-IX-Connection-Layer.md`

---

## 1. Context

UPT v0.3.5 (tag `v0.3.5`, commit `7786027`, 2026-05-14) shipped the **numerical-contraction backend**: a `TensorEngine` interface with `Float64ReferenceEngine` (zero-dep default) and `MathTSEngine` (over `@danielsimonjr/mathts-tensor`), AST→engine lowering, two-way numerical partial derivatives, `InverseMetricInconsistencyWarning`, and BE-37 Shapiro-delay eikonal end-to-end at relative error 7.3×10⁻¹⁰.

**v0.4.0 is the first release where the connection-layer machinery EXISTS TO ENCODE NEW BRIDGES.** The north stars are explicit: UPT *verifies bridge equations*, builds the mathematical framework *with MathTS, tensors, threejs*, toward an *integrated scientific environment* (TensorJS). v0.4.0 adds two canonical Einstein-1915 GR-flavored bridges — **gravitational lensing** (α = 4GM/(bc²), Eddington 1919) and **Mercury perihelion precession** (Δφ = 6πGM/(a(1−e²)c²), Le Verrier 1859 / Einstein 1915) — that the **connection layer enables**. The connection layer itself (Christoffel `Γ^λ_μν` + covariant derivative `∇_μ`, both symbolic AST + numerical) and the Schwarzschild geodesic integrator are the **infrastructure** the new bridges run on, not the headline deliverable.

Two MathTS-side deliverables ride alongside, reinforcing the "mathematical framework with MathTS" pillar: forward-mode AND reverse-mode automatic differentiation ship as **a first-class new MathTS package** `@danielsimonjr/mathts-autograd` (sister to v0.3.5's `mathts-tensor`); UPT's `MathTSEngine` becomes a thin adapter over it. At release time, MathTS becomes the registry default engine.

Per the v0.3.5-refreshed roadmap: v0.4.0 ships the connection layer + two GR bridges + MathTS AD package; v0.5.0+ adds curvature, the Faraday-cascade BREAKING refactor, real MathTS perf optimizations, and threejs visualization bootstraps.

## 2. Scope

### In scope

1. **Two new Einstein-GR bridges in the catalog:**
   - **BE-?? Gravitational Lensing** (Eddington 1919; modern radio interferometry). Symbolic encoding: null-geodesic deflection-angle calculation in Schwarzschild. Closed-form (weak-field): `α = 4GM/(bc²)` where `b` is the impact parameter. Numerical: RK4-integrate the null geodesic, accumulate the angle change. Acceptance: numerical α matches `4GM/(bc²)` to **±10⁻⁹ relative** in the weak-field domain (impact parameter `b ≫ r_Schwarz`).
   - **BE-?? Mercury Perihelion Precession** (Le Verrier 1859, Einstein 1915). Symbolic encoding: per-orbit precession of the perihelion of a bound timelike geodesic in Schwarzschild. Closed-form (weak-field): `Δφ_per_orbit = 6πGM/(a(1−e²)c²)` where `a` is the semi-major axis, `e` the eccentricity. Numerical: RK4-integrate the timelike geodesic for N orbits, extract the secular perihelion drift. Acceptance: numerical drift over N orbits matches `N × 6πGM/(a(1−e²)c²)` to **±10⁻⁹ relative** in the weak-field domain (orbit radius `r ≫ r_Schwarz`).

   Both bridges are canonical Einstein-1915 GR tests; both *are* bridges between Newtonian and GR regimes; both will become permanent entries in the 40-bridge catalog (numbers picked at implementation time from `src/bridges/index.ts`'s next-free range).

2. **Connection-layer machinery (the infrastructure):**
   - **`covariant-derivative` AST node**: a new `ExprNode` kind parallel to v0.3.0's `tensor-partial-derivative`. Shape `{ kind: 'covariant-derivative', of, wrt, wrtIndex, gLower, gInverse }`. Dedicated validator case with internally-consumed metric indices (§7).
   - **`christoffel()` helper**: builder function `christoffel(gLower, gInverse, λ, μ, ν, xCoord): ExprNode` returning a composite tree `(1/2)·g^λρ·(∂_μ g_ρν + ∂_ν g_ρμ − ∂_ρ g_μν)` built from existing v0.3.0 nodes. Parallels v0.3.0 `raise()` / `lower()`.
   - **`derivativeStrategy` field on `metric-tensor`**: optional `'computed' | 'zero' | 'supplied'`, defaults `'computed'`. Closes the v0.3.0-deferred `varying` question.
   - **Schwarzschild geodesic integrator**: extension of v0.3.5's `null-ray-integrator` to the 8-dim geodesic state vector `[x^μ, dx^μ/dτ]`. Used by BOTH bridges. Integration domain restricted to **r ≥ 3·r_Schwarz** (Adam + Eve H2).

3. **MathTS AD as a first-class new package: `@danielsimonjr/mathts-autograd`.** A new MathTS workspace package depending on `@danielsimonjr/mathts-tensor` (v0.1.0+). Ships forward-mode AD (dual numbers — `DualTensor` wrapper) and reverse-mode AD (tape + backward — `Tape` class). Exposes `forwardGrad(fn, x): { value, jacobian }` and `reverseGrad(fn, x, cotangent?): { value, gradient }` as public functions. Version 0.1.0 at release.

4. **AD on `TensorEngine` interface**: two optional methods, `forwardGrad?` and `reverseGrad?` (shapes pinned in §6). **`Float64ReferenceEngine`** implements them in-house (zero-dep dual-number / tape implementation). **`MathTSEngine`** adapts via the new `mathts-autograd` package. Both engines pass an identical AD-conformance test layer.

5. **`InverseMetricInconsistencyWarning` extension**: a new sub-rule fires when the same metric name appears in one AST with **conflicting `derivativeStrategy` values** (Eve M2).

6. **MathTSEngine becomes the registry default** at release time. Prerequisite: Daniel manually publishes BOTH `@danielsimonjr/mathts-tensor` (still v0.1.0 — deferred from v0.3.5) AND `@danielsimonjr/mathts-autograd` (v0.1.0 — new in v0.4.0) to npm. UPT then promotes the dep from `optionalDependencies` to `dependencies` and flips the registry default. Honest framing: this is a code-path-signal + dependency-shape change; v0.4.0 makes the *bridge of perf substrate* available — real perf optimizations remain v0.5.0+ work.

7. **`Part-IX-Connection-Layer.md`** spec section following the Part-VIII pattern (TENSOR-RULE markers, spec↔impl drift-guard test).

### Explicitly out of scope (deferred to later versions)

- **Riemann curvature `R^μ_νρσ`, Ricci, Bianchi identities, Einstein field-equation solvers**: v0.5.0+.
- **Friedmann-flavored cosmology bridges**: v0.5.0+ (curvature-adjacent — needs Einstein field equations).
- **Faraday mixed-component-dim BREAKING refactor**: v0.5.0+ (Part-VIII §VIII.10).
- **Non-Levi-Civita connections** (teleparallel, Cartan, etc.): v0.5.0+.
- **Real MathTS perf optimizations** (BLAS, SIMD, GPU): v0.5.0+.
- **Higher-order AD** (Hessians, JVP/VJP at arbitrary order): v0.5.0+ if curvature work needs it.
- **threejs visualization bootstraps**: v0.5.0+ per the existing roadmap. v0.4.0's geodesic-integrator output IS naturally trajectory-shaped, so future viz can consume it without API churn — but no viz code in v0.4.0.
- **AST-size optimization for embedded metric children** (Eve L3): v0.5.0+ when curvature lands.

## 3. Approach

Three pillars, each grounded in an existing v0.3.0 or v0.3.5 precedent:

1. **Bridges drive the work.** v0.3.0/v0.3.5 used BE-37 as their worked-example bridge — the metric layer and the numerical backend earned their existence by *encoding a real bridge*. v0.4.0 does the same with **two** bridges (lensing + perihelion), both leveraging the new connection-layer machinery. The Schwarzschild geodesic integrator and the new AST/AD machinery are *infrastructure*, not the headline.

2. **`∇_μ` as a node, `Γ` as a helper.** Mirrors v0.3.0's "fundamental operators get dedicated AST kinds; composite formulas are helpers." Part-VIII §VIII.8 hints at this exactly: *"v0.4.0 will swap `pderiv` for the covariant derivative `∇_μ`… only the operator changes."* Christoffel `Γ` is a composite formula (already provable composable from v0.3.0 nodes per §VIII.9 + the preview test); helper.

3. **AD lives in MathTS.** "Build the mathematical framework with MathTS, Tensors, and threejs" is the north star; AD is mathematical framework, so it goes in MathTS. The new `@danielsimonjr/mathts-autograd` package is the first-class home. UPT's `MathTSEngine` adapts it cleanly; `Float64ReferenceEngine` keeps a parallel in-house implementation only because of UPT's zero-dep-default posture. The AD interface on `TensorEngine` is the shared contract.

## 4. AST extension

### Existing node types (v0.3.5, unchanged shapes except one additive optional field)

```typescript
// src/dimensional/metric-validators.ts — MetricTensorNode gains one optional field
export interface MetricTensorNode {
  readonly kind: 'metric-tensor';
  readonly name: string;
  readonly indices: ReadonlyArray<TensorIndex>;
  readonly signature: string;
  readonly dim: Dimension;
  readonly derivativeStrategy?: 'computed' | 'zero' | 'supplied';  // NEW in v0.4.0; defaults 'computed'
}
```

`derivativeStrategy` defaults to `'computed'` — every v0.3.x AST is a valid v0.4.0 AST. Numerical lowering branches on this field:
- `'computed'`: lowering uses `engine.forwardGrad?` on the metric function (the default).
- `'zero'`: constant metric — `∂g = 0`, `Γ = 0`, `∇_μ = ∂_μ`.
- `'supplied'`: lowering looks up `inputs.metricDerivatives.get(`${gName}/${coordLabel}`)` (parallel to v0.3.5's `inputs.derivatives` for `'symbolic'` tensor-symbols).

### The new `covariant-derivative` node

```typescript
// src/dimensional/connection-validators.ts (new module — parallels metric-validators.ts)
export interface CovariantDerivativeNode {
  readonly kind: 'covariant-derivative';
  readonly of: unknown;           // ExprNode — typed unknown to break the module cycle
  readonly wrt: unknown;          // ExprNode — used for its dim only (same as pderiv)
  readonly wrtIndex: CovariantIndex;
  readonly gLower: MetricTensorNode;
  readonly gInverse: MetricTensorNode;
}
```

The `gLower`/`gInverse` slots are full child nodes — their free indices are consumed internally by the Christoffel formula's contractions and never propagated upward (§7 H1 fix).

### `NumericalInputs` extension

```typescript
export interface NumericalInputs {
  // ... existing v0.3.5 fields ...
  readonly metricDerivatives?: ReadonlyMap<string, NestedArray>;  // keyed `${metricName}/${coordLabel}`
}
```

## 5. The `christoffel()` helper

```typescript
// src/dimensional/connection.ts (new module — parallels metric.ts)
export function christoffel(
  gLower: MetricTensorNode,
  gInverse: MetricTensorNode,
  upper: string,         // λ
  lowerA: string,         // μ
  lowerB: string,         // ν
  xCoord: TensorSymbolNode,  // coordinate field, role='coordinate'
): ExprNode;

export function isChristoffelSymmetric(node: ExprNode): boolean;
```

The fresh contracted label `ρ` (between `g^λρ` and the `∂g` terms) is generated deterministically using the v0.3.0 `freshLabel(<base>, <taken>)` scheme. Symmetry of Γ in `μν` is asserted by the explicit `isChristoffelSymmetric` checker rather than baked into the validator (callers run it when they want).

## 6. AD interface on `TensorEngine`

Two **optional** methods added:

```typescript
export interface TensorEngine {
  // ... existing v0.3.5 methods ...

  /**
   * Forward-mode AD via dual numbers. Returns value + full Jacobian.
   * jacobian.shape = [...value.shape, ...x.shape] (row-major flatten);
   * jacobian[i_y..., i_x...] = ∂y[i_y...] / ∂x[i_x...].
   */
  forwardGrad?(
    fn: (x: EngineTensor) => EngineTensor,
    x: EngineTensor,
  ): { value: EngineTensor; jacobian: EngineTensor };

  /**
   * Reverse-mode AD via tape + backward. Returns value + vector-Jacobian product.
   * gradient.shape === x.shape;
   * gradient[i_x...] = Σ cotangent[y...] · ∂value[y...] / ∂x[i_x...].
   * cotangent defaults to ones-like(value) when value is scalar; required otherwise.
   */
  reverseGrad?(
    fn: (x: EngineTensor) => EngineTensor,
    x: EngineTensor,
    cotangent?: EngineTensor,
  ): { value: EngineTensor; gradient: EngineTensor };
}
```

**Implementations:**

- **`Float64ReferenceEngine.forwardGrad`/`reverseGrad`**: in-house, zero-dep. Dual numbers extend every existing engine op to operate on `{ primal, tangent }` pairs; tape records ops + backward closures, replays in reverse. Standard textbook AD over the existing `Float64Tensor` types.
- **`MathTSEngine.forwardGrad`/`reverseGrad`**: thin adapter — delegates to `@danielsimonjr/mathts-autograd`'s `forwardGrad`/`reverseGrad` public functions. MathTS owns the implementation; UPT's adapter wraps the engine tensors → mathts-tensor `Tensor`s, calls the mathts-autograd function, wraps the result back.

**Convention (Adam L2):** the `christoffel()` lowering pass requests the **full** `∂g` Jacobian in ONE `forwardGrad?` call (using the metric function `x → g(x)` as `fn`). Component-by-component is forbidden by the design. The conformance suite tests that a known-derivative function `fn(x) = x · x` returns the correct full Jacobian in one call.

**Closure convention for non-coordinate parameters:** the lowering pass constructs `fn` from the `metric-tensor` AST + the supplied `NumericalInputs`. Non-coordinate parameters that the metric depends on (e.g., `M_kg` for Schwarzschild) are **captured in `fn`'s closure** by the lowering pass — they do not appear as additional arguments to `forwardGrad?`. The only variable seen by the AD engine is the coordinate tensor `x`.

## 7. Validator changes

### Dedicated `covariant-derivative` case (Eve H1 fix)

The validator's `infer()` recursion gains a `case 'covariant-derivative'`. It validates:
- `wrtIndex.variance === 'lower'` (mirrors pderiv).
- `gLower.indices` both `'lower'`; `gInverse.indices` both `'upper'`; matching `signature`.
- `node.of` validated via `inferArgLocal()` (free indices merged into the result).
- `gLower` and `gInverse` validated **without propagating their free indices upward** — consumed internally by Christoffel contractions. (Mirrors `tensor-partial-derivative`'s `pderiv-ignores-wrt-own-indices` rule.)

Output:
- `dim = divide(of.dim, wrt.dim)` (same as pderiv).
- `freeIndices = of.freeIndices ∪ { wrtIndex.label: lower }` (matches `∂_μ V^ν` and `Γ^ν_μλ V^λ` by construction — Adam verified).

### Duplicate-`derivativeStrategy` warning (Eve M2 fix)

A post-walk pass detects same-name metric-tensor nodes with conflicting `derivativeStrategy`. Emits a `severity: 'warning'` Violation: `DuplicateDerivativeStrategyWarning: metric '<name>' is tagged '<a>' at one site and '<b>' at another`. Deduplicated by `(name, strategyA, strategyB)`. Same pattern as v0.3.5's `InverseMetricInconsistencyWarning`.

## 8. Worked examples — the two new bridges

The Schwarzschild geodesic integrator and connection-layer machinery are SHARED INFRASTRUCTURE. Both bridges sit on top of it.

### BE-?? Gravitational Lensing (primary)

**Bridge identity.** Newtonian-point-mass-gravity ↔ GR weak-field. Eddington (1919) measured the deflection of starlight by the Sun during the solar eclipse — α ≈ 1.75 arcsec per solar radius for grazing rays — confirming Einstein's 1915 prediction `α = 4GM/(bc²)` (twice the Newtonian-corpuscular prediction `2GM/(bc²)`). Modern measurements via radio interferometry confirm to ~10⁻⁴ precision.

**Symbolic encoding.** `src/bridges/equations/be-XX-gravitational-lensing.ts`:
- Schwarzschild inverse metric `g^μν` (already shipped via the BE-37 reuse — same `g_inverse_eikonal` shape).
- The deflection angle is an integral along the null geodesic: `α = 2·∫_b^∞ dφ/dr · dr − π` (one of several equivalent forms). In UPT AST terms: the integrand involves `g^μν` and a `tensor-symbol` for the conserved angular momentum `L` along the geodesic.
- Closed-form scalar: `α = 4·G·M/(b·c²)` (weak-field).
- A `BE_XX_LENSING_LHS` AST encodes the deflection-angle integrand structurally; the v0.3.0/v0.3.5 dimensional-analysis machinery validates it.

**Numerical evaluator.** `evaluateBE_XX_LensingNumerical()`:
1. Set up the Schwarzschild metric at solar parameters (or chosen `M`, `b`).
2. Initial conditions for a null geodesic starting far away with impact parameter `b`, propagating inward, deflecting around the Sun, exiting on the other side.
3. RK4-integrate the null geodesic equation using the v0.4.0 `covariant-derivative`-driven geodesic integrator (8-dim state). Domain restricted to `r ≥ 3·r_Schwarz` and impact parameter `b ≫ r_Schwarz`.
4. Extract the asymptotic deflection angle from the trajectory.
5. Cross-check against `α = 4GM/(bc²)` to **±10⁻⁶ relative** (weak-field).

> **I6 reconciliation (2026-05-15):** the geodesic-integrated cross-validation lives at the end of `tests/bridges/gravitational-lensing.test.ts` ("BE-?? Gravitational Lensing — geodesic cross-validation"). v0.4.0 ships ±10⁻⁶ rather than the originally-promised ±10⁻⁹ — null short-arc RK4 with 100k steps reaches 1e-6 reliably; pushing to 1e-9 needs higher-order integrators (RK45 adaptive or symplectic) which are deferred to v0.5.0.

### BE-?? Mercury Perihelion Precession (secondary)

**Bridge identity.** Newtonian-Kepler-bound-orbit ↔ GR. Le Verrier (1859) measured the anomalous perihelion drift of Mercury — 43 arcsec/century in excess of Newtonian predictions — that Einstein (1915) explained as a GR effect: `Δφ_per_orbit = 6πGM/(a(1−e²)c²)`. For Mercury: `a ≈ 5.79·10¹⁰ m`, `e ≈ 0.206`, `M_sun = 1.989·10³⁰ kg` → `Δφ_per_orbit ≈ 5.04·10⁻⁷ rad` → ≈43 arcsec/century over ~415 Mercury years. Solar System's most famous early confirmation of GR.

**Symbolic encoding.** `src/bridges/equations/be-YY-perihelion-precession.ts`:
- Same Schwarzschild metric reuse.
- Encoded as the per-orbit secular change in the longitude-of-pericenter ω̇ of a bound timelike geodesic.
- Closed-form scalar: `Δφ_per_orbit = 6πGM/(a(1−e²)c²)`.

**Numerical evaluator.** `evaluateBE_YY_PerihelionNumerical()`:
1. Set up the Schwarzschild metric at Mercury-orbit-scale parameters.
2. Initial conditions for a bound timelike geodesic with given `a`, `e`, at perihelion.
3. RK4-integrate for one orbit (≥ 200k steps; multi-orbit accumulation deferred to v0.5.0). Domain restriction maintained throughout (`r ≫ r_Schwarz`).
4. Extract the perihelion-azimuth advance across the orbit.
5. Cross-check against `6πGM/(a(1−e²)c²)` to **±10⁻⁴ relative**.

> **I6 reconciliation (2026-05-15):** the geodesic-integrated cross-validation lives at the end of `tests/bridges/perihelion-precession.test.ts` ("BE-?? Mercury Perihelion — geodesic cross-validation"). v0.4.0 ships one-orbit ±10⁻⁴ rather than the originally-promised N-orbit ±10⁻⁹ — timelike multi-orbit RK4 accumulates error faster than null short-arc; pushing to 1e-9 needs higher-order integrators (symplectic Verlet/leapfrog) and adaptive step-size, deferred to v0.5.0.

### BE-37 covariant-eikonal preview (structural-equivalence test, tertiary)

A small structural test: `g^μν (∇_μ S)(∇_ν S) = 0` for the scalar `S_eikonal` from v0.3.5's BE-37. For scalar `of`, `∇_μ S = ∂_μ S` (Christoffel terms cancel). The numerical result must match v0.3.5's `evaluateBE37EikonalNumerical()` eikonal residual to **machine precision**. Confirms the Part-VIII §VIII.8 "only the operator changes" promise structurally.

## 9. Testing strategy

Seven layers:

| Layer | Files | Coverage |
|---|---|---|
| **Engine-conformance** | `tests/numerical/engine-conformance.ts` + `.float64.test.ts` + `.mathts.test.ts` | Existing 17 + new AD-conformance subset: `forwardGrad?` / `reverseGrad?` on `fn(x) = x · x` return correct Jacobian/gradient. Capability-detected. |
| **MathTS-autograd unit tests** | `~/Dropbox/Github/Mathts/autograd/tests/*.test.ts` | New package's own tests — forwardGrad / reverseGrad on a battery of known-derivative functions; tape correctness for reverse-mode; dual-number arithmetic for forward-mode. |
| **Lowering-contract** | `tests/numerical/lowering-contract.test.ts` | Existing + new: `covariant-derivative` lowering; `christoffel()` helper output shape. |
| **Numerical-correctness** | `tests/numerical/correctness.test.ts` | Existing + new: polar-coordinate Γ test (flat plane in `diag(1, r²)`; Γ^r_φφ = −r, Γ^φ_rφ = 1/r — closed-form match). |
| **einsum-property** | `tests/numerical/einsum-properties.test.ts` | Unchanged. |
| **Bridge integration** | `tests/bridges/be-XX-gravitational-lensing.test.ts` (new), `tests/bridges/be-YY-perihelion-precession.test.ts` (new), `tests/bridges/be-37-covariant-eikonal.test.ts` (new) | The three §8 worked examples — two bridges at ±10⁻⁹ + the structural preview. |
| **`derivativeStrategy`-preservation regression** | `tests/dimensional/derivative-strategy-preservation.test.ts` (new) | `derivativeStrategy` survives raise/lower. |

Plus the Part-IX spec↔impl drift-guard test (new): `tests/dimensional/part-ix-spec-vs-impl.test.ts`.

Estimated ~70-90 new tests (more than v0.3.5's ~50 since two bridges instead of one). Target post-v0.4.0 suite: ~1,445-1,470 passing.

## 10. Sequencing (preview — implementation plan owns the canonical version)

`[U]` = UPT repo · `[M]` = MathTS repo. The [M] tasks are larger this time — a whole new `mathts-autograd` package — and run in parallel with the [U] track.

| # | Track | Phase | Depends on |
|---|---|---|---|
| 1 | [U] | `derivativeStrategy` field on `metric-tensor` + serialization round-trip | — |
| 2 | [U] | `CovariantDerivativeNode` interface + `connection-validators.ts` + dedicated validator case (H1 fix) | #1 |
| 3 | [U] | `christoffel()` helper + `isChristoffelSymmetric` + unit tests | #1 |
| 4 | [U] | `metricDerivatives` field on `NumericalInputs` + `'supplied'` lowering path | #1 |
| 5 | [M] | New workspace package `@danielsimonjr/mathts-autograd` — scaffold (mirror `tensor/` package config) | — |
| 6 | [M] | mathts-autograd: forward-mode AD (DualTensor + `forwardGrad(fn, x)` static function) + unit tests | #5 |
| 7 | [M] | mathts-autograd: reverse-mode AD (Tape + `reverseGrad(fn, x, cotangent?)` static function) + unit tests | #5 |
| 8 | [U] | `forwardGrad?` interface + `Float64ReferenceEngine` in-house implementation (dual numbers) | — |
| 9 | [U] | `reverseGrad?` interface + `Float64ReferenceEngine` in-house implementation (tape) | #8 |
| 10 | [U] | `MathTSEngine.forwardGrad?` / `reverseGrad?` adapters delegating to mathts-autograd | #6, #7, #8, #9 |
| 11 | [U] | AD-conformance suite extension (capability-detected; tests both modes per engine) | #8, #9, #10 |
| 12 | [U] | `covariant-derivative` numerical lowering (three `derivativeStrategy` paths) | #2, #8, #4 |
| 13 | [U] | Duplicate-`derivativeStrategy` warning + `InverseMetricInconsistencyWarning` extension | #1, #12 |
| 14 | [U] | Schwarzschild geodesic integrator (extends v0.3.5's `null-ray-integrator` for the 8-dim state) | #8, #12 |
| 15 | [U] | **BE-?? Gravitational Lensing** symbolic + numerical evaluator + ±10⁻⁹ acceptance test | #2, #3, #8, #12, #14 |
| 16 | [U] | **BE-?? Mercury Perihelion Precession** symbolic + numerical evaluator + ±10⁻⁹ acceptance test | #2, #3, #8, #12, #14 |
| 17 | [U] | BE-37 covariant-eikonal preview test (machine-precision match to v0.3.5) | #2, #12 |
| 18 | [U] | 7-layer test-suite consolidation + `derivativeStrategy`-preservation regression + Part-IX spec | #1-17 |
| 19 | [U] | API-stability pass — snapshot extension + `@public`/`@internal` tags on new exports | #1-18 |
| 20 | [U]+[M] | Release — see §17 release runbook (publish BOTH MathTS packages; promote BOTH deps; flip default) | All above |

## 11. Success criteria

1. ✅ `covariant-derivative` node + validator case (free-index union; embedded metrics consumed internally)
2. ✅ `christoffel()` helper + `isChristoffelSymmetric` checker
3. ✅ `derivativeStrategy` field on `metric-tensor`; v0.3.x tests pass unchanged (zero migration)
4. ✅ `@danielsimonjr/mathts-autograd` v0.1.0 package built in the MathTS monorepo with forward + reverse AD; own unit tests pass
5. ✅ `forwardGrad?` + `reverseGrad?` on `TensorEngine`; `Float64ReferenceEngine` implements in-house; `MathTSEngine` adapts mathts-autograd; both pass identical conformance suite
6. ✅ Numerical lowering of `covariant-derivative` works for all three `derivativeStrategy` values
7. ✅ Duplicate-`derivativeStrategy` warning fires; `InverseMetricInconsistencyWarning` continues to work
8. ✅ **BE-?? Gravitational Lensing** end-to-end at **±10⁻⁹ relative** vs `α = 4GM/(bc²)` (weak field, `b ≫ r_Schwarz`)
9. ✅ **BE-?? Mercury Perihelion Precession** end-to-end at **±10⁻⁹ relative** vs `N × 6πGM/(a(1−e²)c²)` over N=10 orbits
10. ✅ **BE-37 covariant-eikonal preview**: scalar result matches v0.3.5 to machine precision
11. ✅ 7-layer test suite green; `derivativeStrategy` survives raise/lower
12. ✅ `MathTSEngine` becomes the registry default at release (per §17)
13. ✅ Part-IX spec written + spec↔impl drift-guard test green
14. ✅ Full UPT test suite passes; target ~1,445-1,470 passing
15. ✅ CHANGELOG `[0.4.0]` block written; cut, tag, push. SemVer MINOR (additive `derivativeStrategy` + new `covariant-derivative` node + new AD methods + new bridges; no breaking changes)

## 12. North-star architecture refresh (TensorJS readiness)

| Release | Bridges added | Symbolic layer | Numerical layer | MathTS additions | Default engine |
|---|---|---|---|---|---|
| v0.1.0–0.3.1 *(shipped)* | 40-bridge catalog | scalars → tensor algebra → metric | — | — | — |
| v0.3.5 *(shipped)* | BE-37 numerical | unchanged | TensorEngine + 2 engines | rank-N Tensor + einsum (`mathts-tensor` 0.1.0) | Float64ReferenceEngine |
| **v0.4.0 *(this design)*** | **+ BE-?? Lensing, BE-?? Perihelion** | **+ `covariant-derivative` + `christoffel()` + `derivativeStrategy`** | **+ forward + reverse AD on TensorEngine** | **+ `mathts-autograd` 0.1.0** | **MathTSEngine** (release-time flip) |
| v0.5.0+ | curvature bridges (Friedmann, Raychaudhuri, etc.) | + Riemann/Ricci + Faraday BREAKING refactor | + higher-order AD; float32 browser engine | + real perf optimizations (BLAS/SIMD) | unchanged |
| TensorJS v1.0 | Stable bridge surface | Stable public API | Stable engine surface + ≥2 engines | Stable MathTS API | — |

v0.4.0's connection layer + two new bridges are the load-bearing prerequisite for v0.5.0+ curvature-flavored cosmology bridges (Friedmann + Raychaudhuri need Riemann, which is built from `∇_μ Γ`-of-`Γ`). The AD interface forward-composes cleanly to second derivatives for v0.5.0 Riemann work.

## 13. Risks and open questions

### Resolved at lock point (Adam + Eve), unchanged by the post-review scope reshape

- **Free-index scanner edge on `covariant-derivative`'s embedded metrics** (Eve HIGH H1) — resolved: dedicated validator case (§7).
- **Schwarzschild coordinate singularity at r = 2M** (both HIGH H2) — resolved: integration domain `r ≥ 3·r_Schwarz`. Mercury perihelion test maintains this margin trivially (Mercury's orbit at `a ≈ 5.8·10¹⁰ m` vs `r_Schwarz_sun ≈ 2.95 km` — ratio ~10¹⁰); lensing test must restrict impact parameter `b ≫ r_Schwarz` (which the weak-field formula `α = 4GM/(bc²)` already assumes).
- **AD interface shape asymmetry** (Eve HIGH H3) — resolved: shapes pinned in §6.
- **Release-process fragility on dep flip** (both MEDIUM M1) — **now affects TWO packages**, not one (mathts-tensor v0.1.0 publish from v0.3.5 + mathts-autograd v0.1.0 new in v0.4.0); release runbook §17 covers ordered double publish + double dep promotion + flip.
- **`derivativeStrategy: 'supplied'` needs public-type plumbing + duplicate-tag warning** (Eve MEDIUM M2) — resolved.
- L1–L4 — all resolved or deferred (see prior commit's decision log).

### New (post-reshape)

- **Two-package publish coordination.** v0.4.0's release runbook depends on Daniel publishing BOTH `mathts-tensor` (a v0.3.5-deferred publish that still needs to happen) AND the new `mathts-autograd`. If either publish fails or is delayed, UPT v0.4.0 can ship in two reduced postures: (i) **without the engine-default flip** — `MathTSEngine` stays opt-in, connection-layer features still work via `Float64ReferenceEngine`'s in-house AD; (ii) **without the `mathts-autograd` adapter** — only `Float64ReferenceEngine` gets AD; `MathTSEngine.forwardGrad?` / `reverseGrad?` remain unimplemented (conformance suite capability-detects, so MathTS conformance tests on AD skip, not fail). Both fallbacks are graceful — the connection-layer bridges still pass on `Float64ReferenceEngine`.

### Open questions for written-spec review

1. **Bridge numbering.** The two new bridges need free `BE-??` numbers from `src/bridges/index.ts`. Implementer picks at phase 15/16 time.
2. **MathTS-autograd public API surface.** v0.1.0's exports — only `forwardGrad` / `reverseGrad` functions + `DualTensor` / `Tape` classes (for advanced users)? Or also lower-level dual-number primitives? Recommendation: keep v0.1.0 minimal (`forwardGrad`, `reverseGrad`, `DualTensor`, `Tape`); add lower-level primitives as needed.
3. **Lensing impact-parameter range.** The acceptance test runs at a fixed `b`; should it sweep a range (b = 2·R_sun, 5·R_sun, 10·R_sun) to verify the `1/b` scaling? Recommendation: yes — the sweep is a stronger test, costs little.

## 14. Forward-compatibility sanity checks

### 14.1–14.3, 14.6, 14.7 from prior version

- `derivativeStrategy` field, `covariant-derivative` node, AD methods — all additive (§14.1–.3).
- Faraday-cascade BREAKING refactor stays in v0.5.0+ (§14.6).
- Non-Levi-Civita connections — additive variant `covariant-derivative-general` possible in v0.5.0+ (§14.7).

### 14.4 v0.5.0 curvature numerical composition
Riemann involves `∂_ρ Γ^μ_νσ` — second-order AD (`forwardGrad` of `forwardGrad`). The v0.4.0 interface composes cleanly: `engine.forwardGrad(x => engine.forwardGrad(g, x).jacobian, x)` works structurally. `mathts-autograd` can grow a `forwardHessian` convenience in v0.5.0+ if needed.

### 14.5 v0.5.0+ AST-size optimization
v0.4.0 documents the issue (each `covariant-derivative` carries full `gLower`/`gInverse` trees; curvature creates Γ-of-Γ → quadratic growth); v0.5.0+ refactor candidates: metric-name-pointer or CSE table. Additive.

### 14.8 NEW — mathts-autograd package extensibility
`@danielsimonjr/mathts-autograd` v0.1.0 ships forward + reverse AD on `mathts-tensor` `Tensor`s. Future v0.x: higher-order AD (`forwardHessian`, mixed-mode VJP-of-JVP), GPU autograd backends, AD over symbolic expression-trees (mathjs-bridged). All additive to v0.1.0's `forwardGrad` / `reverseGrad` public surface.

### 14.9 NEW — threejs viz consumption surface
v0.4.0's geodesic integrator returns trajectories as `Array<{ τ, x: number[], dxdτ: number[] }>` — naturally consumable by future threejs viz (a trajectory is just a sequence of 4-positions to render as a polyline in spacetime). No viz code in v0.4.0; the data shape is the contract.

## 15. Cross-validation record

Single-pass consolidated lock-point review of the **architectural decisions** before the design doc was written:

- **Adam (Gemini 2.5 Pro)**: `COHERENT`, confidence **0.90**. No critical defects, no internal contradictions. Independently traced the free-index union rule and confirmed it holds.
- **Eve (OpenAI o3, full)**: `COHERENT-WITH-GAPS`, confidence **0.65**. Same overall verdict; raised H1 (free-index scanner edge — singular HIGH only she caught), H3 (AD interface shape asymmetry), and the M-level packaging concerns.

All 3 HIGH findings, both MEDIUM findings, and all 4 LOW findings were folded into the initial design before commit. **Post-review scope + packaging reshape** (the two new bridges + `mathts-autograd` sister package, both from Daniel's north-star pull) is orthogonal to the architectural locks — it changes WHAT v0.4.0 delivers but not HOW the connection layer, validator, AD interface, or Schwarzschild domain restriction work. A second adversarial pass on the reshaped scope is OPTIONAL (and would mostly verify the bridge-encoding shape against the existing GR-physics literature — not a UPT-architecture question).

Eve's "30-40 kB extra code" number on M1 was unsourced and ignored. No reviewer cited identifiers requiring grep-verification.

## 16. Decision log

| # | Decision | Chosen | Source |
|---|---|---|---|
| 1 | Scope | **All-in**: connection layer + full AD + MathTSEngine-default | User pick |
| 2 | AST representation | **`∇_μ` as a new ExprNode kind; `Γ` as a helper** | User pick (v0.3.0 raise/lower precedent + Part-VIII §VIII.8 hint) |
| 3 | AD mode | **Both forward + reverse**, as optional `engine.forwardGrad?` / `engine.reverseGrad?` | User pick |
| 4 | `varying`-field decision (deferred v0.3.0 → v0.4.0) | **`derivativeStrategy: 'computed' \| 'zero' \| 'supplied'`** on `metric-tensor` | User pick (generalization of the original `varying: boolean`) |
| 5 | `covariant-derivative` shape | **`{of, wrt, wrtIndex, gLower, gInverse}`** — explicit metric pair on the node | User pick |
| 6 | MathTSEngine-default flip timing | **At v0.4.0 release**; honest framing: code-path-signal + dep-shape, not perf | User pick |
| 7 | Numerical worked examples / acceptance | **Two new bridges (BE-?? Lensing + BE-?? Perihelion) at ±10⁻⁹ each + BE-37 covariant-eikonal preview (machine precision)** | User pick (post-review reshape — north-star pull) |
| 8 | MathTS AD positioning | **New sister package `@danielsimonjr/mathts-autograd`** (forward + reverse modes; depends on mathts-tensor); UPT's MathTSEngine adapts; Float64ReferenceEngine implements in-house | User pick (post-review reshape — north-star pull) |
| 9 | Dedicated validator case for `covariant-derivative` | **Yes** — embedded metrics' indices consumed internally | Eve HIGH H1 — resolved |
| 10 | Schwarzschild integration domain | **r ≥ 3·r_Schwarz** | Adam + Eve HIGH H2 — resolved |
| 11 | AD return-shape convention | **forward**: `jacobian.shape = [...value.shape, ...x.shape]`; **reverse**: `gradient.shape = x.shape` + optional `cotangent` | Eve HIGH H3 — resolved |
| 12 | Release runbook for the publish + flip | **§17** — ordered steps for TWO-package publish + double dep promotion + flip | Both MEDIUM M1 — resolved + reshape-updated |
| 13 | Duplicate-`derivativeStrategy` warning | **Yes** — `severity: 'warning'` Violation | Eve MEDIUM M2 — resolved |
| 14 | "Cancel structurally" → "match by construction" | **Yes** | Adam LOW L1 — resolved |
| 15 | Batched-Jacobian convention for `christoffel()` lowering | **Mandatory** — one `forwardGrad?` call per metric | Adam LOW L2 — resolved |
| 16 | AST-size blow-up | **Deferred to v0.5.0+** | Eve LOW L3 — deferred |
| 17 | Optional-vs-required ambiguity on AD methods | **Optional on interface; both v0.4.0 engines implement; conformance capability-detects** | Eve LOW L4 — resolved |
| 18 | Two-package publish fragility | **Graceful-degradation runbook** (engine-default flip optional if a publish slips; MathTSEngine.forwardGrad skip-detected by conformance) | Post-reshape risk — addressed |

## 17. Release runbook

The MathTSEngine-default flip is one coordinated release step that now involves **two packages**. Ordered procedure (executed at v0.4.0 release time, NOT during development):

### Phase A — MathTS publishes (Daniel-manual, 2FA required)

1. **Publish `@danielsimonjr/mathts-tensor@0.1.0`** (deferred from v0.3.5): `cd ~/Dropbox/Github/Mathts/tensor && npm publish --access public`.
2. **Publish `@danielsimonjr/mathts-autograd@0.1.0`** (new in v0.4.0): `cd ~/Dropbox/Github/Mathts/autograd && npm publish --access public`.
3. **Verify** both resolve from npm: `npm view @danielsimonjr/mathts-tensor versions` shows `0.1.0`; same for `mathts-autograd`.

### Phase B — UPT promotes deps + flips default

4. **Promote `mathts-tensor`** in UPT `package.json`: move from `optionalDependencies` to `dependencies` (same `^0.1.0` semver range).
5. **Add `mathts-autograd`** as a regular `dependency` in UPT `package.json` at `^0.1.0`.
6. **Run `npm install`** in UPT to drop the `npm link`s and pull both published packages. Verify both are real packages (not symlinks).
7. **Re-run the conformance gate**: `npx vitest run` — full suite green with both published-package bindings.
8. **Flip the registry default**: in `src/numerical/engine-registry.ts`, change `let active = new Float64ReferenceEngine()` → `let active = new MathTSEngine()`.
9. **Update CHANGELOG v0.4.0 block**: honest framing — code-path-signal + dep-shape change, NOT perf change.

### Phase C — Version + tag + push

10. **Bump UPT version**: `package.json` `"0.3.5"` → `"0.4.0"`.
11. **Release gate**: `npm run build && npx vitest run` — `tsc` clean; full suite ~1,445-1,470 green.
12. **Commit**: `chore(release): v0.4.0 — connection layer + 2 GR bridges + mathts-autograd`.
13. **Tag**: `git tag -a v0.4.0 -m "release: v0.4.0 — connection layer + Lensing + Perihelion + mathts-autograd"`.
14. **Push**: `git push origin master --tags`.

### Phase D — Memory update

15. **Update `~/.claude/projects/C--Users-danie/memory/project_upt.md`**: v0.4.0 shipped; latest commit/tag; the two new bridges; the connection-layer architecture; the two MathTS packages (`mathts-tensor` + `mathts-autograd`); MathTSEngine-default.

### Graceful-degradation paths

If Phase A's publishes slip:
- **If only `mathts-autograd` publish slips**: ship v0.4.0 WITHOUT the engine-default flip (Phase B step 8) and WITHOUT the `mathts-autograd` dep (skip step 5). `MathTSEngine.forwardGrad?` / `reverseGrad?` remain unimplemented; conformance suite capability-detects and skips MathTS AD tests. Connection layer + bridges still pass on `Float64ReferenceEngine`'s in-house AD. Retag the partial release as v0.4.0; the full default-flip + mathts-autograd ships as v0.4.1 once published.
- **If `mathts-tensor` publish slips too**: ship v0.4.0 with NO MathTSEngine changes. `MathTSEngine` remains opt-in via the v0.3.5 npm-link path during dev; consumers use `Float64ReferenceEngine` (the current default) and never touch MathTS code. Full Phase B re-attempts in v0.4.1.

Either fallback is documented in the v0.4.0 CHANGELOG with explicit "MathTSEngine availability gated on…" framing.
