# v0.10.0 Improvement Plan — Phase C/D Closure, Uncertainty, Graph Growth

> 2026-06-11, branch `claude/bridge-equations-specs-review-4mfy38`.
> Source assessment: the post-v0.9.0 "what else needs improvement"
> review. **Version decision:** package bumps 0.8.0 → **0.10.0** at this
> plan's wrap; the intermediate v0.8.0/v0.9.0 milestones roll into the
> single v0.10.0 tag at final HEAD (repo precedent: v0.5.1→v0.7.0 rolled
> into one tag). CHANGELOG keeps the per-milestone sections.
> **Vet note:** executed without a fresh Adam pass (honest deviation) —
> this batch contains no new physics formulations: Phase-C items are
> negative tests, Phase-D is graph mechanics, uncertainty is standard
> first-order propagation; all TDD-gated. The edge tranche reuses
> existing validated evaluators only.

## Executed in this cycle

### T1 — CI gate gap (release logistics)
`ci.yml` predates the strict tests-typecheck gate: add
`npx tsc -p tsconfig.tests.json` as a step, plus a grep-guard step for
the diff-gate's known blind spot (`as unknown as number[][]`-style
casts on fixture results in NEW code are allowed only with the
sanctioned-idiom comment). Refresh CLAUDE.md's release-state paragraph.

### T2 — Part-IX Phase C: stress tests (≥2, PRE-REGISTERED here)
Compositions that MUST fail diagnosably:
- **ST-1 (junction/dimension mismatch):** `composeEdges(be12Edge,
  be16Edge)` — thermal de Broglie outputs a LENGTH named
  `thermal-de-broglie-wavelength`; Landauer wants `temperature` [Θ]. No
  name match, no registered identification → `CompositionJunctionError`
  naming both quantity sets. Then force it with an ad-hoc
  identification `{from: 'thermal-de-broglie-wavelength', to:
  'temperature'}` → must throw `CompositionDimensionError` citing
  [length] vs [temperature] — the dimension functor refusing a
  physics-nonsense junction even when a human asserts it.
- **ST-2 (domain incompatibility through the pipe):** compose the
  Schwarzschild-radius law into `be51Edge` via an identification
  `schwarzschild-radius → impact-parameter` (physically: a photon
  grazing AT r_s). The junction type-checks ([length]=[length]) but
  be51's weak-field domain `b ≥ 10·r_s` is violated by construction →
  `DomainViolationError` at evaluation. Acceptance: the composed edge
  EXISTS (compose succeeds — the failure is correctly attributed to
  domain, not type).
Part-IX §6 stress-test bar (≥2 surviving stress tests = failures that
the framework names correctly) recorded as MET when both pass.

### T3 — Part-IX Phase D: novel-candidate enumeration
`enumerateCompositions(edges, opts)` in `src/composition/enumerate.ts`:
all ordered pairs where `composeEdges` succeeds (name or registered
identification), partitioned into REGISTERED (the CT-* set, by id) and
NOVEL. Emit `docs/research/v0.10.0-novel-candidates.md` for physicist
review. Pre-identified expected member: `be-42 >> be-12` — the thermal
de Broglie wavelength of a particle at the Hawking temperature,
λ_T(m, T_H(M)) — physically meaningful (the wavelength of thermally
emitted quanta vs horizon scale) and NOT pre-registered. Acceptance:
enumerator finds every CT-* composition (completeness check against
the registered set) and ≥1 novel candidate; the report documents each
candidate's formula, dimensions, confidence, and review status
(NO status promotion — P-5 protocol).

### T4 — Uncertainty propagation (the unfulfilled G-3 claim)
`src/composition/uncertainty.ts`: `propagateUncertainty(edge, inputs,
sigmas)` — first-order: σ_out² = Σᵢ (∂f/∂xᵢ · σᵢ)², central-difference
Jacobian (h = 1e-6·max(|xᵢ|, ε) relative step). Because composed edges
ARE edges (D-2 closure), propagation through chains needs no extra
machinery. Wire into the GW170817 confrontation:
`confrontBE36WithUncertainty()` propagates Δt = 1.74±0.05 s into the
positive bound → σ ≈ (0.05/1.74)·6.502e-16 ≈ 1.9e-17. Acceptance:
linear-edge analytic check (σ_out = |a|·σ_in exact to 1e-9 rel);
CT-1 chain σ scaling ∝ σ_M/M; GW bound ±σ pinned.

### T5 — Graph growth tranche (toward the full migration)
Six catalog-backed edges wrapping EXISTING validated evaluators
(be-14 RT, be-19 LQC bounce, be-21 KSS, be-48 GRW, be-53 YM β,
be-54 RS brane), with G-8 domains and sparse attributes. Delegated;
evaluator signatures verified from modules before wrapping. Grows the
graph 10 → 16 edges; the full-44 migration remains the v0.11 headline
(quantity-naming judgment is the bottleneck, per the v0.8.0 design).

### T6 — Research-note draft (the human-review vehicle)
`docs/research/v0.10.0-Composition-Research-Note.md` (~2 pages):
pre-registration methodology, the three realized calibration targets
with literature anchors, the GW170817 confrontation including the
honest symmetric-vs-asymmetric bound finding, limitations (toy scale,
LLM-only review, P-5 protocol). The artifact to send to physicists;
Zenodo DOI minting stays a user action.

### T7 — Wrap
package.json → 0.10.0; CHANGELOG `[0.10.0 — Unreleased]` section
consolidating; todo.md release-decision entry (recommend: single
v0.10.0 tag at final HEAD); CLAUDE.md refresh.

## Explicitly deferred (queued, not dropped)

- **v0.11 headline:** full 44-edge catalog→graph migration (+ diagonal
  laws growth) — bottleneck is per-bridge quantity naming, a reviewable
  physics task (CONTRIBUTING).
- C2 (Einstein-Cartan Newtonian limit) + C3 (Higgs→Λ residue)
  calibration targets — C2 needs weak-field-limit machinery that does
  not exist yet.
- Second data confrontation (BE-21/BE-23 vs condensed-matter data).
- O-4 flat migration of computeKretschmann/computeWeylTensor (removes
  the unflatten shims); sibling-fixture layout unification.
- G-9 units layer (design note ready, one-foundation-change rule).
- Klein-Gordon numerical evaluator.
- BE-44/46/50 contested adjudications + Zenodo DOI + actually sending
  the research note — human actions.
