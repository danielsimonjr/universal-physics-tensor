# UPT v0.7 — Proposal 1: Intelligent Index Layer Wrapping Einsum-Style Tensors — Design

**Generated**: 2026-05-23 (post-v0.6.1 hygiene sprint).
**Source**: `docs/planning/UPT v0.70 - Proposals.md` §2 (also §0, §1, §11, §12).
**HEAD at design time**: tip of `master` (v0.6.1 prep complete).
**Pre-vet count**: 0 findings (initial draft — no Adam+Eve pass yet).
**Effort target** (per proposals doc §0): 2-3 weeks, classified Low risk —
this design adjusts to **Medium risk** pending Phase 0 confirmation of
the adapter-on-top reconciliation (Decision #1, Risk 1). Risk returns to
Low if Phase 0 closes cleanly; re-classifies upward if it forces a
refactor of `computeContraction` / `freeIndices` threading.

> **Sister docs**:
> - `docs/planning/UPT v0.70 - Proposals.md` (the seed proposals).
> - `docs/planning/v0.6.0-Design.md` (house-style template — v0.6.0 also
>   introduced architecturally substantial new node kinds; this proposal
>   reuses its TDD-per-phase shape).
> - `docs/planning/v0.6.1-Design.md` (most-recent reconciliation-style
>   design with Adam+Eve fingerprints).
> - `docs/architecture/ARCHITECTURE.md` (the system the wrapper sits on).

---

## Goal

Ship **v0.7.0** as the **Intelligent Index foundation release**:
introduce a UPT-level identity-bearing index type (`UniversalIndex<Axis>`)
plus a wrapper (`LabeledTensor`) over the existing `TensorEngine`
einsum surface, such that physics-axis identity, name, and metadata
are carried alongside every engine-level tensor without altering the
engine compute contract. Three interlocking themes:

1. **Capture physics-axis identity at the wrapper level.** UPT
   already has labeled indices on the AST side (`TensorSymbolNode.indices`,
   `freeIndices` Maps — `src/dimensional/tensor.ts:30-87`,
   `src/dimensional/validator.ts:122-152`), but they are
   *string-equality based and scoped to a single AST validation*.
   They carry no persistent identity across catalog entries: two
   bridges that both contract a "scale-quantum" axis use the string
   `'μ'` (or whatever the bridge author chose) with no enforced
   registry link. Proposal 1 introduces that registry.

2. **Wrap engine-level tensors with axis-tagged metadata.**
   `LabeledTensor` composes an `EngineTensor` (the opaque handle
   from `src/numerical/tensor-engine.ts:22-24`) + a `TensorEngine` +
   a `Labels: Record<string, UniversalIndex<AxisName>>` map.
   `contract(other)` matches by **`UniversalIndexId` identity**, not
   by einsum-string coincidence. The engine call itself is unchanged.

3. **Establish the wrapper as the catalog-facing surface, NOT the
   AST-facing one.** The dimensional layer's existing labeled-index
   system stays intact in v0.7.0. The Intelligent Index layer sits
   as an **adapter** between AST-level free indices and engine-level
   positional axes — it does not subsume either. This reconciliation
   is the most important decision in this proposal (Decision #1).

**Non-goals** (deferred to v0.8.0+ or later):

- Replacing `TensorSymbolNode.indices` with `UniversalIndex` references
  (the breaking-refactor variant of reconciliation — would re-classify
  proposal as High risk; deferred unconditionally).
- Bulk migration of all 42 v0.6 bridges to `Axes` registry. Proposals
  doc §2.4 lists this as acceptance; v0.7.0 narrows to **one**
  representative bridge in Phase 4.
- ITensor's QN-aware index sectors (Section 8.2). UPT's physics-axis
  identity does NOT generalize to U(1)/Z_n-conserving block-sparse
  storage in v0.7.0.
- The `tools/migrate-strings-to-indices.ts` codemod (proposals doc §2.4).
  Deferred alongside bulk migration.
- **`prime` mark and `arrow` direction fields** on `UniversalIndex`.
  **UNVERIFIED**: the brief that pointed this designer at Proposal 1
  mentions "name, dimension, prime mark, and arrow (per the ITensor
  design)," but proposals doc §2.2 itself does NOT include prime or
  arrow on the sketch (`id`, `axis`, `name`, `tags`, `limits`, `notes`
  only). See Decision #2.
- Replacing einsum-string-based contraction inside the engines. They
  stay positional; the wrapper is the identity layer.
- Compile-time axis-mismatch detection via template-literal types
  (Decision #7 — v0.7.0 ships runtime detection; compile-time is
  v0.8.0+ research).

---

## Tag Decision

**v0.7.0 (minor bump, non-breaking)**. SemVer applies from v0.1.0 per
CLAUDE.md. The Intelligent Index layer is purely additive: new types,
new wrapper class, new optional helpers. Existing `evaluateNumerical()`,
`validate()`, the `TensorEngine` interface, `TensorSymbolNode`,
`computeContraction`, and the 42-bridge catalog remain untouched.

If during execution the adapter-on-top shape becomes infeasible, the
release re-scopes to v0.8.0 *with* a breaking-change carry-forward
through a fresh Adam+Eve pass — this design does NOT silently re-classify.

---

## Architectural Decisions (locked)

### Decision #1 — Intelligent Index is an **adapter on top** of the existing dimensional + engine layers

The architecturally pivotal decision. Three plausible relationships
between the Intelligent Index layer and UPT's existing labeled-index
system (`TensorSymbolNode.indices` + `computeContraction` +
`freeIndices`):

| Shape | Description | Effort | Risk |
|---|---|---|---|
| (A) Adapter | New `LabeledTensor` wraps `EngineTensor`; AST is unchanged; bridging happens at the evaluator surface only. | 2-3 weeks | Low |
| (B) Unified | `TensorSymbolNode.indices` rewired to reference `UniversalIndex` objects; `computeContraction` learns axis identity. | 5-8 weeks | High |
| (C) Parallel | Two independent index systems coexist forever; the wrapper does NOT consult the AST's free indices. | 2-3 weeks | Low (technically) but creates long-term coherence debt. |

**v0.7.0 locks shape (A) — adapter on top.** Concretely:

- `TensorSymbolNode.indices: ReadonlyArray<TensorIndex>` stays exactly
  as defined in `src/dimensional/tensor.ts:30-34`. No rewiring.
- `computeContraction` stays as the AST-layer Einstein-contraction
  primitive (`src/dimensional/tensor.ts:182-235`). No rewiring.
- `LabeledTensor` is introduced as a **new class** in
  `src/core/labeled-tensor.ts` owning an `EngineTensor` + a
  `TensorEngine` + a labels record.
- The bridge between AST `freeIndices` and wrapper `Labels` is
  built **only for evaluator entry points that opt in** (Phase 4
  demonstrates this for one bridge). Other evaluators continue to
  flow `ExprNode → validate → lowering → engine` as in v0.6.1.

Shape (C) rejected: parallel string-keyed and identity-keyed systems
are exactly the redundancy proposals doc §2.2 flags ("two strings
that happen to be equal"). Shape (B) rejected as out-of-scope for one
release.

**Rationale.** v0.6.0 set the precedent (`CurvatureCompositeNode<K,S>`
factory, v0.6.0-Design Decision #5) that bulk migrations are NOT free
even when LOC delta favors them. Bulk-migrating 42 bridges to
`UniversalIndex` would force `TensorSymbolNode.indices` to either
change shape (breaking v0.6.0's API contract) or carry parallel label
systems on every node (v0.5.0 ricci-slot-trap pattern). v0.7.0 keeps
both worlds intact and demonstrates the wrapper on the catalog-facing
edge.

### Decision #2 — `UniversalIndex` ships with `axis`, `name`, `tags`, `limits?`, `notes?` — **NOT prime or arrow** in v0.7.0

The brief specifying Proposal 1 mentions "name, dimension, prime mark,
and arrow (per the ITensor design)." Proposals doc §2.2 sketch does
NOT include `prime` or `arrow` (only `id`, `axis`, `name`, `tags`,
`limits`, `notes`).

**UNVERIFIED**: Whether ITensor's Index actually carries an arrow
direction field (in/out, dagger-relevant for QN sectors) and whether
prime marks are first-class on the Index or a separate `prime(index)`
operation. The proposals doc cites Section 3 and 8.2 but paraphrases
through the CHANGELOG-grounded investigation in §0/§1, not by direct
re-reading. Phase 0 Task 0.6 closes this either by direct paper read
or by locking the §2.2 sketch as the canonical v0.7.0 set.

**v0.7.0 ships only the §2.2 sketch fields**: `id: UniversalIndexId`
(branded string from `crypto.randomUUID()`), `axis: AxisName` (the
discriminator), `name: string`, `tags: ReadonlySet<string>`,
`limits?: string`, `notes?: string`.

**Deferred to v0.8.0+** (with explicit verification gate): `prime:
number` (ITensor-style bra/ket distinguishing), `arrow: 'in' | 'out'`
(ITensor QN-aware direction). If a v0.7.0 acceptance test ends up
*needing* prime or arrow, the release re-scopes; it does NOT silently
extend the type mid-cycle.

### Decision #3 — `LabeledTensor.contract(other)` matches by `UniversalIndexId` (UUID), not by `axis` or `name`

The contract algorithm:

1. `shared = labels(this) ∩ labels(other)` by full `UniversalIndexId`
   equality.
2. Each shared id becomes one `EinsumContraction` over the matching
   `(operand, axis)` coordinates.
3. The non-shared ids in both operands become the free axes of the
   result, in `this`-first, `other`-second order.
4. Call `engine.einsum(spec, this.tensor, other.tensor)`.
5. Return `new LabeledTensor(result, remainingLabels)`.

Strictly identity-based. Two `UniversalIndex` objects with the same
`axis` and `name` but distinct `id`s do NOT contract. The `Axes`
registry (Decision #4) guarantees same-object identity at the
consumer surface.

### Decision #4 — `Axes` is the canonical singleton registry, built at module load

`Axes.scale.quantum`, `Axes.scale.classical`, `Axes.force.electromagnetic`
etc. are stable references created exactly once per module load. The
implementation pattern uses `Object.freeze` at both the outer
namespace and the per-axis sub-objects. The exact count of regimes
per axis is **deferred to Phase 0 Task 0.1** (axis census against
`src/bridges/index.ts`) — the proposals doc §2.4 figures ("4 scales,
5 forces, 4+placeholder symmetries, 4 information measures") may be
stale relative to the v0.6 catalog as actually shipped.

Module-load semantics: `Axes` is computed exactly once per Node
process. Side-effect ordering: `src/core/axes-registry.ts` imports
only from `src/core/types.ts` and a new `src/core/universal-index.ts`
(the type + `makeIndex` factory). NO circular deps with `bridges/`.

### Decision #5 — `LabeledTensor` composes (does NOT extend); the §2.2 sketch needs translation for UPT shape

Cross-check against actual src/: proposals doc §2.2 sketches
`constructor(readonly tensor: Tensor, readonly labels: Labels)` where
`Tensor` is MathTS's class. UPT's engine-level handle is the opaque
`EngineTensor` *interface*, not a class — see
`src/numerical/tensor-engine.ts:22-24`. Inheritance isn't even on the
table.

Divergences from the §2.2 sketch (flagged in Phase 2 task
descriptions so TDD doesn't silently inherit them):

- `readonly tensor: Tensor` → `readonly tensor: EngineTensor`. UPT
  doesn't depend on `@danielsimonjr/mathts-tensor` at the wrapper
  level — the engine abstraction is the dependency boundary.
- `Tensor.einsum(einsumStr, ...)` → `engine.einsum(spec, ...operands)`.
  UPT's einsum is **spec-based, not string-based**
  (`src/numerical/tensor-engine.ts:42-45`,
  `src/numerical/float64-engine.ts:492-582`).
- No `engine` field in the sketch → MUST include `engine` in the UPT
  wrapper. MathTS's `Tensor` may carry a module-level einsum without
  an engine handle; UPT requires the engine to be passed because
  both `Float64ReferenceEngine` and `MathTSEngine` must work
  identically.
- `LabeledTensor<any>` parameters → typed `LabeledTensor<R extends
  Record<string, UniversalIndex<AxisName>>>` with `MergedLabels<L, R>`
  result type. Decision #6 keeps generics precise.

### Decision #6 — `Labels` is `Record<string, UniversalIndex<AxisName>>` — structural string keys, no branded `EinsumLabel` type

The string key is engine-facing implementation detail; identity and
axis checks happen on the *value* (the `UniversalIndex`), not the
key. Branding the key would require callers to thread `makeLabel('i')`
instead of `'i'` and yields no incremental safety. The generic is
preserved through `contract()` so result label-record types are
precise.

### Decision #7 — `contract` rejects axis-mismatch contractions at runtime, not compile-time in v0.7.0

Cross-axis contraction (e.g., a `'scale'` index against a `'force'`
index) is a physics error. Proposals doc §2.3 claims this catches
"before the einsum string is built." Compile-time rejection in TS
would require encoding axis identity in the type-level shape of the
contraction — feasible with template literal types but expensive in
compile time and brittle in error messages.

**v0.7.0 ships runtime rejection.** `AxisMismatchError` thrown by
`contract` before calling `engine.einsum`; error names both indices,
both axes, both operand positions. Acceptance criterion §2.4
("wrong-axis contractions caught before the einsum string is built")
is satisfied by the runtime variant; v0.8.0+ may upgrade to
compile-time without breaking the runtime guarantee.

### Decision #8 — No AST-level integration in v0.7.0

The most tempting scope creep: plumbing `UniversalIndex` into the
AST (e.g., `TensorSymbolNode.indices[k].universalIndex?:
UniversalIndex<AxisName>`), then teaching `computeContraction` to
consult it. **Rejected** for v0.7.0:

- `TensorSymbolNode` shape change touches every bridge module — a
  42-file ripple.
- `computeContraction`'s contract is currently string-equality; a
  mixed-mode rewrite is the v0.5.0 ricci-slot-trap pattern.
- Phase 4's BE-52 demonstration covers the wrapper pattern at the
  evaluator surface (NOT the AST), which is the proposals-doc-claimed
  scope of "wrapper, not replacement."

If v0.8.0+ proceeds with shape (B) (Decision #1), it gets its own
release and breaking-change carry-forward. Locked out of v0.7.0.

### Decision #9 — Engine-agnostic; both engines participate

The wrapper consumes the `TensorEngine` interface
(`src/numerical/tensor-engine.ts:67-116`) — `einsum`, `transpose`,
`reshape`. Works with `Float64ReferenceEngine` and `MathTSEngine`
identically. The existing engine-conformance suite is not modified;
a wrapper-level conformance subset is added that runs against either
engine via parameterization.

### Decision #10 — Public-surface additions are `@public`-tagged at declaration

Per v0.6.1 Decision #4. Additions: `Axes` (registry singleton),
`UniversalIndex<Axis>` (type), `UniversalIndexId` (branded type),
`AxisName` (union), `LabeledTensor` (class), `makeIndex` (factory),
the new error classes. `tests/api/public-surface.test.ts` snapshot
regenerated as part of Phase 4 commit. No symbols removed.

---

## Phase Architecture

```
v0.7.0 — Intelligent Index layer (estimated ~32-38 tasks across 5 phases)
┌──────────────────────────────────────────────────────────────────────┐
│ Phase 0 — Scope validation + dimensional-layer reconciliation (~6)   │
│   STRUCTURAL: confirm adapter-on-top is feasible; axis census;       │
│   wrapper-interface header; pre-execution verification gate;         │
│   resolve §2.2 prime/arrow UNVERIFIED gap                            │
├──────────────────────────────────────────────────────────────────────┤
│ Phase 1 — UniversalIndex type + Axes registry (~7-9)                 │
│   TYPE: UniversalIndex<Axis>, branded id, axis discriminator,        │
│   makeIndex factory, frozen Axes singleton, per-axis tests           │
├──────────────────────────────────────────────────────────────────────┤
│ Phase 2 — LabeledTensor wrapper + contract() (~10-12)                │
│   STRUCTURAL: LabeledTensor<Labels> composing EngineTensor +         │
│   TensorEngine + labels; contract() builds EinsumSpec from shared    │
│   UniversalIndexId; transpose/reshape passthroughs;                  │
│   engine-agnostic conformance suite                                  │
├──────────────────────────────────────────────────────────────────────┤
│ Phase 3 — Identity-aware merge + axis-mismatch + runtime guards (~6) │
│   STRUCTURAL: AxisMismatchError, IdentityConflictError, ill-formed-  │
│   shared-id rejection, comprehensive error tests                     │
├──────────────────────────────────────────────────────────────────────┤
│ Phase 4 — Single-bridge demo + docs + release prep (~5-7)            │
│   DEMO: wire one bridge evaluator (BE-52 OR BE-21) through Labeled-  │
│   Tensor at its catalog-facing entry point — leaves AST untouched;   │
│   audit/outdated/build/bench; CHANGELOG; version bump + tag          │
└──────────────────────────────────────────────────────────────────────┘

Adam+Eve adversarial review gates:
  - Design vet (this doc — runs next, before plan-writing)
  - Plan vet (after writing-plans skill drafts implementation plan)
  - Mid-cycle vet (between Phase 2 and Phase 3 — the contract()
    algorithm is the highest-stakes commit)
  - Pre-publish vet (Phase 4 — honesty audit on dep-health + demo)
```

---

## Phase 0 — Scope-validation + dimensional-layer reconciliation

### Motivation

Per CLAUDE.md "Workflow gotchas" ("Plan templates routinely have wrong
inline test snippets ... Always cross-check inline test code against
existing fixtures before TDD'ing it") and the v0.6.1 Phase 0 precedent
(baseline-capture + recon BEFORE bulk work), v0.7.0 Phase 0 is the
verification gate. Decision #1 locks adapter-on-top; Phase 0
*verifies* that AST-layer `freeIndices` and wrapper-layer
`UniversalIndex` stay independent without leakage. If Phase 0 finds
a constraint that forces interlock, risk re-classifies and design
returns to vet.

### Components

- **Task 0.1 — `src/bridges/index.ts` axis-value census.** Read HEAD,
  extract every distinct `regime`/`from`/`to`/`scale` value across
  the 42 catalog entries. Compare against proposals doc §2.4. Record
  actual vs. claimed in `docs/architecture/v0.7-axis-census.md`.
  Discrepancies feed Decision #4's registry shape; do NOT change
  `src/bridges/index.ts`.

- **Task 0.2 — `computeContraction` decoupling verification.** Grep-
  confirm `computeContraction` is invoked ONLY from `validator.ts`
  (the AST validation path) and `src/numerical/lowering.ts:504-509`.
  If any caller exists outside these two paths, document and
  re-vet. Expected: zero additional callers.

- **Task 0.3 — `EngineTensor` opacity verification.** Confirm no
  consumer of `EngineTensor` outside the engines themselves reaches
  into a concrete subclass (`Float64Tensor`, the MathTSEngine's
  Tensor adapter). The §2.2 sketch's direct `Tensor` access doesn't
  apply — UPT's contract is the opaque `EngineTensor` interface.

- **Task 0.4 — Wrapper-interface header.** Write
  `src/core/labeled-tensor.ts` as a *type-only header* (interfaces +
  class signature, no implementation bodies). Type-only RED commit:
  confirm `tests/api/public-surface.test.ts` is ready; tsc compiles
  the header alone. Phase 1 fills bodies.

- **Task 0.5 — Adversarial-review checkpoint memo.** Write
  `docs/planning/v0.7-Phase-0-Reconciliation.md` (one-screen)
  capturing: chosen shape (A), rejected shapes (B, C), v0.7.0 /
  v0.8.0+ split. This doc is the Adam+Eve vet anchor for the rest
  of the release.

- **Task 0.6 — `prime` / `arrow` UNVERIFIED resolution.** Either
  (a) confirm via direct ITensor-paper Section-3 read that prime and
  arrow are first-class on the Index, in which case Decision #2 stays
  with stronger citation; or (b) confirm the §2.2 sketch is the
  canonical v0.7.0 set. Output: one-line citation update in this
  design doc.

### Tests

Phase 0 produces measurement and design-anchor artifacts. No new
behavioral tests; the wrapper-interface header will be exercised by
Phase 1's TDD cycle.

### Deliverable

3 commits: axis census, reconciliation memo, type-only wrapper
header.

---

## Phase 1 — `UniversalIndex` type + `Axes` registry

### Theoretical motivation

`UniversalIndex` is the persistent-identity carrier that distinguishes
Proposal 1 from naive einsum string-labels. Per proposals doc §2.3,
the catch is **identity-aware merging**: two AST trees that both
reference `Axes.scale.quantum` get the *same object*, not two strings
that happen to be equal. The registry singleton (Decision #4)
guarantees this at module load time.

### Components

- **`src/core/universal-index.ts`** —
  - `UniversalIndexId` (branded type).
  - `AxisName` union (`'scale' | 'force' | 'symmetry' | 'information'
    | 'dimension' | 'topology'`).
  - `UniversalIndex<Axis extends AxisName>` interface.
  - `makeIndex<Axis>(axis, name, opts?)` factory — generates `id`
    via `crypto.randomUUID()`, casts through the brand.

- **`src/core/axes-registry.ts`** —
  - `Axes` singleton: `Object.freeze({ scale: Object.freeze({...}),
    force: Object.freeze({...}), ... })`.
  - Per-namespace entries from Phase 0's census (Task 0.1), not from
    the possibly-stale proposals doc §2.4 figures.

- **`src/index.ts` re-exports** — Decision #10 list, added at the
  appropriate slot (after `bridges/index.ts` exports, before the
  numerical-engine block).

### Tests

- `tests/core/universal-index.test.ts` —
  - `makeIndex('scale', 'quantum')` returns object with `axis ===
    'scale'`, `name === 'quantum'`, `id` is a UUID string.
  - Two consecutive `makeIndex` calls with identical args produce
    *different* `id`s.
  - Brand check via `// @ts-expect-error` annotation: plain string
    cannot be assigned to `UniversalIndexId`-typed variable.

- `tests/core/axes-registry.test.ts` —
  - `Axes.scale.quantum` is the SAME reference across two import
    sites (module-singleton check).
  - `Axes.scale.quantum.axis === 'scale'`.
  - Frozen-ness: reassignment throws in strict mode.
  - Axis-name population: every entry's `axis` field equals its
    parent-namespace key.

### Pre-execution verification gate

Per CLAUDE.md "Workflow gotchas" — read `src/core/types.ts` and
`src/core/constants.ts` before writing new modules to avoid
circular-import surprises. New files import only from
`src/core/types.ts` (for convention alignment) and standard
`crypto`.

---

## Phase 2 — `LabeledTensor` wrapper + `contract()`

### Theoretical motivation

`LabeledTensor` is the engine-facing wrapper. The single critical
operation is `contract(other)`. Per Decision #3, contract is strictly
identity-based — `UniversalIndexId` equality is the only matching
predicate. Per Decision #5, the wrapper composes `EngineTensor` +
`TensorEngine` + a labels record.

### Components

- **`src/core/labeled-tensor.ts`** (the full implementation building
  on Phase 0's header):
  - Constructor: `(tensor: EngineTensor, engine: TensorEngine, labels:
    Record<string, UniversalIndex<AxisName>>)`. Enforces
    `Object.keys(labels).length === tensor.shape.length`.
  - `contract(other: LabeledTensor): LabeledTensor` per Decision #3:
    1. Find shared `UniversalIndexId` set between `this.labels` and
       `other.labels`.
    2. For each shared id, the (operand=0, axis) coordinate is the
       label-key's position in `this.labels` key-order; (operand=1, axis)
       is the same for `other`. Order canonicalized via
       `canonicalLabelOrder(labels)` (Risk 2 mitigation — not raw
       `Object.entries`).
    3. Build `EinsumSpec` using these coordinates.
    4. Call `this.engine.einsum(spec, this.tensor, other.tensor)`.
    5. Build result labels with id-conflict guard (Decision #7 +
       Phase 3 error surface).
  - `transpose(axisMap: Record<string, string>)` — rewires labels,
    then calls `engine.transpose(t, perm)` with perm computed from
    the axisMap.
  - `reshape(shape)` — calls `engine.reshape(t, shape)`. **Rank-
    preservation guard**: rank-changing reshape throws
    `RankPreservationError`. v0.7.0 only allows axis-by-axis size
    change (e.g., `[4, 4]` → `[2, 2, 4, 1]` rejected); v0.8.0+ may
    add explicit `mergeAxes` / `splitAxis`.

### Cross-check against the §2.2 sketch — divergences flagged

Per Decision #5. The implementation-plan task descriptions explicitly
list these divergences so TDD does not silently inherit the
MathTS-shaped sketch:

- `Tensor` → `EngineTensor`.
- `Tensor.einsum(einsumStr, ...)` → `engine.einsum(spec: EinsumSpec,
  ...operands)`.
- Add `engine` field to constructor.
- Typed generics (`L extends Record<string, UniversalIndex<AxisName>>`)
  instead of `<any>`.

### Tests

- `tests/core/labeled-tensor.test.ts` —
  - **Construction**: label count matches rank or throws
    `LabeledTensorConstructionError`.
  - **Identity contraction**: contracting two tensors sharing
    `Axes.scale.quantum` produces a result with one fewer axis;
    result label set is union minus shared id.
  - **Non-identity contraction (negative test)**: two tensors with
    `Axes.scale.quantum` vs `Axes.scale.classical` — different ids —
    produce NO contractions; result is the outer product.
  - **Engine-agnostic conformance**: same fixture against both engines
    (`Float64ReferenceEngine`, `MathTSEngine` when available); outputs
    converted via `engine.toNested()` must match.
  - **transpose passthrough**: transposed `LabeledTensor` carries
    re-ordered labels.
  - **reshape rank-preservation guard**: rank-changing reshape
    throws.

### Mid-cycle Adam+Eve vet (between Phase 2 and Phase 3)

The `contract()` algorithm (Decision #3 + Phase 2 components) is the
highest-stakes commit. Adam (Gemini 2.5 Pro) re-derives the algorithm
*independently* from `EinsumSpec` semantics
(`src/numerical/tensor-engine.ts:29-45`) and confirms the label-key →
axis-position mapping. Eve (OpenAI o3) red-teams identity-conflict
cases: triple contraction (three operands sharing one id), `Object.entries`
order variance across engine implementations, contract chained more
than twice. Findings reconcile into Phase 3 components.

---

## Phase 3 — Identity-aware merge + axis-mismatch + runtime guards

### Theoretical motivation

Phase 2 lands the happy path; Phase 3 lands the error surface. Cross-
classify proposals doc §2.3 against the v0.7.0 implementation:

| §2.3 claim | v0.7.0 catch level | Mechanism |
|---|---|---|
| Typos at construction (`Axes.scale.quantam`) | tsc compile-time | Typed object literal; non-existent keys are tsc errors (Phase 1). |
| Cross-axis errors (`force.weak` where `UniversalIndex<'scale'>` required) | tsc compile-time | `Axis extends AxisName` generic propagates (Phase 1). |
| Identity-aware merging | runtime + tsc (partial) | `contract` matches by `UniversalIndexId` (Phase 2). |
| Wrong-axis contractions in `LabeledTensor` ops | runtime | `AxisMismatchError` thrown by `contract` (Phase 3). |

### Components

- **`src/core/errors.ts` extensions** — `AxisMismatchError`,
  `LabeledTensorConstructionError`, `RankPreservationError`,
  `IdentityConflictError`. Placement aligned with existing error
  conventions (`src/numerical/errors.ts`, `src/dimensional/errors.ts`).

- **Runtime guards in `LabeledTensor.contract`**:
  - **Identity-conflict guard**: if `this.labels` and `other.labels`
    both have a remaining (non-contracted) label with the same id,
    throw `IdentityConflictError`. Same physical axis showing up
    free on both sides — physically meaningful but engine-level
    ambiguous.
  - **Axis-mismatch guard** (Decision #7): if `this.labels[k]` and
    `other.labels[m]` share an `id` but differ in `axis`, throw
    `AxisMismatchError`. Impossible if Phase 1's registry invariant
    holds; defensive against downstream consumers constructing
    off-registry indices via direct `makeIndex` calls.

### Tests

- `tests/core/labeled-tensor-errors.test.ts` — exhaustive coverage of
  the four guard types with edge-case inputs.

- `tests/core/labeled-tensor-conformance.test.ts` (engine-parameterized)
  expanded: all error cases run on both engines.

### Pre-execution verification gate

Per CLAUDE.md "Workflow gotchas" — read `src/numerical/errors.ts` and
`src/dimensional/errors.ts` before writing `src/core/errors.ts` to
align with existing error-class conventions
(`NumericalBackendError`, `DimensionMismatchError`,
`IndexLabelCollisionError`, `VarianceMismatchError`).

---

## Phase 4 — Single-bridge demonstration + docs + release prep

### Theoretical motivation

Proposals doc §2.4's "All 42 v0.6 bridges re-register through Index
objects" is moved out of v0.7.0 (non-goal). What v0.7.0 demonstrates
is the *pattern* on a single representative bridge — enough to
validate the wrapper-on-the-catalog-edge claim and to provide a
tutorial for downstream maintainers and physicist contributors.

### Components

- **Bridge choice — BE-52 (Mercury perihelion) OR BE-21 (Wheeler-
  DeWitt)**. BE-52 has a richer numerical surface; BE-21 is closer to
  the regime-bridging spirit. Phase 0 Task 0.1's axis census informs
  the choice — the implementation-plan task description pins exactly
  one.

  **Constraint**: the demo MUST NOT modify the chosen bridge's
  existing AST evaluation path. The `LabeledTensor` wrapping is added
  as an *alternative* entry point (e.g.,
  `evaluateBE52WithLabeledOutput(...)` returning a `LabeledTensor`
  whose axes are tagged via `Axes.scale.cosmological` etc.), and the
  original `evaluatePerihelionPrecession` is left untouched. This
  preserves Decision #1 (adapter on top, NOT replacement).

- **Tutorial doc**: `docs/architecture/intelligent-index-tutorial.md`
  — single-screen walkthrough of constructing a `LabeledTensor`,
  choosing labels from `Axes`, contracting, catching axis-mismatch
  errors.

- **JSDoc backfill** in every Phase 1-3 public export per v0.6.1
  Decision #4. One-paragraph explanation each — not just
  paraphrasing the proposals doc.

- **CHANGELOG entry** — v0.7.0 section with:
  - Summary: "Intelligent Index layer (Proposal 1 from v0.7+
    proposals)."
  - New public exports list (Decision #10).
  - Adapter-on-top reconciliation note (Decision #1 — explicit:
    "TensorSymbolNode and computeContraction are unchanged in
    v0.7.0").
  - v0.8.0+ split: prime/arrow, bulk-bridge migration, codemod,
    AST-level integration.
  - Dependency health snapshot per CLAUDE.md release pre-flight.

- **Release pre-flight per CLAUDE.md**: `npm audit`, `npm outdated`,
  full vitest suite, `npm run build`, `npm run smoke`, `npm run
  bench:ci`. Document HIGH/CRITICAL audit findings (expect 0) and
  outdated-deps shortlist in CHANGELOG block.

- **Version bump** 0.6.1 → 0.7.0 in `package.json`. CHANGELOG entry.
  `todo.md` roll-forward. Direct-push to `master`. Tag `v0.7.0`.
  Push tag. `npm publish --ignore-scripts --access public` (gated on
  NPM_TOKEN rotation — if still expired, tagged commit is the
  deliverable and publish lands separately).

### Tests

- `tests/bridges/be-XX-with-labeled-tensor.test.ts` — demo bridge's
  new entry point validates against existing numerical output AND
  carries correct `Axes.<...>` labels.

- `tests/api/public-surface.test.ts` snapshot regenerated to include
  Phase 1-3 additions.

- Full vitest suite green: target ≥ Phase 0 baseline.

---

## Cross-Phase Invariants

1. **Suite green** at every task commit. Baseline measured in
   Phase 0 Task 0.1.
2. **tsc clean** at every task commit (`npm run build` exits 0).
3. **No new `@ts-ignore` / `as any`** — v0.5.1 TS-1 zero-bar. The
   branded `UniversalIndexId` requires ONE cast inside `makeIndex`
   (the brand application) — isolated to one line, documented inline.
4. **`TensorSymbolNode` and `computeContraction` UNCHANGED** —
   Decision #1 invariant. Any diff to either file aborts the cycle
   and re-enters Adam+Eve vet.
5. **`TensorEngine` interface UNCHANGED** — Decision #9 invariant.
6. **`Axes` is module-singleton** — Phase 1 invariant. Two import
   sites get the same object references.
7. **42 bridges left at v0.6 status** — per v0.6.0 Decision #9
   precedent. Adding a `LabeledTensor` entry point to a bridge does
   NOT promote its confidence.

---

## Subagent Parallelism Boundaries

File-disjoint partitioning per v0.5.1 / v0.6.0 / v0.6.1 precedent.
CHANGELOG.md is the single sequential contention point.

| Phase | Parallel partition |
|---|---|
| 0 | Axis census (A) ∥ wrapper-interface header (B) ∥ reconciliation memo (parent). |
| 1 | `universal-index.ts` + tests (A) ∥ `axes-registry.ts` + tests (B). |
| 2 | `labeled-tensor.ts` contract algorithm (A) ∥ transpose/reshape passthroughs (B) ∥ engine-conformance suite (parent). |
| 3 | Error-class additions (A) ∥ guard implementations (B) ∥ error tests (parent). |
| 4 | Demo bridge wiring (A) ∥ tutorial doc (B) ∥ JSDoc backfill (parent) ∥ release prep (parent post-merge). |

---

## Adam+Eve adversarial review plan

Per todo.md §Conventions (Gemini 2.5 Pro = Adam, OpenAI o3 = Eve
for UPT, with Opus-subagent fallback per v0.6.1 Decision #9):

- **Design vet (THIS DOC)** — Adam re-derives Decision #1 (adapter vs.
  unified vs. parallel) from `src/dimensional/tensor.ts` HEAD; Eve
  red-teams the §2.2 sketch-vs-actual-src divergences (Decision #5)
  and the prime/arrow UNVERIFIED gap (Decision #2).

- **Plan vet** — Adam re-derives the `contract()` algorithm against
  `EinsumSpec` semantics from `src/numerical/tensor-engine.ts`; Eve
  red-teams the engine-agnostic claim (Decision #9 — does
  MathTSEngine's `einsum` actually accept arbitrary spec shapes, or
  hidden constraints?).

- **Mid-cycle vet (between Phase 2 and Phase 3)** — Adam re-derives
  the label-key → axis-position mapping (Phase 2 step 2); Eve red-
  teams triple-contraction (three operands sharing one id — possible
  in MPO products but not exercised in v0.6 catalog).

- **Pre-publish vet (Phase 4)** — confirm dep-health snapshot is
  honest; demo bridge test is not silently re-using AST-path
  outputs.

Per CLAUDE.md "Workflow gotchas" — every Eve finding requires
grep-verification before action (v0.5.1 lesson: 5/9 Eve-vet
challenges were fabrications).

---

## Risks

1. **Phase 0 invalidates adapter-on-top (Decision #1)**. Probability:
   low (AST and engine paths are well-separated per v0.6.0
   ARCHITECTURE.md), but if it fires the release re-scopes to
   v0.8.0 with breaking-change carry-forward. **Mitigation**: Task
   0.2 produces a written verification artifact the design vet
   signs off on.

2. **`Object.entries` order non-determinism in `contract()` mapping**.
   The `contract()` algorithm relies on label-key iteration order
   matching tensor-axis order. JS guarantees string-key insertion
   order, but the §2.2 sketch's `Record<string, ...>` doesn't make
   that explicit at the type level. **Mitigation**: Phase 2 commits
   a `canonicalLabelOrder(labels: Record): string[]` helper and the
   algorithm threads through that, not raw `Object.entries`.

3. **`prime` / `arrow` ITensor parity demanded mid-cycle**. If a
   reviewer argues the wrapper is "incomplete" without prime marks
   (needed for bra/ket distinction in QFT-side bridges), the design
   fields the request as a v0.8.0 brainstorm seed — NOT a v0.7.0
   scope addition. **Mitigation**: Phase 0 Reconciliation memo
   explicitly names the v0.7.0/v0.8.0 split.

4. **Engine-agnostic claim fails for `MathTSEngine`**. The wrapper
   assumes `engine.einsum(spec, ...)` accepts the same `EinsumSpec`
   shape on both engines. **Cross-check status**: this design has
   NOT directly read `src/numerical/mathts-engine.ts` (large, behind
   an optional dep). **Mitigation**: Phase 2's engine-conformance
   suite runs `LabeledTensor.contract` tests against both engines;
   if MathTSEngine fails, the demo bridge in Phase 4 falls back to
   `Float64ReferenceEngine` and the gap is a v0.8.0 follow-up.

5. **Phase 4 demo bridge wiring leaks into the AST**. The Decision
   #1 invariant ("`TensorSymbolNode` and `computeContraction`
   UNCHANGED") is the hardest invariant to keep — it's tempting to
   thread a `UniversalIndex` reference through `TensorSymbolNode.indices[k]`
   "just for the demo." This is the v0.5.0 ricci-slot-trap pattern.
   **Mitigation**: Phase 4 explicitly forbids any
   `src/dimensional/*` or `src/bridges/equations/*` edits beyond the
   demo bridge's evaluator entry-point function body. Pre-publish
   vet greps for unintended edits.

6. **Public-surface explosion**. Decision #10 adds ~7 new exports.
   **Mitigation**: each new export carries a JSDoc paragraph (Phase
   4); the snapshot regen commit is a single deliberate landing,
   not drift.

7. **`crypto.randomUUID` availability**. Per Node ≥18, available
   globally. **Cross-check status**: not directly verified in
   tsconfig or test-runner environment for this design.
   **Mitigation**: Phase 1 Task 0 includes a one-line preflight; if
   absent, fall back to `node:crypto`'s `randomUUID` import.

---

## Non-goals (recap)

Explicitly NOT in scope for v0.7.0:

- Replacing `TensorSymbolNode.indices` with `UniversalIndex` references.
- Bulk re-registration of all 42 v0.6 bridges through `Axes`.
- The `tools/migrate-strings-to-indices.ts` codemod.
- `prime` and `arrow` fields on `UniversalIndex` (Decision #2 —
  UNVERIFIED until Phase 0 Task 0.6 closes).
- Compile-time axis-mismatch detection via template literal types
  (Decision #7 — v0.7.0 ships runtime; compile-time is v0.8.0+
  research).
- Changes to `TensorEngine`, `Float64ReferenceEngine`, `MathTSEngine`
  (Decision #9 invariant).
- Proposals 2-8 from `docs/planning/UPT v0.70 - Proposals.md` —
  separate releases per proposals doc §11. Proposal 1 is
  independently shippable.
- AST→`LabeledTensor` automatic bridging (Decision #8).
- A sparse `Map<IndexTuple, Cell>` catalog (Proposal 2).
- The npm publish step — gated on NPM_TOKEN rotation, carried from
  v0.6.0/v0.6.1.

---

## Public-API Additions

Final list:

- `UniversalIndex<Axis extends AxisName>` (type, `@public`).
- `UniversalIndexId` (branded type, `@public`).
- `AxisName` (union type, `@public`).
- `Axes` (frozen registry singleton, `@public`).
- `makeIndex<Axis extends AxisName>(...)` (factory, `@public`).
- `LabeledTensor<L>` (wrapper class, `@public`).
- `AxisMismatchError`, `IdentityConflictError`,
  `LabeledTensorConstructionError`, `RankPreservationError` (error
  classes, `@public`).

---

## Breaking Changes (CHANGELOG-bound)

**None.** v0.7.0 is a purely additive minor bump per Tag Decision.
Decision #1 + Decision #9 invariants are designed to guarantee this;
Cross-Phase Invariants 4-5 and Risk 5 are the operational safeguards.

If during execution a breaking change becomes necessary, the cycle
aborts and re-enters design vet — it does NOT silently re-classify.

---

## Task Count

Estimated **~32-38 tasks** across 5 phases (6 + 8 + 11 + 6 + 6).
Comparable to v0.6.0's 36 and larger than v0.6.1's 22 — expected
because the wrapper-class extraction is architecturally non-trivial
(Phase 2 alone is ~10-12 tasks). Adam+Eve mid-cycle vet between
Phase 2 and Phase 3 typically adds 1-2 reconciliation tasks.

Effort estimate per proposals doc §0 was **2-3 weeks** at Low risk;
this design adjusts to **2-3 weeks at Medium risk** pending Phase 0
adapter-on-top confirmation, reverting to Low if Phase 0 closes
cleanly.

---

## Cross-check summary: proposals doc §2 vs. this design

| §2 claim | This design's position |
|---|---|
| "wrapper, not a replacement" | Locked as Decision #1 (adapter-on-top). |
| `UniversalIndex` has `id`, `axis`, `name`, `tags`, `limits`, `notes` | Adopted as Decision #2 (no prime, no arrow in v0.7.0 — UNVERIFIED ITensor parity until Phase 0 Task 0.6 closes). |
| `LabeledTensor.contract` matches by axis identity | Locked as Decision #3 (full UUID match). |
| `Tensor.einsum(einsumStr, ...)` (MathTS-shaped) | Translated to `engine.einsum(spec, ...)` per Decision #5 — divergence flagged in Phase 2. |
| "All 42 v0.6 bridges re-register through Index objects" | **MOVED to v0.8.0+** — Phase 4 demonstrates the pattern on ONE bridge only. |
| Codemod in `tools/migrate-strings-to-indices.ts` | **MOVED to v0.8.0+**. |
| "Compile-time index-mismatch detection" | **PARTIAL** — typo and cross-axis catches at tsc; identity-mismatch via runtime (Decision #7). |
| Source-readability gains | Demonstrated via Phase 4 BE-52 (or BE-21) entry point + tutorial. |
| 2-3 weeks Low risk | Re-classified Medium risk pending Phase 0 — reverts to Low on adapter-on-top confirmation. |

---

## Vet outcome

To be populated after Adam+Eve design vet runs. Pressure-test
specifically:

- Decision #1 (adapter-on-top vs. unified — is shape (B) actually
  required by some constraint this design overlooked?).
- Decision #2 (prime/arrow UNVERIFIED — does Phase 0 Task 0.6 close
  it definitively?).
- Decision #5 (§2.2 sketch divergences — are there others this design
  missed?).
- Phase 0 Task 0.2 (the `computeContraction` decoupling claim — is
  the grep claim "ONLY called from validator.ts and lowering.ts"
  defensible?).
- The engine-agnostic claim (Decision #9 + Risk 4 — has
  `MathTSEngine.einsum` been confirmed to accept arbitrary
  `EinsumSpec` shapes?).
