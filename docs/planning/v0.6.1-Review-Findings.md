# UPT v0.6.1 Design — Adam+Eve Adversarial Review Findings

**Date:** 2026-05-23
**Reviewers:**
- **Adam** (steel-man, Opus subagent) — fallback per Decision #9 (no `llm-gemini`/`llm-openai` MCPs in this remote-execution environment).
- **Eve** (red-team, Opus subagent) — same fallback.
**Scope:** `docs/planning/v0.6.1-Design.md` (~520 lines, 9 decisions, 5 phases).
**Method:** Independent parallel review (Adam steel-mans claims; Eve hunts for hidden assumptions). Per the v0.5.1 carry-forward lesson, every claim with specific-evidence (file:line, symbol name, count) was grep-verified by me before being accepted as load-bearing.

---

## Summary

- **Total findings:** 24 (Adam: 11 with verdicts ✅/❌/⚠; Eve: 13 risks).
- **Shared concerns (both reviewers caught the same issue):** 3 critical issues.
- **Adam-only:** 3 verified count/arithmetic errors.
- **Eve-only:** 7 risk-surfacing findings (5 actionable, 2 speculative).
- **Verdict:** **Ship with edits — material rework required before plan-drafting.**

Mathematics and architectural framing remain sound. Defects concentrate in the export-classification recon (test-imports counted incorrectly), the validator-registry pattern claim (3 patterns, not 2), and the dep-graph generator-fix premise (the fix is the wrong fix — generator already has the `--include-tests` mode it needs; the npm script just doesn't pass the flag).

---

## Shared concerns (both reviewers, priority fixes)

| # | Severity | Phase | Issue | Fix |
|---|---|---|---|---|
| **S1** | **HIGH** | 1 | "Internal-only" bucket counted test-imports as internal. Dropping `export` on any of `EXPECTED_DIMENSION_BY_BRIDGE`, `GL4_C/A/B`, `riemannLowerAt`, `covariantDerivRiemannLowerAt`, `metricDerivSupplied`, `isEinsumSpec` would break test files. **Verified** at `tests/dimensional/bridge-check.test.ts:13`, `tests/numerical/gl4-butcher-tableau.test.ts:2`, `tests/dimensional/bianchi-residual.test.ts:66-67`. | **Redefine bucket (a)** as "no importers at all, including tests". Add new bucket **(a')** "test-only importers — keep `export`, add `@internal` JSDoc with rationale". Re-classify the recon's 13 → roughly 8 bucket-(a) drops + 5+ bucket-(a') `@internal` tags. |
| **S2** | **HIGH** | 2 | Validator-registry pattern is NOT uniform across 10 arms. Three arms (`ricci-tensor`, `einstein-tensor`, `bianchi-residual`) pass a `riemannChild → {dim, freeIndices}` closure into their validator; the other 8 don't. A `propagateFreeIndices: boolean` flag does NOT capture this — this is exactly the v0.5.0 ricci-slot-bug shape ("naive registry-driven dispatch" missing the per-arm callback wiring). **Verified** at `src/dimensional/validator.ts:605-650`. | **Registry contract must encode three patterns**: (A) no callback + propagate freeIndices (5 arms: riemann, killing-vector, stress-energy, weyl-tensor, einstein-equation); (B) riemann-closure callback + propagate freeIndices (3 arms: ricci-tensor, einstein-tensor, bianchi-residual); (C) no callback + skip freeIndices (3 arms: conserved-charge, cosmological-constant, kretschmann-scalar). Total **11 arms**, not 10. |
| **S3** | **HIGH** | 3 | The Track-C premise ("the dep-graph generator is missing the `BRIDGE_EQUATIONS` array imports") is **wrong**. **Verified**: `src/bridges/index.ts` has only 2 file-top imports (`./gravitational-lensing.js`, `./perihelion-precession.js`); the 40 `be-NN-*.ts` modules are NOT imported by any non-test file. Generator is correct; the 40-file false-positive comes from `npm run docs:deps` not passing `--include-tests` (the flag exists in the generator at line 128, but the npm script invocation at `package.json:scripts.docs:deps` lacks it). | **Trivialize Phase 3 Task 3.2**: replace "regex fix to follow BRIDGE_EQUATIONS imports" with "add `--include-tests` to `package.json` `docs:deps` script". Eliminates all 40 BE false-positives in one line. Phase 3 Task 3.1 (parse `package.json` exports for the `mathts-engine` subpath) still applies. |

---

## Adam findings unique to Adam (verified-arithmetic class)

| # | Severity | Issue | Fix |
|---|---|---|---|
| **A-V1** | MEDIUM | Bucket (a) row count sums to ~40, not 13 (prose). The 19 `*ValidationResult` figure is itself wrong — `unused-analysis.md` lists 17 such symbols, of which 3 ARE re-exported by `src/index.ts` (Einstein-field-equation, Kretschmann-scalar, top-level `ValidationResult`) → net droppable 14, not 19. | Rework Phase 1 table as a verifier-gated symbol-by-symbol list: each row gets a recon-step grep query that must come back empty (no external importers) BEFORE the drop commit. |
| **A-V6** | LOW | Validator arm count is 11, not 10. `kretschmann-scalar` is the 11th (validator.ts:721). | Update Decision #2 and Phase 2 text: "11 arms across 3 patterns". |
| **A-V9** | LOW | Phase 1 "~10 commits" arithmetic is off. 13 drops + JSDoc commit + the validator/curvature batch ≈ 16 files at 1 commit/file = ~16 commits. After S1 reclassification this drops to ~10-12 commits (some "drops" become `@internal` tags batched together). | Re-derive commit count after S1's reclassification. |
| **A-V10** | LOW | LOC delta claim (`validator.ts` 816 → 720) is slightly aggressive — 11 arms × ~7 lines + dispatch ≈ 80 LOC, not 90. Within noise. | Soften to "validator.ts ~816 → ~735 (drop ~80 LOC)". |

---

## Eve findings unique to Eve (red-team)

| # | Severity | Issue | Fix |
|---|---|---|---|
| **E-R3** | LOW | Phase ordering contradiction: Phase 4 "should run last" but the table puts Phase 5 after. | Make explicit order: **Phase 3 → Phase 1 → Phase 2 → Phase 5 → Phase 4**. Rationale below. |
| **E-R4** | LOW-MEDIUM | "Suite stays ≥1693" baseline was captured at v0.6.0 tag `ac0cf06`. Post-tag commits (BE-33 fix `394d164`, etc.) may have shifted the number. | Add a **Phase 0** that runs `npm test` on `master`-HEAD, records the actual baseline, and gates Phase 1+ against THAT number. |
| **E-R5** | MEDIUM | `@public` JSDoc sweep deliverable is unspecified — could be 40 or 200+ symbols. v0.6.0 commit `0270460` already backfilled 94 `@public` tags; the v0.6.1 delta is unknown. | Phase 0 also produces the recon: count un-tagged re-exported symbols. If >100, descope the JSDoc sweep to only the symbols touched by Phase 1's reclassification. |
| **E-R6** | LOW-MEDIUM | Phase 2 "pre-extraction snapshot tests" are brittle on floating-point + AST serialization, and the design admits existing tests already cover the behavior. | **Drop snapshot scaffolding**. Trust `tests/dimensional/*` + `tests/numerical/weyl-*`. Phase 2 deliverable becomes "extract + re-run suite + bit-for-bit numerical equality on Schwarzschild fixtures (already covered)". |
| **E-R7** | LOW | "Phase 1 and Phase 3 can run in parallel" leaves baseline attribution ambiguous (the unused-exports drop could come from either source). | Sequence Phase 3 first per E-R3 fix; the metric drop in `unused-analysis.md` after Phase 1 is then cleanly attributable. |
| **E-R8** | MEDIUM | Track-C scope misses `await import()` dynamic-import patterns (`src/numerical/mathts-engine.ts:145,186`). | **Explicitly out-of-scope** for v0.6.1. Decision #3 adds a sub-clause: "dynamic `await import()` reachability is NOT taught to the generator in v0.6.1; if any file becomes a new false-positive due to dynamic-import usage, document by name with rationale, no generator change". |
| **E-R9** | LOW | Bench LOC estimate (~40-70 each) is optimistic. Existing benches are 89-160 LOC. Mercury IC setup alone is ~30-50 LOC. Realistic: ~80-150 LOC per file. | Soften Phase 5 LOC budget to "~80-150 LOC per bench; ~300 LOC total across 3 files". |
| **E-R11** | LOW-MEDIUM | No dep-health snapshot phase, but `CLAUDE.md` release pre-flight requires `npm audit && npm outdated` for v0.5.1+. | Phase 0 adds the dep-health snapshot; result lands in the v0.6.1 CHANGELOG block under "Dependency health". |
| **E-R13** | MEDIUM | `tests/api/public-surface.test.ts` snapshot must be regenerated per file-batch that touches a `@public` symbol set; design didn't call this out. | Phase 1 + Phase 2 task templates include "regenerate public-surface snapshot if any `@public` export changed". |

---

## Eve findings rejected (per the "grep-verify Eve's specific evidence" discipline)

| # | Eve claim | Verification | Verdict |
|---|---|---|---|
| **E-R2** (closure-passing in 3 arms) | "ricci/einstein/bianchi pass a *closure* into their validator" | Verified ✓ at validator.ts:611, 627, 642. | **ACCEPTED — promoted to S2.** |
| **E-R10** | "`tools/plan-doc-audit/` may import from src/ — bucket (a) deletion could break the script" | `grep -rn "from.*src/" tools/plan-doc-audit/` returns nothing — `tools/plan-doc-audit/audit.ts` is a stand-alone scanner with zero `src/` imports. | **REJECTED.** Speculative; verified false. |
| **E-R12** | "`it.todo` claims unverified" | The design's Decision #8 already encodes "no-op for v0.6.1" regardless of count accuracy. | **NO-OP** — design is correct as written; verification doesn't change the action. |

---

## Adam findings accepted as-stated

| # | Claim | Verdict |
|---|---|---|
| A-V5 | (c) bucket may need more entries (`isEinsumSpec` test-imported, GL4 constants test-imported) | Folded into S1 reclassification. |
| A-V8 | "Suite ≥1693" baseline | ✅ VERIFIED. Held by E-R4's "but re-baseline at HEAD" caveat. |
| A-V11 | Bridge-status distribution 8/31/3/0 | ✅ VERIFIED. CLAUDE.md is current; Decision #6's re-tally directive is unnecessary. |

---

## Reconciliation actions (applied to `v0.6.1-Design.md`)

The following edits land in this commit alongside this findings doc:

1. **Decision #1 redefined**: bucket (a) tightened to "no importers including tests"; new bucket (a') "test-only importer — keep `export`, add `@internal` JSDoc". Per-symbol classification table moved to Phase 0 recon (not pre-locked in the design).
2. **Decision #2 + Phase 2 reworked**: validator-registry contract specs 3 patterns (A/B/C); arm count corrected to 11; LOC delta softened (Adam-V10).
3. **Decision #3 + Phase 3 simplified**: BE-module reachability fix is "add `--include-tests` to `docs:deps` npm script" (one-line change). `await import()` dynamic-imports explicitly out-of-scope.
4. **New Phase 0 added**: baseline capture (suite count, dep-health snapshot, `@public` tag count + delta projection, per-symbol drop-vs-keep classification grep).
5. **Explicit phase order**: 0 → 3 → 1 → 2 → 5 → 4. Stated in Phase Architecture section.
6. **Snapshot test scaffolding dropped** from Phase 2 (E-R6); existing test suite is the guard.
7. **Phase 5 LOC budget** revised to ~80-150 LOC per bench (E-R9).
8. **`tests/api/public-surface.test.ts` snapshot regen** called out in Phase 1 + Phase 2 task templates (E-R13).
9. **Count claims fixed**: "13 symbols" → re-derived in Phase 0; "10 arms" → "11 arms"; "~10 commits Phase 1" → re-derived.
10. **Goal #6 (suite count)** updated to "stays ≥ Phase 0 baseline" instead of hard-pinned 1693.

---

## What this review did NOT cover

- TDD task structure (deferred to plan-time review, per v0.4.0 / v0.6.0 precedent).
- Per-bench specific physics correctness (gl4-picard-alloc Mercury IC, ricci-lowering Schwarzschild fixture choice). Plan-time concern.
- Adam+Eve themselves did not run `npm test` to verify suite-count baseline — that's Phase 0 work.

---

## Net assessment

**Ship-with-edits.** Three load-bearing problems (S1/S2/S3) require design rework — applied in this same commit cycle. The decision framing, risk register, and Phase 2/4/5 content remain sound. Adversarial-review process worked as intended: caught real bugs (the v0.5.0 ricci-slot trap pattern showed up in Decision #2; would have been a TDD-RED failure mid-sprint), and the Eve-vet discipline rejected 1/13 speculative claim (E-R10) without action.

Per CLAUDE.md "Adversarial review runs on both artifacts independently — never assume the plan inherits the design's fixes", the upcoming `v0.6.1-Implementation-Plan.md` will get its own Adam+Eve pass.
