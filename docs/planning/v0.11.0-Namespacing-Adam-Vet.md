# Adam Vet — v0.11 Quantity-Namespacing Design Note r1 → RED

> 2026-06-11. Verdict: **RED**; all five required revisions adopted in
> the note's r2. Condensed findings (full reasoning in the r2 note):

- **A-1 [HIGH]** r1's same-object carve-out was factually wrong about
  the code it cited: be-42>>be-12 and ST-2 are BOTH `massQ === massQ`
  (calibration.ts:141/341 vs :187/:236) — object identity cannot carry
  the wrong-vs-correct-sharing distinction; r1's acceptance criteria 1
  and 2 were mutually contradictory. → r2: pure name-collision rule.
- **A-2 [HIGH]** Object identity already module-coupling-fragile both
  ways: catalog-tranche's be48 defines its OWN 'mass' node while its
  docstring falsely claims reuse (false-positive class); calibration
  has duplicate-name distinct-object 'temperature' nodes (:77 vs :294).
  → r2: centralize Quantity nodes + uniqueness pin BEFORE migration.
- **A-3 [HIGH]** "Zero regression" was incomplete: the Phase-D aliasing
  pin (enumerate-uncertainty.test.ts:61-79, self-described
  v0.11-replaceable) and ST-2 are the exact two breaking call sites.
  → r2: churn made explicit in criterion 2.
- **A-4 [MED]** `renameSecond` without an evaluator-input remap is a
  no-op (the spread at compose.ts:200 still feeds the first operand's
  value); plus rename-target validity + no-dedup-for-'shared' rules.
  → r2: implementation requirements pinned.
- **A-5 [MED]** Criterion-3 pin computed: T_H(M_sun) = 6.1684e-8 K;
  λ_T(m_e, T_H) = 3.0012e-4 m (vs aliased 2.031e-34 m); repo lacks an
  electron-mass constant → add `M_E_SI`.
- **A-6 [LOW]** Option D (name-rule + registered
  `SOURCE_ALIAS_DISPOSITIONS`) — adopted as r2's recommendation.
- **A-7 [LOW]** CT-2/CT-4 are consistency quotients, not compositions —
  excluded from regression scope.
