# v0.4.0 Implementation Plan — Adam+Eve Adversarial Review Findings

**Date:** 2026-05-15
**Reviewers:** Adam (Gemini 2.5 Pro), Eve (OpenAI o3, via `OPENAI_REASONING_MODEL=o3`)
**Scope:** Full `v0.4.0-Implementation-Plan.md` (3,193 lines, 20 tasks) + design `v0.4.0-Design.md` for cross-reference.
**Method:** Independent parallel review via MCP llm-tools, text-only prompt with full plan inlined per [[mcp-llm-text-only-no-fs-access]].

## Summary

- **Total findings:** 34 (Adam: 14, Eve: 20; shared concerns: 12)
- **Showstoppers:** 6 distinct architectural defects (S1–S4 and S6 caught by both reviewers; S5 flagged by Eve only)
- **Important:** 11 defects (off-by-one bugs, type-friction, race conditions, environment guards)
- **Minor:** 6 (cosmetic, doc accuracy, deliverable framing)

Mathematics verified sound by both reviewers (Christoffel formula, dual-number arithmetic, bridge physics: Mercury ~43"/century, solar grazing ~1.75"). Defects are in **plumbing, dispatch, contradictions, and unspecified work** — not the core algorithms.

---

## Shared concerns (both reviewers, priority fixes)

| # | Severity | Task | Issue | Fix |
|---|---|---|---|---|
| S1 | SHOWSTOPPER | 10 | MathTSEngine adapter `fnMathts = (t) => toMathTSTensor(fn(fromMathTSTensor(t)))` strips Dual/Taped instrumentation, so the AD trace is destroyed. Primal returns correct; gradients are 0/garbage. | Wrap **individual engine ops** at the boundary, not the top-level `fn`. OR make `fromMathTSTensor` passthrough Dual/Taped instances unchanged. |
| S2 | SHOWSTOPPER | 12 | Multiple sub-defects: (a) `derivativeStrategy='computed'` path uses `pderiv` finite-difference, never invokes the AD chain that Tasks 6-10 build; (b) `of.freeIndices` accessed on raw `ExprNode` but `freeIndices` lives on the validator-result, not the node; (c) helper functions `buildChristoffelForIndex`, `contractChristoffelWithOperand`, `tensorAdd`, `tensorAddScaled`, `zeroTensorLike` are all "implementer writes" with no spec; (d) sign rule only handles fully-free tensors, not mixed upper/lower. | Thread validation-result `freeIndices` through lowering context (or re-validate `of`); write explicit contraction loops in the plan; decide architecturally whether `'computed'` is FD (current pderiv) or AD-via-forwardGrad. |
| S3 | SHOWSTOPPER | 7 | `Tape.allocate` → `id = nodes.length + 1000`; `Tape.record` → `id = nodes.length`. Collision at `nodes.length ≥ 1000` silently overwrites `inputGradSlots`. | Disjoint ID namespaces: separate counters for inputs and ops, OR negative IDs for inputs, OR `Symbol`-keyed slots. |
| S4 | SHOWSTOPPER | 17 | `evaluateBE37CovariantEikonalNumerical` stub returns `shapiroDelaySec: 0`; test computes `relErr = |0 − closed|/closed = 1.0`, expects `< 1e-6`. Guaranteed FAIL at task time. | Either fully implement geodesic integration in Task 17 (substantial), OR mark the second `it.todo` and defer the Shapiro cross-check to v0.5.0. |
| S5 | SHOWSTOPPER | 9 (Eve only) | `DualTensor` lacks `.data` field; engine ops downstream of dispatch (e.g., the existing `contract`, `lower`) reach into `.data` → runtime crash on first call. | Add `data` getter on `EngineDualTensor`/`EngineTapedTensor` returning `this.primal` (or restructure dispatch to never let non-primal tensors reach non-AD-aware ops). |
| S6 | SHOWSTOPPER | 8/9/10/12/17 | Task 8 interface returns `Promise<T> \| T`; Task 9 sync; Task 10 async. Awaiting a sync value works at runtime but TS strict-mode return-type inference may reject through the union. Conformance suite (Task 11) assumes await on all paths. | Mandate **`async` everywhere** (or always-Promise-return) for uniform `await` semantics across consumers. |
| I1 | IMPORTANT | 2 vs 13 | Direct contradiction: Task 2 (`MetricSignatureError`) throws on `wrt`-label vs free-index collision; Task 13 (`DuplicateCoordinateWarning`) only warns via `process.emitWarning` on the same condition. Lowering must pick one. | **Needs Daniel's call.** Adam recommends "keep warn, drop throw." Eve recommends "keep throw, drop the relaxation." See "Open question for Daniel" below. |
| I2 | IMPORTANT | 14 | Initial 4-velocity `v0 = [1, 0, 0, 0]` (dt/dτ=1) wrong. Schwarzschild requires `dt/dτ = 1/√(1 − r_s/r₀) ≈ 1.005` at r₀ = 100·r_s. The 0.5% normalization error swamps the ±1e-6 cycloid tolerance. | `const v0 = [1/Math.sqrt(1 - r_s/r0), 0, 0, 0];` |
| I3 | IMPORTANT | 6/7 | `t.mul(t)` produces correct gradient only by **aliasing accident** (`thisGradSlot === otherGradSlot`, both `+= outputGrad·primal` → 2x). Fragile against future refactors. | Explicit alias check in `mul`'s backward closure: `if (this === other) thisGradSlot[i] += 2·outputGrad[i]·primal[i]; else split normally.` |
| I4 | IMPORTANT | 19 | Race condition: two concurrent first-time `getActiveEngine()` calls can both pass the cache-undefined guard, both create separate `MathTSEngine` instances; last-writer wins. | Cache a Promise, not an instance: `_resolutionPromise ??= detectAndCreate();` then `return _resolutionPromise;`. |
| I5 | IMPORTANT | 19 | `process.env.UPT_QUIET_FALLBACK` ReferenceError in browser. | `typeof process !== 'undefined' && process.env?.UPT_QUIET_FALLBACK !== '1'`. |
| I6 | IMPORTANT | 15/16 vs Design §8 | Design promises ±10⁻⁹ relative accuracy via geodesic-integration cross-validation; plan ships closed-form scalars only. Deliverable mismatch. | **Pick one** — add geodesic cross-validation tests to Tasks 15/16, OR scale back the design/CHANGELOG claim to "closed-form-only, geodesic verification deferred." |
| M1 | MINOR | 3 | Doc comment `DIMENSIONLESS / LENGTH = DIMENSIONLESS^(-1) * LENGTH^(-1) = LENGTH^(-1)` — middle equality is mathematical nonsense (DIMENSIONLESS is multiplicative identity, not multiplicative inverse). | `// Γ has units 1/length`. |

---

## Adam (Gemini 2.5 Pro) findings unique to Adam

| # | Severity | Task | Issue | Fix |
|---|---|---|---|---|

(see structured report below — folded into Shared above where Eve flagged the same.)

---

## Eve (OpenAI o3) findings unique to Eve

| # | Severity | Task | Issue | Fix |
|---|---|---|---|---|
| E8 | IMPORTANT | 3 | Unary `{op:'-', args:[...]}` with a single arg — many existing validators only accept binary `-`. | Special-case `args.length === 1` for `'-'` in the validator and executor. |
| E9 | IMPORTANT | 3 | `isChristoffelSymmetric` body in the plan is just `node.kind === 'tensor-product'` — that's not a symmetry check at all. | Implement actual μ↔ν symmetry check (swap labels, recursively compare ASTs), OR remove the helper from v0.4.0. |
| E11 | IMPORTANT | 14 | `(christoffelFn as any).r_s` monkey-patch is fragile — minification, ES-module passing, etc. break it. | Pass `r_s` as an explicit integrator option: `integrateGeodesic({ ..., domainMinRadiusFactor: 3, r_s })`. |
| E13 | IMPORTANT | 6 | forwardGrad Jacobian layout possibly mis-specced: stride = `xSize` (input-inner) but doc claims `[...y.shape, ...x.shape]` row-major. | **Verify carefully.** With `kY` outer and stride = `xSize` between successive y-rows, layout IS row-major `[...y.shape, ...x.shape]` (y outer, x inner). Eve may be wrong on this one — needs a hand-trace before changing. |
| E18 | MINOR | 20 | Test count: plan says ~1,420; design §9 says 1,445-1,470. Cosmetic but inconsistent. | Reconcile to actual CI count at release time. |
| E19 | MINOR | 7 | `Tape.backward` `+=` seed-then-pass — wasteful (slot starts at 0). | Overwrite-then-pass for the seed step. |
| E20 | MINOR | 3 | Validator may accept 3+ args to `'-'` if special-casing only adds unary. | `assert args.length === 1 || args.length === 2` for `'-'`. |

---

## Open question for Daniel (requires user decision)

**Task 2 vs Task 13: when `wrt`-label collides with an existing free index in `of`, do we throw or warn?**

- **Adam's recommendation:** Drop the throw in Task 2; keep the warn in Task 13. Friendlier developer experience; lets non-strict code paths continue.
- **Eve's recommendation:** Keep the throw in Task 2; drop the warn in Task 13. Soundness > friendliness — the collision really does produce silently-wrong results, so it should be a hard error.

**My recommendation:** Eve's path. Christoffel/covariant-derivative semantics are unforgiving; silent wrongness is the worst failure mode. A throw with a clear "rename the free index" message is the right safety floor for v0.4.0. Add a future-version env-flag (`UPT_ALLOW_COORD_SHADOW=1`) if a relaxation is ever genuinely useful.

**Action:** await Daniel's call before applying fixes to Tasks 2 and 13.

---

## Reviewer agreement on what's RIGHT

Both reviewers explicitly verified and confirmed:

- **Mercury perihelion:** `Δφ = 6πGM/(a(1-e²)c²)` evaluates to ~43 arcsec/century at standard Mercury parameters. ✓
- **Solar grazing lensing:** `α = 4GM/(b c²)` at b = R_sun, M = M_sun → 8.49e-6 rad ≈ 1.75 arcsec. ✓
- **Christoffel formula:** `Γ^λ_μν = (1/2)·g^λρ·(∂_μg_ρν + ∂_νg_ρμ − ∂_ρg_μν)` — standard Levi-Civita form. ✓
- **Dual-number arithmetic:** `(a + a'ε)(b + b'ε) = ab + (ab' + a'b)ε` rules written correctly. ✓
- **Reverse-mode backward closures:** `∂(a·b)/∂a = b`, `∂(a·b)/∂b = a` written correctly. ✓
- **Cycloid parametric form for Schwarzschild radial infall:** `r(η) = (r₀/2)(1+cos η)`, `τ(η) = (r₀/2)√(r₀/r_s)(η+sin η)/c` — standard GR reference. ✓

The plan's MATHEMATICS is sound. The defects are entirely in implementation plumbing.

---

## Reconciliation status

- [x] Daniel decides Task 2 vs Task 13 (throw vs warn) — **HYBRID**: default throws `MetricSignatureError`; env `UPT_ALLOW_COORD_SHADOW=1` downgrades to `process.emitWarning(DuplicateCoordinateWarning)`. Test covers both paths.
- [x] Apply all 12 shared-concern fixes inline to the plan (S1 MathTSEngine adapter passthrough; S2 covariant-derivative lowering re-validates `of`, documents `'computed'`=FD, specs helpers, mixed upper/lower sign rule; S3 Tape ID disjoint namespaces in both autograd and engine; S4 BE-37 second test `it.skip` for v0.4.0; S5 `.data` getters on Dual/Taped tensors; S6 Promise-only async interface; I1 hybrid; I2 4-velocity normalization; I3 alias check in `mul`; I4 Promise caching in registry; I5 browser-safe `process` guard; I6 geodesic cross-validation tests added)
- [x] Apply Eve-unique fixes — E8 (binary subtraction), E9 (drop `isChristoffelSymmetric`), E11 (explicit `domainMinRadius` option), E13 (Adam+Eve-verified row-major layout comment), E18 (test count framing), E19 (Tape.backward overwrite), E20 (binary-`-` validator assertion documented)
- [x] Apply Adam-unique fixes (folded into shared above)
- [x] Verify E13 forward-grad layout claim by hand-tracing — **FALSE POSITIVE**: layout IS row-major `[...y.shape, ...x.shape]` (y outer, x inner). Comment added.
- [ ] Commit reconciled plan
- [ ] Begin subagent-driven execution
