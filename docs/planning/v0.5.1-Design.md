# UPT v0.5.1 — Bug / Minimize / Simplify Release — Design

**Generated**: 2026-05-18 (post-v0.5.0 audit, post-Adam+Eve audit vet).
**Source**: `docs/architecture/v0.5.1-audit.md` (49 reconciled findings) + `todo.md` §"From v0.5.0" + memory entries.
**HEAD**: `ae59ce1` (post-audit commit).
**Pre-vet count**: 42 findings. **Post-vet count**: 49. Adam (Gemini 2.5 Pro) surfaced 5 net-new findings + 1 severity bump (PC-3 → HIGH); Eve (OpenAI o3) killed 3 spurious entries (PD-1 dup, LC-1/PD-4 over-severed) but fabricated 5 of her 9 challenges — see `feedback_eve_o3_hallucinations.md`.

> **Sister docs**: `docs/architecture/v0.5.1-audit.md` (authority for every finding; design references finding IDs directly). `docs/planning/v0.5.0-Design.md` (template for this file's shape). `docs/architecture/v0.4.6-minimize-targets.md` (precedent for category-style audits).

---

## Goal

Ship v0.5.1 as a **stability / hygiene patch** on top of v0.5.0's GR foundations release:

1. **Fix the physics-correctness gaps** the v0.5.0 ship surfaced (constants drift dominating BE-37 residual; silent diagnostic failure on the GR pipeline; doc-vs-code skew on the Ricci slot).
2. **Backfill missing test coverage** for the heaviest v0.5.0 additions (`connection-validators.ts`, `fresh-label.ts`, flat-Minkowski curvature zero-test, real Mercury 100-orbit Picard-convergence).
3. **Tighten type safety** on the new `bianchiResidual()` public surface and adjacent runtime casts.
4. **Establish bench harness** for the two SUSPECTED performance hotspots Adam surfaced (GL4 Picard allocation pressure, Ricci lowering round-trip) — measure before committing perf wins.
5. **Document the curvature-layer pattern** for next-addition extraction without extracting in v0.5.1 (premature).

**Non-goals** (deferred):
- `christoffelFn` flat-array refactor (BR-2) — breaking, defer to v0.6.0.
- Curvature `CurvatureCompositeNode<K,S>` factory (PD-6) — doc-only note in v0.5.1; extract on next curvature primitive (Weyl, Kretschmann, etc.).
- Vitest 4.2+ async-bench fix (PD-5) — upstream-blocked.

---

## Tag Decision

**v0.5.1 (additive / backwards-compatible) if BR-2 stays deferred**, which is the recommendation. The only candidate breaking change (BR-2 christoffelFn flat-array) is deferred. All other findings ship as additive (new modules, new tests, new helpers; old paths preserved).

If a finding's fix unexpectedly requires breaking an export (e.g., the bianchiResidual TS-1 type-tightening removes a coercion consumers depend on), bump to v0.6.0 instead — but post-vet analysis suggests this is unlikely.

---

## Architectural Decisions (locked)

### Decision #1 — Constants canonicalization shape

**Decision**: Introduce `src/core/constants.ts` with **flat named exports** (`C_SI`, `G_SI`, `HBAR_SI`, `H_SI`, `K_B_SI`, `M_P_SI`, `T_P_SI`, `L_P_SI`, `E_SI`, `ALPHA`, `H0_SI`), **not** an extension of the `PhysicalConstants` namespace.

**Rationale**: The flat-export pattern matches v0.5.0 ergonomics (`{ christoffel, ricci, einstein, bianchiResidual }` from `core/curvature`; `{ GL4_C, GL4_A, GL4_B }` from `numerical/gl4-integrator`). Per Adam+Eve audit vet (Eve's PC-4 challenge) the existing `PhysicalConstants` namespace in `src/core/types.ts:173-200` is **dead public API** (never imported); migrating to flat exports rather than evangelizing the namespace minimizes refactor surface in consumer code.

**Alternative considered**: Re-export `PhysicalConstants` via `src/index.ts` and have callers do `PhysicalConstants.c`. Rejected: longer call sites, `PhysicalConstants.c` reads as instance-access (it's a frozen namespace), and the v0.4.6 audit similarly preferred flat-export ergonomics.

**Migration path**: `PhysicalConstants` stays exported for v0.5.1 (no break); v0.6.0+ may deprecate. v0.5.1 wires every internal call site to the new flat exports.

### Decision #2 — `pderiv.ts` 4th-order stencil ergonomics

**Decision**: Add an **optional `order` parameter** (`order?: 2 | 4 = 2`) to `pderivNumericalFn` and `pderivGrid`, preserving v0.4.0 2nd-order default behavior. Consumers explicitly opt in to 4th-order via `{ order: 4 }`.

**Rationale**: Adam pushed for an unconditional `pderiv.ts` upgrade (his finding #3, reinforcing PD-7). I reject the unconditional upgrade — it changes the precision floor for every v0.4.0 consumer (covariant-derivative lowering, BE-37 eikonal, geodesic Christoffel evals), some of which are calibrated against the 2nd-order tolerance band. Opt-in is the conservative middle path: callers that hit SI-c² cancellation upgrade their site; callers that don't aren't perturbed.

**Migration**: `curvature-lowering-helpers.ts` (the v0.5.0 site already using 4th-order) refactors to pass `{ order: 4 }` to the shared `pderiv` rather than reimplementing the stencil. Net LOC reduction; behavior preserved.

### Decision #3 — `scanForMetricPair` extension shape

**Decision**: Extend the existing `walk` function in `src/numerical/metric-inverse.ts:73-83` with case arms for the 5 curvature node kinds: `covariant-derivative`, `riemann-tensor`, `ricci-tensor`, `einstein-tensor`, `bianchi-residual`. Each case walks the relevant metric sub-references.

**Rationale**: Minimal-surface fix. Adam upgraded PC-3 to HIGH and the reasoning is "a validation mechanism that doesn't run is worse than no validation at all". The fix is a 5-case `switch` extension, ~20 LOC, no API change.

**Alternative considered**: Refactor `scanForMetricPair` to walk via a visitor pattern dispatched off `ExprNode`'s discriminated union. Rejected: bigger refactor for marginal benefit; opens the door to future "forgot to update the visitor" bugs identical to the one PC-3 documents.

### Decision #4 — `connection-validators.ts` test coverage scope

**Decision**: Add `tests/dimensional/connection-validators.test.ts` with **per-throw-path unit tests** for `validateCovariantDerivative` and `validateRiemannTensor`. Mirror the v0.5.0 `tests/dimensional/riemann-tensor.test.ts` test-naming pattern.

**Rationale**: UC-1 is the highest-priority test-coverage gap. Per-throw coverage on this critical-path validator catches the kind of bugs that v0.5.0's Riemann-formula-and-Ricci-slot debacle exposed (validators silently accepting wrong inputs). v0.4.6's pattern of "per-function unit coverage on the validator layer" is the precedent.

**Test inventory** (minimum):
- `validateCovariantDerivative`: 4 throw paths (`wrtIndex.variance !== 'lower'`, `gLower` not both-lower, `gInverse` not both-upper, `wrt` label collision) + happy path.
- `validateRiemannTensor`: 5 throw paths (`upperIndex.variance !== 'upper'`, `lowerIndices.length !== 3`, gLower signature, gInverse signature, label collision) + happy path.
- Total: ~10 `it(...)` blocks.

### Decision #5 — Flat-Minkowski curvature test placement

**Decision**: Create `tests/dimensional/minkowski-curvature.test.ts` covering Riemann/Ricci/Einstein/Bianchi lowering on the trivial Minkowski metric `g_μν = diag(-1, 1, 1, 1)·c²` (mostly-plus SI convention matching v0.5.0 Schwarzschild fixture). All curvature components must be zero to machine precision.

**Rationale**: Adam's flat-Minkowski test gap (NEW-1, HIGH post-vet) is a real physics edge case. Minkowski is the universal "everything goes to zero" sanity check. If the curvature lowering pipeline returns non-zero on flat spacetime, every subsequent Schwarzschild test result is suspect.

**Tolerance**: `|R^ρ_{σμν}| / scale_local < 1e-12` (machine-epsilon range — flat space has no FD cancellation noise since `g^μν` is constant).

### Decision #6 — `bianchiResidual()` type-tightening via `import type`

**Decision**: Break the `dimensional/` → `numerical/` runtime cycle in `bianchiResidual()` (currently dynamic-imported per call, with `any`-typed engine/inputs) by switching to `import type { TensorEngine, NumericalInputs, NestedArray }` from `../numerical/*`. Type imports are erased at compile time so they introduce no runtime cycle. Replace the 6 `any` annotations + the dynamic `import('../numerical/index.js')` with a hoisted static import.

**Rationale**: TS-1 (HIGH) + AS-4 (MEDIUM) collapse into one fix. The cycle-avoidance pattern Daniel originally chose is correct in spirit (no runtime cycle) but unnecessarily reaches for `any` and per-call dynamic import.

**Verification**: `tsc --noEmit` must still pass post-change. If TypeScript surfaces a real circular import (it shouldn't — types are erased), fall back to keeping the dynamic import but typing it via `import type` only.

### Decision #7 — `it.todo` zombie removal scope

**Decision**: Delete all 7 zombie `it.todo` markers identified in PD-9 / UC-5 / AS-5 (they're three audit IDs for the same set). Replace nothing — the milestones they refer to have all shipped and have real tests elsewhere. Keep the single legitimate `it.todo` in `tests/bridges/catalog-integrity.test.ts:137` (BE-42 geodesic intentional deferral — Hawking temperature has no geodesic content).

**Rationale**: Zombie markers carry "pending" semantics in vitest reporters and mask actual coverage gaps. The Pareto-clean fix is removal, not replacement.

### Decision #8 — Bench harness for NEW-2 (GL4 alloc) before opt

**Decision**: Add `bench/gl4-picard-alloc.bench.ts` measuring Picard inner-loop wall-time on a representative Mercury orbit segment **before** attempting the buffer-reuse optimization Adam proposed.

**Rationale**: Adam's GL4 hot-loop allocation finding is SUSPECTED (he flagged it as "likely"). The audit's NEW-2 is properly marked SUSPECTED — bench-required. If the benchmark shows allocation cost is <2% of wall-time, the optimization is premature (Karpathy's "measure before optimize" rule from `karpathy-guidelines` skill).

**Trigger threshold**: Implement buffer reuse only if bench shows allocation >5% of step wall-time, or >10% GC-attribution under Node's `--trace-gc`.

### Decision #9 — `--ignore-scripts` framing

**Decision**: **Won't fix.** Add a note to `CLAUDE.md` and `README.md` documenting the deliberate `npm publish --ignore-scripts --access public` pattern (vitest cold-start avoidance on Windows; suite is already verified at release gate).

**Rationale**: Adam flagged this as a release-hygiene concern. The pattern is intentional per CLAUDE.md / todo.md `§Conventions`. Documenting it more visibly in README is the right call; "fixing" it (re-enabling scripts) would re-introduce the 3-5 min Windows cold-start that triggered the workaround in v0.4.5.

---

## Per-Theme Design

### Theme A — Constants canonicalization (PC-1, PC-2, PC-4, UC-6, LC-5, BR-1)

**Goal**: Eliminate the 6-file `c_SI = 2.998e8` / 4-file `G_SI = 6.6743e-11` drift. Single source of truth.

**Deliverables**:
1. New `src/core/constants.ts` with flat named exports (Decision #1). Include `@public` JSDoc citing CODATA 2018.
2. Migrate `src/numerical/be37-covariant-eikonal.ts:37-38`, `src/bridges/gravitational-lensing.ts:28-29`, `src/bridges/perihelion-precession.ts:30-31`, `src/bridges/equations/be-37-shapiro-delay.ts:304-305,484-485`, `tests/fixtures/schwarzschild.ts:31-33`.
3. After migration: re-run BE-37 Shapiro test. **Expected**: relErr drops from ~1.76e-4 to <1e-5 (PC-1 hypothesis verification). If it doesn't, PC-1's claim of "dominant cause" was wrong and we have a second source of drift to find.
4. Drop LC-5's now-obsolete "matches tests/fixtures/schwarzschild.ts" comment.
5. Re-export `C_SI`, `G_SI`, etc. from `src/index.ts` for consumer ergonomics.

**Track**: `refactor(constants)` for the migration; `fix(be37)` if the Shapiro residual changes.

### Theme B — Doc-vs-code skew (PD-3, LC-1, LC-2, LC-3, LC-4, LC-7, LC-8)

**Goal**: Eliminate every documented contradiction between source comments / planning docs and current implementation.

**Deliverables**:
1. **LC-1 (HIGH)** — Rewrite `RicciTensorNode` interface JSDoc at `src/dimensional/curvature.ts:31-43`. Spell out the Carroll Eq. 3.91 contraction (`lowerIndices[1]` is the dummy; free outputs are `[0]` and `[2]`). Add an ASCII index-map diagram matching `ricci()` function JSDoc.
2. **PD-3 / LC-2** — Add an addendum block at top of `docs/planning/v0.5.0-Design.md` and `docs/planning/v0.5.0-Implementation-Plan.md`: *"NOTE (post-S1 correction, 2026-05-18): the Ricci-contraction wording below pre-dates commit `76628c4`. The actual implementation contracts on `lowerIndices[1]` per Carroll Eq. 3.91. See `src/dimensional/curvature.ts` JSDoc + commit body."* Do NOT rewrite the bodies — preserve historical record.
3. **LC-3** — Drop the version literal from `src/numerical/engine-registry.ts:55` warn: `'[universal-physics-tensor] @danielsimonjr/mathts-tensor...'`. Avoid future-staleness.
4. **LC-4** — Drop the "as of v0.4.6" qualifier in `src/dimensional/connection-validators.ts:8-9`. If Part-IX is permanently absent, say "Part-IX-Connection-Layer.md — pending". Pattern fix in Theme G note.
5. **LC-7** — Polish `einstein()` JSDoc at `src/dimensional/curvature.ts:248`: clarify that Einstein inherits Carroll Eq. 3.91 from `ricci()` and that Einstein's own derivation is the `G_μν = R_μν - ½ R g_μν` step.
6. **LC-8** — Update `src/numerical/null-ray-integrator.ts:1-7` module description: "Used by `src/bridges/equations/be-37-shapiro-delay.ts` (closed-form bridge encoding); the v0.5.0 covariant-eikonal path uses the symplectic `integrateGeodesicGL4` instead."

**Track**: `docs(...)` per file. Each is a small atomic commit; no behavior changes.

### Theme C — Test coverage gaps (UC-1, UC-2, NEW-1 flat-Minkowski, PD-4 GL4_LONG, PD-9/UC-5/AS-5 zombie todos)

**Goal**: Backfill the heaviest test-coverage gaps from v0.4.0 + v0.5.0.

**Deliverables**:
1. **UC-1 (HIGH)** — `tests/dimensional/connection-validators.test.ts` per Decision #4. ~10 it() blocks.
2. **UC-2 (MEDIUM)** — `tests/dimensional/fresh-label.test.ts`: deterministic suffix, counter-starts-at-1, collision-with-existing-labels behavior. ~5 it() blocks.
3. **NEW-1 (HIGH)** — `tests/dimensional/minkowski-curvature.test.ts` per Decision #5. Riemann/Ricci/Einstein/Bianchi all zero to ≤1e-12 normalized on Minkowski metric.
4. **PD-4 (MEDIUM post-vet)** — Implement the real `GL4_LONG=1` Mercury 100-orbit Picard-convergence test in `tests/numerical/gl4-integrator.test.ts:159-173`. Instrument `solveGL4Stage` to expose `iterations` per call; aggregate across 100-orbit integration; assert `failureFraction < 0.001`. Wire Mercury IC from Task 10 (`tests/bridges/perihelion-precession.test.ts`).
5. **PD-9 / UC-5 / AS-5** — Delete the 7 zombie `it.todo` markers per Decision #7. Files: `tests/dimensional/covariant-derivative-preview.test.ts:34,38`; `tests/dimensional/inverse-metric-consistency.test.ts:15,16`; `tests/dimensional/tensor-symbol.test.ts:79,82,85`.

**Track**: `test(...)` per addition; `chore(test)` for zombie removal.

### Theme D — Diagnostic propagation (PC-3 HIGH post-vet, PD-7 HIGH, NEW-3 LOW)

**Goal**: Re-establish UPT's diagnostic guarantees post-v0.5.0 curvature-layer addition.

**Deliverables**:
1. **PC-3 (HIGH post-vet)** — Extend `scanForMetricPair` in `src/numerical/metric-inverse.ts:73-83` per Decision #3. Add 5 case arms for v0.4.0+v0.5.0 curvature node kinds. **Add a regression test** asserting `InverseMetricInconsistencyWarning` fires when a `RiemannTensorNode` is wrapped around inconsistent g/g⁻¹.
2. **PD-7 (HIGH)** — Add `order` parameter to `pderiv.ts` per Decision #2. Refactor `curvature-lowering-helpers.ts` to use it. Net-reduce LOC.
3. **NEW-3 (LOW)** — Triage the 10 `.toBe(<float>)` instances across 8 test files. Per-instance verdict:
   - Integer/exact-zero result → keep `toBe`.
   - True floating-point comparison → swap to `toBeCloseTo(x, n)`.
   - Convert ≥1 instance to demonstrate the pattern; defer the rest as polish.

**Track**: `fix(diagnostic)` for PC-3; `refactor(pderiv)` for PD-7; `test(polish)` for NEW-3.

### Theme E — Type-safety hardening (TS-1, TS-2, TS-3, TS-4, AS-4, NEW-4)

**Goal**: Tighten the v0.5.0 type surface that survived initial review with `any` / runtime-cast escape hatches.

**Deliverables**:
1. **TS-1 (HIGH) + AS-4 (MEDIUM)** — `bianchiResidual()` per Decision #6. Replace 6 × `any` with `import type` -sourced symbol names; hoist dynamic import to static; remove eslint-disable comments. **Verify** `tsc --noEmit` clean post-change. Public-API snapshot test should be unchanged (only types refined; runtime behavior identical).
2. **TS-2 (MEDIUM)** — `src/numerical/lowering.ts:472,478`: extract `isMetricTensorNode(n: unknown): n is MetricTensorNode` type predicate. Remove the `as MetricTensorNode` cast.
3. **TS-3 (MEDIUM)** — `src/dimensional/curvature.ts:480-493`: tighten `walk(v: unknown)` to `walk(v: NestedArray)`. Add `else throw new Error(\`unexpected shape: \${typeof v}\`)` branch to fail loud on engine-output deviations.
4. **TS-4 (MEDIUM)** — `src/numerical/engine-registry.ts:43-47`: replace `@ts-ignore` for the `@danielsimonjr/mathts-autograd` optional-peer import with a `try { (await import('@danielsimonjr/mathts-autograd' as 'undefined')) }` ergonomic pattern. If that fights `tsc`, declare a minimal `MathTSAutograd` ambient type (matching the v0.4.6 TS-1 fix for `mathts-tensor`).
5. **NEW-4 (LOW)** — Add `@param`-level unit annotations to public numerical evaluators (`evaluateBE37CovariantEikonalNumerical`, `evaluateGravitationalLensing`, `evaluatePerihelionPrecession`, `integrateGeodesicGL4`, `findPerihelion`). E.g., `@param M_kg gravitational mass in kilograms (SI)`. Pattern note in README.

**Track**: `refactor(types)` per change; `fix(typesafety)` for TS-1 since it's a public API change in semantics (any → typed).

### Theme F — Algorithmic simplifications (AS-1, AS-2, AS-3, PD-6)

**Goal**: Eliminate the largest duplications without abstracting prematurely.

**Deliverables**:
1. **AS-1 (HIGH)** — Extract `contractRiemannJS(flatR, N, contraction)` helper from `src/numerical/lowering.ts:596-690`. Both Ricci and Einstein arms use it. Configuration: `{ upperAxis: 0, lowerAxis: 2, outAxes: [1, 3] }` for the canonical Carroll Ricci contraction. **Verify** post-extraction: scoped vitest on `tests/dimensional/ricci.test.ts`, `tests/dimensional/einstein.test.ts`, `tests/dimensional/bianchi-residual.test.ts` all still green. Public API snapshot unchanged.
2. **AS-2 (MEDIUM)** — Extract `makeSchwarzschildContext(M_kg, x): { r_s, r, theta, sinT, sinT2, cosT, f }` from `tests/fixtures/schwarzschild.ts` 5 closures. Each closure opens with `const ctx = makeSchwarzschildContext(M_kg, x);`. Net ~25-30 LOC reduction.
3. **AS-3 (MEDIUM)** — Test-side helper: introduce `tests/fixtures/schwarzschild-pin.ts` exporting `pinR3RsScenario(M_kg)` returning the canonical M7 pin coordinate + pre-evaluated metric/Christoffel/Riemann for that point. Replace the ~65 duplicate `schwarzschild*Fn(M)([0, 3*r_s, π/2, 0])` invocations across 11 test files. Optional — could defer if scoped vitest scope-creep risk is too high.
4. **PD-6 (MEDIUM)** — Add `## Curvature layer pattern (v0.5.0)` section to `docs/architecture/COMPONENTS.md` (create file if absent). Document the 4-instance pattern (Riemann/Ricci/Einstein/Bianchi), the proposed `CurvatureCompositeNode<K extends string, S extends 'simple'|'scalar'|'gPair'>` factory shape, and the extraction trigger ("next curvature primitive — Weyl tensor, Kretschmann scalar, Bianchi-2nd-form, or Riemann-Cartan torsion piece"). **No code extraction.** Doc-only.

**Track**: `simplify(...)` for AS-1, AS-2, AS-3; `docs(architecture)` for PD-6.

### Theme G — Performance bench + opt (NEW-2 SUSPECTED, PO-1, PO-2, PO-3)

**Goal**: Establish measurement before optimization.

**Deliverables**:
1. **NEW-2 + PO-1** — `bench/gl4-picard-alloc.bench.ts` and `bench/ricci-lowering.bench.ts` per Decision #8. Measure Picard inner-loop allocation cost on Mercury orbit segment + Ricci round-trip overhead on M7-pin coordinate. **Threshold gates**: only implement perf optimization if bench shows >5% wall-time attributable to the suspected hot path.
2. **PO-2** — `bench/pderiv-grid.bench.ts` on a representative grid size. Defer optimization until grid-field lowering becomes a hot path (v0.6.0+ scope).
3. **PO-3** — Document in `docs/architecture/benchmarks.md`: 4th-order FD costs ~1024 effective Christoffel evals per Riemann sample at one point. No v0.5.1 action.

**If bench gates trigger**:
- NEW-2 → Implement pre-allocated `X`, `P`, `Xnew`, `Pnew` buffers in `solveGL4Stage`. Mutate in-place. **`perf(gl4)` commit with before/after hz numbers in commit body**.
- PO-1 → Implement engine-resident contraction primitive in `tensor-engine.ts`. **`perf(lowering)` commit**.

**Track**: `bench(...)` for harness additions; `perf(...)` for bench-backed opt commits (with before/after evidence).

### Theme H — Build / release hygiene (NEW-5, PD-5)

**Goal**: Modernize the release checklist without over-engineering.

**Deliverables**:
1. **NEW-5 (LOW)** — Add `npm audit` + `npm outdated` to the v0.5.1 release-gate checklist (Phase 5). If `npm audit` surfaces vulnerabilities, address before publishing. Document in `CLAUDE.md` as part of the release pattern.
2. **PD-5 (LOW)** — No v0.5.1 action. Note in `docs/architecture/benchmarks.md` that vitest 4.2+ may surface per-bench hz tables for async benches; revisit on vitest upgrade. Track at the dep level via NEW-5.

**Track**: `chore(release)` for checklist updates; `docs(benchmarks)` for the vitest note.

### Theme I — Doc-only "won't fix" / deferred

**Goal**: Capture decisions that don't get a code commit but should be visible.

**Deliverables**:
1. **LC-6 (already withdrawn)** — Confirmed false positive in audit reconciliation. No action.
2. **TS-5 (LOW)** — `src/numerical/geodesic-integrator.ts:62` historical-context comment about removed monkey-patch. Could shorten or leave. **Recommend leave** — it's a useful breadcrumb for future archaeology.
3. **UC-3 (LOW)** — `gl4-integrator.ts` exports flagged unused but test-consumed. Re-tag `solveGL4Stage`, `StageSolveResult`, `GL4_C`, `GL4_A`, `GL4_B` as `@internal` (already public via the test import; nothing changes at runtime). Single doc-only commit.
4. **UC-4 (verified consumed)** — No action; documented in audit reconciliation.
5. **PD-8 (MEDIUM)** — Wrap the two intentional `DuplicateCoordinateWarning` emission tests in `vi.spyOn(process, 'emitWarning')` to capture rather than surface to stderr. Cosmetic but eliminates the 2 noise lines in suite stderr output.
6. **--ignore-scripts** (Adam #7) — Note in README + CLAUDE.md per Decision #9.
7. **BR-2 christoffelFn flat-array** — Defer to v0.6.0 design. Note in `docs/planning/v0.6.0-Brainstorm.md` (create file).

**Track**: `docs(...)` for documentation; `chore(...)` for `@internal` retagging.

---

## Track Classification Summary

| Track | Commit count est. | Findings covered |
|---|---|---|
| `fix(...)` (physics/diagnostic correctness) | 3 | PC-1/PC-2/PC-4 (1 commit), PC-3, BE-37 residual verification |
| `refactor(...)` (additive cleanup, no break) | 6 | Constants migration; pderiv order param; bianchiResidual types; lowering type predicate; walk-tightening; engine-registry @ts-ignore |
| `simplify(...)` (dedup) | 3 | AS-1 contractRiemannJS; AS-2 schwarzschild context; AS-3 test-side helper (optional) |
| `test(...)` (coverage) | 5 | UC-1, UC-2, NEW-1, PD-4, NEW-3 polish |
| `chore(test)` (zombie todo removal) | 1 | PD-9/UC-5/AS-5 |
| `chore(...)` (retag, hygiene) | 3 | UC-3 @internal, NEW-5 release checklist, PD-8 spy-wrap |
| `docs(...)` (LC fixes + PD-6 + Adam #7 + BR-2 deferral) | 8 | LC-1, LC-2/PD-3, LC-3, LC-4, LC-7, LC-8, PD-6, NEW-4, --ignore-scripts framing |
| `bench(...)` (harness establishment) | 2 | NEW-2 GL4 alloc, PO-1 ricci |
| `perf(...)` (bench-backed opt — conditional) | 0-2 | NEW-2 + PO-1 if gates trigger |
| `chore(release)` (Phase 4-5) | 3-4 | Version bump, tag, push, publish |

**Total estimated commits**: 34-38. **v0.4.6 comparison**: shipped 25 tasks across 5 tracks. v0.5.1 is moderately larger by raw count but each commit is smaller / more atomic.

---

## Risk Register

| Risk | Trigger | Mitigation |
|---|---|---|
| **R1** Constants migration breaks BE-37 internal-consistency tests | New `C_SI` value (exact) propagates inconsistently | Migrate test fixtures AND production code in the same commit. Run BE-37 scoped suite after each file migration. |
| **R2** `pderiv` order parameter changes 2nd-order tolerance bands silently | Default `order = 2` preserved but consumers might still drift | Default is 2 (no consumer changes). Migration is opt-in — `curvature-lowering-helpers.ts` is the only v0.5.1 caller passing `order: 4`. |
| **R3** `scanForMetricPair` regression test would have caught a real v0.5.0 ship bug | PC-3 was latent in v0.5.0 ship — test wasn't there to fire | Adding the test as part of PC-3 fix establishes the regression baseline. No retroactive damage. |
| **R4** `bianchiResidual` type tightening surfaces a real public consumer break | `any` → typed parameters may reject consumer code that was passing untyped values | Public-API snapshot test (`tests/api/public-surface.test.ts`) will catch shape changes. Run before commit. If consumer breakage surfaces, fall back to `unknown` + runtime guard (still better than `any`). |
| **R5** Bench harness reveals NEW-2 GL4 alloc cost is <2% (no perf win available) | Adam's "likely allocates" was speculative | OK — bench-gated opt won't ship. Document the negative result in the bench module JSDoc. |
| **R6** AS-1 `contractRiemannJS` extraction introduces a regression in Ricci or Einstein lowering | Refactor risk on critical-path code | Scoped vitest on Ricci/Einstein/Bianchi after extraction. Public-API snapshot unchanged. |
| **R7** Flat-Minkowski test fails (curvature lowering produces non-zero on flat spacetime) | Real bug Adam's miss-case caught | This is the whole point of the test. If it fails, it's a HIGH bug to fix — likely a `lowering.ts` arm that doesn't handle the trivial-zero `∂Γ` case correctly. Treat as a blocker. |
| **R8** Eve hallucinations re-occur during design vet | Eve fabricated 5/9 challenges on audit vet | Already documented in `feedback_eve_o3_hallucinations.md`. Submit Eve's specific claims to grep verification before accepting. |
| **R9** v0.5.1 scope creeps past 35 commits | Each "small" cleanup tempts adjacent polish | Each commit must trace back to a finding ID. Reject "while I'm here" additions. Defer LOW polish to v0.5.2 if needed. |

---

## Adam+Eve Design Vet (pending)

This section will be appended after the Adam (Gemini 2.5 Pro) + Eve (OpenAI o3) adversarial design review runs. MCP llm-tools were unavailable during initial draft; vet will land as a follow-up commit on the `v0.5.1-Design.md` once `/reload-plugins` brings them back. Expected adversarial directions:

- **Adam (miss-case)**: which design decisions overlook v0.6.0+ implications? Are there findings the audit caught but the design dropped on the floor? Did the track classification miss any commit type (e.g., `revert(...)` if a previous fix is being undone)?
- **Eve (false-positive)**: are any design decisions over-engineered for v0.5.1 patch scope? Should any "deferred" finding actually be in scope? Is the tag-bump-to-v0.6.0 trigger threshold sound?

Per `feedback_eve_o3_hallucinations.md`: Eve's specific-evidence claims will be grep-verified before acceptance.

---

## Self-Review

**Spec coverage**: Every reconciled audit finding maps to at least one design theme (see Per-Theme Design section above). Cross-walk:

- PC-1, PC-2, PC-4, UC-6, LC-5, BR-1 → Theme A
- PD-3, LC-1, LC-2, LC-3, LC-4, LC-7, LC-8 → Theme B
- UC-1, UC-2, NEW-1, PD-4, PD-9, UC-5, AS-5 → Theme C
- PC-3, PD-7, NEW-3 → Theme D
- TS-1, TS-2, TS-3, TS-4, AS-4, NEW-4 → Theme E
- AS-1, AS-2, AS-3, PD-6 → Theme F
- NEW-2, PO-1, PO-2, PO-3 → Theme G
- NEW-5, PD-5 → Theme H
- LC-6, TS-5, UC-3, UC-4, PD-8, BR-2, Adam #6/#7/#8 → Theme I (deferred / docs / no-action)

**Findings dropped from v0.5.1 scope**:
- BR-2 (christoffelFn flat-array, breaking) → v0.6.0
- PD-6 abstraction extraction → v0.6.0+ when triggered by Weyl/Kretschmann addition
- PO-2 grid `pderiv` optimization → v0.6.0+ when grid-field lowering becomes hot
- PO-3 4th-order FD batching → v0.6.0+ when grid-field Riemann arrives
- Adam #6 (GL4 Options) → rejected (already typed)
- Adam #7 (`--ignore-scripts`) → won't fix (documented Windows workflow)
- Adam #8 (cryptic error messages) → too hand-wavy; re-file if specific instances surface
- LC-6 → false positive (already withdrawn)
- TS-5 → keep as historical-context breadcrumb

**Honest framing**:
- v0.5.1 will NOT make UPT "complete" or "clean" — it ships a patch on top of v0.5.0's GR foundations. Two of the four pre-vet HIGH findings (PD-1 dropped, PD-4 downgraded) survived the adversarial vet as MEDIUM or below. The actual HIGH-severity surface is smaller than the initial audit suggested: PC-1 (constants), PC-3 (diagnostic), PD-7 (FD order), TS-1 (bianchiResidual any), UC-1 (connection-validators test), LC-1 (RicciTensorNode JSDoc), AS-1 (lowering dedup), NEW-1 (Minkowski test).
- Performance commits are bench-gated; they may not ship if benchmarks show the suspected hot paths aren't actually hot.
- BR-2 deferred to v0.6.0 is the load-bearing tag decision. If v0.5.1 surfaces a finding that genuinely needs breaking (unlikely given the audit), we bump to v0.6.0 mid-flight and update the plan accordingly.

**Estimated v0.5.1 task count**: 25-32 (depending on whether bench gates trigger perf commits). Comparable to v0.4.6's 25 actually-shipped tasks.

**Suite target**: ≥1554 passing (v0.5.0 baseline). Net additions: ~30 new tests across UC-1/UC-2/NEW-1/PD-4 + scattered. Net removals: 7 zombie `it.todo`s. Expected final: ~1580 passing.

**Honest-claude notes**:
- Decision #2's "opt-in" framing on `pderiv` order is the conservative call but it does leave the latent precision floor present for non-curvature consumers. If a future bridge differentiates a `c²·g_tt`-scale quantity at 2nd-order and hits the noise floor, this design's opt-in pattern won't catch it. v0.6.0 should reconsider an unconditional upgrade once enough consumers have opted in.
- The flat-Minkowski test (Decision #5) carries a small risk: if the test fails on landing, the curvature pipeline has a latent bug. This is a feature of the test, not a risk to manage out. Treat any failure as a v0.5.1 release-blocker.
- NEW-2 GL4 allocation is bench-gated. The literal phrasing matters: we don't promise a perf win, we promise a measurement.
