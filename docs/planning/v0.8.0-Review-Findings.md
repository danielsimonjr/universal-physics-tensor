# v0.8.0 Review Findings — Adam vet of Design r1 + Plan r1

> Adam (Opus subagent, evidence-based) — 2026-06-11. Verdict: **YELLOW**
> (2 HIGH / 6 MED / 5 LOW, all fixable in design r2; no architectural
> change required). All findings grounded in file:line evidence; the
> CT-1/CT-1b/CT-2 physics and evaluator shapes verified correct.

## HIGH (fixed in Design r2)

- **A-1** `compose` collides with the existing public Cell-factory
  export (`src/index.ts:51` ← `src/core/cell.ts:166`, pinned by
  `tests/api/public-surface.test.ts:75` + snapshot). → Composition
  operator renamed **`composeEdges`**.
- **A-2** GW170817 positive-bound 5% tolerance fails on its own
  arithmetic: recomputed c·1.74/26 Mpc = 6.50e-16 vs published 7e-16
  (1 sig fig) = 7.1% off. → Tests pin the exactly-recomputed values
  (+6.50e-16 / −3.09e-15) and assert the published values are their
  1-sig-fig roundings.

## MED (fixed in Design r2 / Plan r2)

- **A-3** Lower-bound formula spelled out: `upper = +c·Δt_obs/D`;
  `lower = −c·(t_delay_max − Δt_obs)/D` with Δt_obs = 1.74 s,
  t_delay_max = 10 s, D = 26 Mpc.
- **A-4** BE-36's encoded bound is **symmetric** (`|ratio| ≤ 1e-15`,
  be-36-gw-speed-bound.ts:77,111-113), and the published −3e-15 violates
  it. → `passesEncodedBound` compares the positive side only; BE-36
  docstring correction (symmetric vs asymmetric published bounds;
  40→26 Mpc derivation) is a REQUIRED deliverable.
- **A-5** BE-42 tuple flip breaks three count pins in
  `tests/bridges/catalog-adapter.test.ts:68,124,133` (22→23 submittable)
  + stale comments at :51,:58,:115 — invisible to a `bridges:` grep. →
  Phase-4 gate extended to grep `toHaveLength`/`catalogToCells`/
  `scanCatalog`; counts + comments updated per adjudicated entry in the
  same commit.
- **A-6** `['unknown','unknown']` is the repo's deliberate NOT-A-BRIDGE
  structural marker (BE-28 notes index.ts:947, BE-42 notes :1469), so
  "contains unknown → unadjudicated" misclassifies the rejected set. →
  Precedence pinned: rejected-registry overlay FIRST, tuple proxy
  second. Unknown-tuple population: BE-28, 29, 32, 35, 40, 42, 44, 46,
  50.
- **A-7** Public-surface snapshot (`tests/api/public-surface.test.ts:109`
  + `EXPECTED_RUNTIME_EXPORTS` :23-99) named as the explicit pin to
  update with the new composition exports.
- **A-8** v0.8.0 version collision with the queued hygiene sprint
  (todo.md Decision #2). → Phase 8 records the adjudication in todo.md:
  composition MVP owns v0.8.0; the hygiene sprint renumbers to v0.9.0.

## LOW (folded into Plan r2 task gates)

- **A-9** No `M_sun` constant exists; pin the repo-conventional literal
  `1.989e30` kg.
- **A-10** BE-51/52 evaluators return objects (extract `alpha_rad` /
  `dphi_rad_per_orbit`); `evaluatePerihelionPrecession` requires
  `T_yr > 0` even though the ratio doesn't use it — wrapper forwards a
  default.
- **A-11** Reuse `equals` from `src/dimensional/algebra.ts:48` (already
  public) — no new `dimEquals`.
- **A-12** BE-43's tuple order is `['quantum','gravity']`; BE-42's new
  tuple `['gravity','quantum']` is source→target for M→T_H, order-
  reversed from BE-43 — stated explicitly, not "matching" vocabulary.
- **A-13** Wrapped evaluators' input interfaces are `@internal`
  non-exports → edges deep-import module paths and re-declare input
  shapes inline.

## Verified correct (no change)

Evaluator shapes (BE-42 `{M_kg}→K`, BE-16 `{temperature_K}→J`,
BE-51/52 object returns); CT-2 algebra (2a(1−e²)/(3πb), M/G/c cancel);
CT-1 dimensional + numerical check (T_H(M_sun)=6.17e-8 K,
E≈5.90e-31 J); CT-1b algebraic identity; k_B literals bit-identical
across modules (1e-12 pin achievable); dist/ import path for the JSON
emitter; min-confidence inputs (highly-speculative ∘ speculative).
