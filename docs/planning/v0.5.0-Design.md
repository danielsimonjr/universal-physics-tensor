# UPT v0.5.0 Design — GR Foundations: Symplectic + Curvature + Bridge-Validation Sweep

**Date:** 2026-05-18
**Status:** Design draft — Adam+Eve adversarial review reconciled (2026-05-18); pre-implementation-plan handoff.
**Author:** Daniel Simon Jr., with brainstorming-skill agent (Opus 4.7 1M). Architectural decisions locked interactively 2026-05-18; this doc captures those locks for adversarial vetting.
**Companion runbooks:** [`v0.2.0-Design.md`](v0.2.0-Design.md), [`v0.3.0-Design.md`](v0.3.0-Design.md), [`v0.3.5-Design.md`](v0.3.5-Design.md), [`v0.4.0-Design.md`](v0.4.0-Design.md).
**Pre-flight audit:** [`docs/architecture/bridge-coverage-audit.md`](../architecture/bridge-coverage-audit.md) (2026-05-16) — drives Phase 3.
**Spec foundation:** Part-VIII (Metric), Part-IX (Connection — shipped v0.4.0). v0.5.0 introduces `Part-X-Curvature-Layer.md`.

---

## 1. Goal

Land the **GR foundations** that v0.4.0 deferred and the bridge-validation precision that v0.4.6 documented but did not enforce. Concretely: (a) ship a symplectic geodesic integrator (Gauss-Legendre 4th-order, canonical (x,p) state) + bisection perihelion finder so the two `it.skip` cross-validation tests from v0.4.0 (BE-52 Mercury Δφ, BE-37 full Shapiro via geodesic) can be **activated** rather than deferred again; (b) ship the curvature layer (`RiemannTensorNode` AST + `ricci()` / `einstein()` / `bianchiResidual()` helpers) so v0.6.0+ Friedmann-flavored cosmology bridges have the infrastructure they need; (c) close the structural-parity gaps surfaced by the v0.5.0 pre-flight bridge-coverage audit (BE-51 + BE-52 sibling structural tests, plus ~5 catalog-only bridges getting tighter physics anchors).

Pure-additive — no breaking changes. Pattern-mirrors v0.4.0 (CovariantDerivativeNode + christoffel helper) one level up.

## 2. Architecture summary

### Four locked architectural decisions

| # | Decision | Chosen | Rationale |
|---|---|---|---|
| 1 | **Scope** | **Maximal v0.5.0**: bridge validation + symplectic + curvature, all three blocks in the same release. | The three blocks share testing infrastructure (Schwarzschild fixture, geodesic integrator) and share the `it.skip` debt-clearance theme — splitting them produces three half-releases with overlapping test surfaces. |
| 2 | **Symplectic algorithm** | **Gauss-Legendre 4th-order** (2-stage implicit Runge-Kutta, symmetric Butcher tableau). | Mercury Δφ_GR ≈ 5.02·10⁻⁷ rad/orbit requires a true symplectic integrator over multi-orbit integration. Ruth-4 (Yoshida) is symplectic **only** for separable Hamiltonians `H = T(p) + V(q)`; the geodesic Hamiltonian `H = ½ g^μν(x) p_μ p_ν` is NOT separable because `g` depends on `x`. Gauss-Legendre 4-stage is symplectic for **any** Hamiltonian via its symmetric Butcher tableau — it preserves the symplectic 2-form exactly (modulo roundoff) for non-separable systems. State variables are canonical `(x, p)` not `(x, v)` — kicking `v` instead of `p` breaks symplecticity regardless of integrator; see §5.3. Butcher tableau and Legendre coefficients pinned (§5.3). |
| 3 | **Curvature API** | **Mixed pattern**: `RiemannTensorNode` is a first-class `ExprNode` kind (operator); `ricci(R)`, `einstein(R, g)`, `bianchiResidual(R)` are **helpers** that build composite trees from existing v0.4.0 primitives (CovariantDerivativeNode + raise/lower + tensor-product + contract). | Mirrors v0.4.0's locked precedent (`CovariantDerivativeNode` = node; `christoffel()` = helper). Riemann is the **operator** users write; Ricci/Einstein are *contractions* of Riemann — formulaic, not primitive. Bianchi is a *residual identity*, not an operator. |
| 4 | **Sequencing** | **Bottom-up**: Phase 1 Foundations → Phase 2 Activations → Phase 3 Validation → Phase 4 Release. | Phase 2 activations literally cannot fire without Phase 1 (symplectic + bisection) shipped; Phase 3 bridge-validation needs the activated infrastructure to anchor against. Top-down would invert the dependency graph and force late refactors. |

### Four-phase structure (~25–30 tasks total)

- **Phase 1 — Foundations** (12–15 tasks): Gauss-Legendre 4th-order integrator on canonical `(x, p)` state (1a) + bisection perihelion finder (1b) + `RiemannTensorNode` AST/validator/lowering (1c) + `ricci()` (1d) + `einstein()` (1e) + `bianchiResidual()` (1f). Each sub-task is its own implementer+reviewer cycle.
- **Phase 2 — Activations** (3–5 tasks): BE-52 Mercury perihelion `it.skip` → activated (2a); BE-37 full Shapiro `it.skip` → activated (2b); `evaluateBE37CovariantEikonalNumerical` actually returns a non-zero `shapiroDelaySec` (2c).
- **Phase 3 — Bridge validation sweep** (7–8 tasks per bridge-coverage-audit §"Recommended next steps"): BE-51 + BE-52 structural sibling tests (3a-3b); ~5 catalog-only bridges get tighter physics anchors (3c–3g per audit lines 127–135); catalog-level integrity test (3h).
- **Phase 4 — Release** (3 tasks): version bump + CHANGELOG (4a); full suite + `tsc` gate (4b); tag → push → npm publish with `--ignore-scripts` per Windows lesson (4c).

---

## 3. Detailed design per phase

### Phase 1 — Foundations (~12–15 tasks)

#### Task 1a — Gauss-Legendre 4th-order symplectic integrator

**Files**:
- Create: `src/numerical/gl4-integrator.ts` (~180–220 LoC; replaces the Ruth-4 design).
- Test: `tests/numerical/gl4-integrator.test.ts` (new).
- Modify: `src/numerical/index.ts` (export `integrateGeodesicGL4`).

**State variable change (S1 / Daniel decision)**: state is now **canonical `(x, p)`** where `p_μ = g_μν(x) v^ν` (covariant momentum), NOT `(x, v)`. Kicking `v` (not `p`) destroys the symplectic form regardless of integrator. Conversion at integrator construction: `p_μ = g_μν(x₀) v₀^ν`. Conversion at output (if caller wants 4-velocity): `v^ν = g^μν(x) p_μ`.

**Key signatures**:
```typescript
export interface GL4State {
  /** Coordinate 4-vector x^μ. */
  readonly x: readonly number[];
  /** Covariant momentum p_μ = g_μν v^ν. */
  readonly p: readonly number[];
}
export interface GL4Snapshot {
  /** Proper time / affine parameter. */
  readonly tau: number;
  readonly x: readonly number[];
  readonly p: readonly number[];
  /** 4-velocity v^ν = g^μν p_μ (computed at snapshot, optional convenience). */
  readonly v?: readonly number[];
}
export interface GL4Options {
  /** Step count (uniform stepping; GL4 requires Newton iteration per step). */
  readonly steps: number;
  /** Final proper time (or affine parameter for null geodesics). */
  readonly tauMax: number;
  /** Metric inverse g^μν(x): used to derive Hamilton equations. */
  readonly gInverseFn: (x: readonly number[]) => readonly number[][];
  /** Partial derivatives ∂_λ g^μν(x): needed for Hamilton's equations of motion. */
  readonly dgInverseFn: (x: readonly number[]) => readonly number[][][];
  /** Newton iteration tolerance (default 1e-12). */
  readonly newtonTol?: number;
  /** Max Newton iterations per stage (default 50). */
  readonly newtonMaxIter?: number;
  /** Domain restriction (e.g., r ≥ 3·r_s for Schwarzschild). */
  readonly domainMinRadius?: number;
}
export function integrateGeodesicGL4(
  initialState: GL4State,
  options: GL4Options,
): ReadonlyArray<GL4Snapshot>;
```

**Algorithm** (2-stage Gauss-Legendre implicit Runge-Kutta; Hairer/Lubich/Wanner §II.1):

Gauss-Legendre 4th-order is a 2-stage collocation method with Butcher tableau:
```
Butcher tableau (GL4, 2 stages):
  c₁ = ½ − √3/6    a₁₁ = ¼           a₁₂ = ¼ − √3/6
  c₂ = ½ + √3/6    a₂₁ = ¼ + √3/6    a₂₂ = ¼
                    b₁ = ½             b₂ = ½

Quadrature nodes:  c₁ ≈ 0.2113248654, c₂ ≈ 0.7886751346
Stage weights:     b₁ = b₂ = 0.5
```

Hamiltonian equations of motion (from `H = ½ g^μν(x) p_μ p_ν`):
```
dx^μ/dτ = ∂H/∂p_μ = g^μν(x) p_ν
dp_μ/dτ = −∂H/∂x^μ = −½ (∂_μ g^νρ)(x) p_ν p_ρ
```

For each step of size h, solve the implicit system for stage values `(X₁, P₁), (X₂, P₂)`:
```
X_i = x_n + h · Σ_j a_{ij} · g^{·ν}(X_j) P_{j,ν}      (i = 1, 2)
P_{i,μ} = p_n − h · Σ_j a_{ij} · ½ (∂_μ g^νρ)(X_j) P_{j,ν} P_{j,ρ}
```
Solved via **fixed-point / simplified Newton iteration** (explicit dependence on previous Newton iterate — data flows from `(X_j, P_j)` at iterate `k` to update `(X_i, P_i)` at iterate `k+1`; no simultaneous cross-stage coupling in the linearised system because the Jacobian of the simplified Newton step decouples the two stages under the standard GL diagonal-block approximation). Iterate until `max‖δX, δP‖ < newtonTol` or `newtonMaxIter` reached.

Update:
```
x_{n+1} = x_n + h · (b₁ · g^{·ν}(X₁) P_{1,ν} + b₂ · g^{·ν}(X₂) P_{2,ν})
p_{n+1} = p_n − h · (b₁ · ½ (∂_μ g^νρ)(X₁) P_{1,ν} P_{1,ρ} + b₂ · ½ (∂_μ g^νρ)(X₂) P_{2,ν} P_{2,ρ})
```

**Symplecticity** (proof sketch): GL4 is a Gauss collocation method; all Gauss methods satisfy the symplecticity condition `b_i a_{ij} + b_j a_{ji} = b_i b_j` for all `i,j` (Sanz-Serna 1988). This holds for ANY smooth Hamiltonian including `H = ½ g^μν(x) p_μ p_ν`; no separability assumption.

**Test approach**:
1. Schwarzschild radial-infall cycloid fixture — GL4 must match cycloid to **≤1e-13 relative** at 5000 steps (same target as the previous Ruth-4 design; GL4 has the same 4th-order accuracy per step).
2. Energy-conservation test: bound Mercury-like orbit, 100 orbits, Hamiltonian drift `|H(t) − H(0)| / |H(0)| ≤ 1e-10` over the full run. (True symplectic for non-separable H — this tolerance IS achievable; see I1 reconciliation in §11.)
3. Symplectic-form preservation: two initial `(x, p)` states displaced by `δ`; the phase-space volume `δx ∧ δp` preserved to ≤1e-12 after one orbit. (Cheap sanity check.)
4. Newton convergence smoke test: at each step, Newton iteration must converge in ≤20 iterations for the Schwarzschild Mercury-scale orbit at the chosen step size. Fails the test if `newtonMaxIter` is hit in >0.1% of steps.

**Dependencies**: none — GL4 is self-contained. `GL4State`/`GL4Snapshot` are new types (v0.4.0 `GeodesicState`/`GeodesicSnapshot` are retained for backward compatibility with the existing RK4 integrator path).

**Honest framing**: GL4 requires **Newton iteration per step** (implicit RK). This is more expensive than Ruth-4's explicit drift-kick per step. Cost per step: 2 stages × ≤50 Newton iterations × (g^μν eval + ∂g^μν eval). For Mercury 2-orbit at 50k steps this is acceptable on CI but slower than the Ruth-4 alternative would have been. The win is **correct symplecticity for non-separable H** — Ruth-4 would have a secular non-symplectic error because the geodesic Hamiltonian mixes x and p inside g^μν(x).

#### Task 1b — Bisection perihelion finder

**Files**:
- Create: `src/numerical/perihelion-finder.ts` (~80–120 LoC).
- Test: `tests/numerical/perihelion-finder.test.ts` (new).
- Modify: `src/numerical/index.ts` (export `findPerihelion`).

**Key signatures**:
```typescript
export interface PerihelionOptions {
  /** Trajectory snapshots from integrateGeodesicGL4 (must include canonical p). */
  readonly snapshots: ReadonlyArray<GL4Snapshot>;
  /** Tolerance for bisection (proper-time precision target). */
  readonly tauTolerance: number;
  /** Metric inverse for extracting dr/dτ from p (g^μν at snapshot x). */
  readonly gInverseFn: (x: readonly number[]) => readonly number[][];
}
export interface PerihelionResult {
  /** Proper time of perihelion. */
  readonly tau: number;
  /** Position 4-vector at perihelion. */
  readonly x: readonly number[];
  /** Azimuthal angle φ at perihelion (extracted from x[3] in Schwarzschild coords). */
  readonly phi: number;
}
export function findPerihelion(opts: PerihelionOptions): PerihelionResult;
```

**Algorithm** (I6 bisection-cost fix: polynomial interpolation, not full re-integration):
1. Walk snapshots; find consecutive pair `(s_i, s_{i+1})` where `dr/dτ` changes sign from `−` to `+`. Radial velocity is extracted as `dr/dτ = g^{rν}(x) p_ν` (using canonical p — see S1 state-variable fix).
2. **Bracket-width check (M3)**: if `|τ_{i+1} − τ_i| < 2h` (narrower than one integrator step), the snapshot grid is too coarse — emit a warning and refine the enclosing step by sub-stepping GL4 at `h/10` for that step only, caching the sub-step snapshots. Do NOT proceed to bisection on sub-step-width brackets without this refinement.
3. **Bisect via polynomial interpolant**: fit a cubic Hermite polynomial through `(τ_i, dr/dτ_i)` and `(τ_{i+1}, dr/dτ_{i+1})` using `dr/dτ` values already in the snapshot cache. Evaluate the polynomial root — this gives a `τ_mid` estimate in O(1) without re-integrating. Cache sub-interval snapshots from step 2 for repeated evaluations.
4. If the polynomial root estimate has residual `|P(τ_mid)| > tauTolerance`, refine with one bisection step on the cached sub-snapshots (no full GL4 re-integration call per bisection step).
5. Terminate when `|τ_b − τ_a| < tauTolerance` (default suggestion: `1e-10 · T_orbit`).
6. Return final state's `τ, x, phi`.

**Test approach**:
1. Synthetic test: analytic cycloid (which has perihelion at `η = π`), inject `r(τ)` from the closed form, bisect → must find perihelion `τ` to ≤1e-12 relative.
2. Round-trip GL4: Mercury-scale ellipse integrated for one orbit via `integrateGeodesicGL4` on canonical (x, p) → bisection via polynomial interpolant finds perihelion → `φ_perihelion(orbit 2) − φ_perihelion(orbit 1)` must match `6πGM/(a(1−e²)c²)` to ±5×10⁻⁴ relative (consistent with 2a tolerance; see I4 reconciliation).

**Dependencies**: 1a (uses GL4 snapshots as input; sub-step refinement in step 2 re-uses `integrateGeodesicGL4`).

**Honest framing**: bisection precision floor is bounded by GL4's per-step error (~1e-13 per step). The 1e-10 × T_orbit target is conservative. Polynomial interpolation (step 3) avoids O(N log ε) full-integrations per bracket — the previous design's `refine` callback pattern would cost minutes per CI run over many bisection brackets. This does NOT block 2a (Δφ tolerance is ±5×10⁻⁴ relative, far above the bisection floor).

#### Task 1c — `RiemannTensorNode` (AST + validator + lowering)

**Files**:
- Modify: `src/dimensional/connection-validators.ts` — add `RiemannTensorNode` interface + `case 'riemann-tensor'` to `infer()`.
- Modify: `src/numerical/lowering.ts` — add `case 'riemann-tensor'` lowering.
- Create: `src/numerical/curvature-lowering-helpers.ts` (parallels `connection-lowering-helpers.ts`).
- Test: `tests/dimensional/riemann-tensor.test.ts` + `tests/numerical/riemann-tensor-lowering.test.ts`.

**Key shapes**:
```typescript
// src/dimensional/connection-validators.ts
export interface RiemannTensorNode {
  readonly kind: 'riemann-tensor';
  readonly upperIndex: UpperIndex;            // ρ
  readonly lowerIndices: readonly [
    CovariantIndex,                            // σ
    CovariantIndex,                            // μ
    CovariantIndex,                            // ν
  ];
  readonly gLower: MetricTensorNode;
  readonly gInverse: MetricTensorNode;
  readonly xCoord: TensorSymbolNode;
}
```

**Index convention (locked, §5.1)**: 1 upper + 3 lower → `R^ρ_σμν` per Carroll Ch. 3. Definition (S3 corrected — Γ indices from Carroll §3.4):
```
R^ρ_σμν = ∂_μ Γ^ρ_σν − ∂_ν Γ^ρ_σμ + Γ^ρ_λμ Γ^λ_σν − Γ^ρ_λν Γ^λ_σμ
```
Note the index placement: in each Γ term, `σ` occupies the **second** lower slot (not the third). The `μ, ν` indices are the derivative/antisymmetry pair on the outer slots. This differs from the previous draft which had `σ` and `ν` swapped in the Γ factors.

**Validator output**: `freeIndices = {ρ: upper, σ: lower, μ: lower, ν: lower}`; `dim: { L: -2, M: 0, T: 0, I: 0, Theta: 0, N: 0, J: 0 }` (curvature has units 1/L² in SI — Riemann appears in tidal-force equations as `R^μ_νρσ u^ρ u^σ` contracted with velocity²/L → acceleration, consistent with SI; see I3 reconciliation in §11); `gLower`/`gInverse`/`xCoord` validated *without* propagating their free indices upward (same rule as `covariant-derivative` per v0.4.0 §7 H1 fix).

**Lowering**: `riemann-tensor` → emits a composite expression tree built from `christoffel()` outputs and `pderiv` of Christoffel, then computes numerically. The lowering pass is **NOT** a recursive AST rewrite at validation time (which would explode tree size — see §7 Risk Register); instead, lowering walks the `riemann-tensor` node directly, evaluating `Γ^ρ_νσ(x)` and `∂_μ Γ^ρ_νσ(x)` numerically at the supplied coordinate point using `derivativeStrategy`-honoring paths. Result: a 4×4×4×4 `EngineTensor`.

**Test approach**:
1. Validator: free-indices = `{ρ: upper, σ: lower, μ: lower, ν: lower}`; gLower/gInverse indices consumed internally; dim `{ L: -2 }`.
2. Numerical: Schwarzschild fixture → `R^ρ_σμν` components match Carroll Ch. 5 exercise values (e.g., `R^t_rtr = 2GM/r³` in SI units with c=1 geometric convention, `R^θ_φθφ = r_s/r · sin²θ`) to ≤1e-9 relative.
3. Antisymmetry: `R^ρ_σμν = −R^ρ_σνμ` to machine precision (structural check on lowered tensor).
4. **Pinning vitest (M7)**: one-line pinned closed-form assertion: `R^t_rtr(r=6M, M=M☉) === 2GM☉/(6M☉)³` to ≤1e-10 relative. Cross-checks the corrected Riemann formula (S3) at a concrete coordinate — catches index-permutation regressions.

**Dependencies**: none (uses v0.4.0 Christoffel infrastructure unchanged).

**Honest framing**: this is a 4-index tensor with 256 entries (4⁴). Lowering performance is bounded by 256 × (Christoffel-eval + Γ-derivative-eval) operations per point. Adequate for fixture-based testing; **not yet** optimized for grid-field curvature (which is what v0.6.0+ Friedmann/Raychaudhuri evaluation would need).

#### Task 1d — `ricci()` helper

**Files**:
- Modify: `src/dimensional/connection.ts` (add `ricci()` next to `christoffel()`) — OR create `src/dimensional/curvature.ts` for the three curvature helpers grouped together. **Decision**: new file `src/dimensional/curvature.ts` (separation of layers — connection vs curvature).
- Test: `tests/dimensional/ricci.test.ts`.

**Key signature** (M2: simplified — no explicit contraction-label parameters):
```typescript
export function ricci(R: RiemannTensorNode): ExprNode;
```

Builds the trace `R_μν = R^λ_μλν` (Carroll convention: contract upper `λ` against the **first lower** slot — the `μ` position — not the second lower slot). This is the corrected Ricci contraction (S2 fix). Internally uses the existing v0.2.0 `contract()` machinery. Symbolic, returns an `ExprNode`; numerical lowering happens via the existing pipeline.

**S2 correction note**: the previous draft wrote `R_σν = R^ρ_σρν` — this contracts upper `ρ` against the **second** lower slot (the `σ↔ρ` pair). Carroll's convention is `R_μν = R^λ_μλν` — contract upper `λ` against the **first** lower slot (the `μ` position). The difference is a permutation of `σ` and `μ` in the contracted pair; using the wrong slot produces the wrong Ricci tensor for non-symmetric Riemann components.

**Test approach**:
1. Output free-indices: `{μ: lower, ν: lower}`, dim `{ L: -2 }`.
2. Schwarzschild vacuum: `R_μν = 0` to ≤1e-12 (Ricci-flat is the *defining* property of vacuum solutions — this is **the** key curvature test).
3. de Sitter (constant-curvature) fixture: `R_μν = (Λ/3)·g_μν` for the de Sitter solution → trace `g^μν R_μν = R = 4Λ` (this re-uses BE-13's machinery, see §3 Phase 3).

**Dependencies**: 1c (RiemannTensorNode).

**Honest framing**: helper, not a node. The output AST is *bigger* than the input (it expands Riemann into a contracted tree). v0.6.0+ may add an `isRicciFlatVacuum` validator-level optimization that short-circuits the lowering for known vacuum metrics; v0.5.0 lowers everything generically.

#### Task 1e — `einstein()` helper

**Files**:
- Modify: `src/dimensional/curvature.ts` (add `einstein()` next to `ricci()`).
- Test: `tests/dimensional/einstein.test.ts`.

**Key signature**:
```typescript
export function einstein(
  R: RiemannTensorNode,
  g: MetricTensorNode,
  gInverse: MetricTensorNode,
): ExprNode;
```

Builds `G_μν = R_μν − ½ g_μν R` where `R = g^σν R_σν` (Ricci scalar). Uses `ricci()` internally + a fresh metric-trace contraction + scalar-multiply + tensor-subtract (all existing v0.2.0 ops). Output free-indices: `{μ: lower, ν: lower}`.

**Test approach**:
1. Schwarzschild vacuum: `G_μν = 0` to ≤1e-11 (since both `R_μν = 0` AND `R = 0`; same numerical fixture as 1d).
2. de Sitter: `G_μν + Λ g_μν = 0` to ≤1e-11 (matches BE-13's `G_μν = κ T_μν` with `T_μν = −(Λ/κ) g_μν` for cosmological-constant matter). This connects v0.5.0's curvature layer to v0.6.0+'s Einstein-field-equation work without committing to it.
3. Trace: `g^μν G_μν = −R` to machine precision (standard identity).

**Dependencies**: 1c (RiemannTensorNode), 1d (ricci).

**Honest framing**: `einstein()` returns `G_μν`. **Not** `G_μν − κ T_μν = 0` (no matter sources). Vacuum-Einstein-field-equation testing (G_μν = 0 for Schwarzschild) IS in scope; matter-coupled `G_μν = κ T_μν` is **v0.6.0+ work** (see §4 Honest Framing).

#### Task 1f — `bianchiResidual()` helper

**Files**:
- Modify: `src/dimensional/curvature.ts` (add `bianchiResidual()`).
- Test: `tests/dimensional/bianchi-residual.test.ts`.

**Key signature** (M4: return full tensor residual so callers can reduce as needed):
```typescript
export function bianchiResidual(
  R: RiemannTensorNode,
): {
  /** Symbolic residual tree — an ExprNode whose value is zero when the identity holds. */
  residual: ExprNode;
  /** Numerical full 5-index residual tensor R_λμνρσ_residual (callers reduce as needed). */
  evaluate: (engine: TensorEngine, inputs: NumericalInputs) => Promise<EngineTensor>;
  /** Convenience: max |residual_λμνρσ| across all free-index assignments. */
  evaluateMax: (engine: TensorEngine, inputs: NumericalInputs) => Promise<number>;
};
```

Returns the **cyclic-symmetry second Bianchi identity** form (all-lower `R_μνρσ` convention):
```
∇_λ R_μνρσ + ∇_μ R_νλρσ + ∇_ν R_λμρσ = 0   (identically, in any geometry)
```
(See §5.2 for choice rationale.)

**I7 — lowering step**: `RiemannTensorNode` is mixed (1 upper + 3 lower: `R^ρ_σμν`). The all-lower `R_μνρσ` required by the Bianchi identity is obtained by first lowering the upper `ρ` index via the metric: `R_λμνρ = g_λρ R^ρ_μνσ` using v0.3.0's `lower()` helper. This lowering step is applied inside `bianchiResidual` before constructing the `∇ + cyclic sum` expression tree. Callers pass `R: RiemannTensorNode` and do not need to pre-lower.

`residual` is the symbolic LHS as an `ExprNode`; `evaluate` returns the full 5-index residual tensor; `evaluateMax` is a convenience reduction over it.

**Test approach**:
1. Schwarzschild: `evaluateMax(...)` ≤ 1e-9 (identity holds; bounded by Christoffel-derivative numerical error). Honest framing: this is a sanity test that our curvature lowering is *self-consistent*, not a new physics claim.
2. de Sitter: same — residual ≤ 1e-9.
3. Synthetic broken-Christoffel injection: deliberately corrupt one Christoffel component → residual jumps to O(1). Confirms the test is not vacuously passing.

**Dependencies**: 1c (RiemannTensorNode), v0.4.0 CovariantDerivativeNode (for `∇_λ R`).

**Honest framing**: this is a **consistency check**, not new physics. Bianchi is a mathematical identity that *any* connection satisfies in 4d; we test it to confirm our implementations of Riemann + covariant-derivative are mutually consistent.

---

### Phase 2 — Activations (~3–5 tasks)

#### Task 2a — Activate BE-52 Mercury perihelion `it.skip`

**Files**:
- Modify: `tests/bridges/perihelion-precession.test.ts` — flip `it.skip` at line 96 to `it`; replace v0.4.0 RK4 with GL4 on canonical (x, p) + polynomial-interpolant bisection.
- Possibly modify: `src/bridges/equations/be-52-perihelion-precession.ts` to export a `evaluatePerihelionGeodesic({M_kg, a_m, e, T_yr, orbits})` helper that wires GL4 + bisection (or test does it inline — TBD at plan time, weighted toward "test-local for now, public-API in v0.6.0").

**Test approach**:
- Initial state: Mercury at perihelion of Schwarzschild orbit with `a = 5.79e10 m`, `e = 0.206`, central mass `M_sun`.
- Integrate via GL4 on canonical `(x, p)` state for ~2 orbits at ~50k steps.
- Bisect perihelion at orbit start (τ ≈ 0) and orbit end (τ ≈ T) using polynomial-interpolant bisection from 1b.
- `Δφ_measured = φ(τ_end) − φ(τ_start) − 2π`.
- Compare against closed form `6πGM/(a(1−e²)c²)` to **±5×10⁻⁴ relative** (I4 tolerance reconciliation: Δφ_2orbit ≈ 2.1×10⁻⁴ rad; a ±1×10⁻⁴ relative tolerance would require Δφ accuracy of 2.1×10⁻⁸ rad ≈ 4 mas, below double-precision cross-validation reach at this step count; ±5×10⁻⁴ relative is the honest achievable target).

**Dependencies**: 1a (GL4), 1b (bisection).

**Honest framing**: ±5×10⁻⁴ relative is the v0.5.0 target. The v0.4.0 design originally promised ±1e-9 for the deferred test; the v0.4.0 reconciliation softened that to ±1e-4 (per `v0.4.0-Design.md` §8 "I6 reconciliation" line 230). Adam+Eve adversarial review (I4) showed that ±1×10⁻⁴ relative on a 2-orbit Δφ ≈ 2.1×10⁻⁴ rad corresponds to an absolute precision of 2.1×10⁻⁸ rad (≈4 mas) — achievable only with 5×10⁵+ steps, which is too slow for CI without the LONG-flag infrastructure. ±5×10⁻⁴ is the conservative, honest target at 50k steps. Pushing past that would need (i) step count increase to 5×10⁵+ with CI guard, (ii) adaptive step-size, or (iii) higher-order GL, all of which are **v0.6.0+**.

#### Task 2b — Activate BE-37 full Shapiro `it.skip`

**Files**:
- Modify: `tests/dimensional/covariant-derivative-preview.test.ts` — flip `it.skip` at line 68 to `it`.

**Test approach**:
- Solar mass + Earth-Mars Shapiro geometry (R_far ≈ Earth–Sun, R_near ≈ Mars–Sun at conjunction).
- Integrate null geodesic via GL4 on canonical `(x, p)` state — extract the Shapiro time delay from coordinate-time-vs-affine-parameter accumulation.
- Compare against `evaluateShapiroDelay({M_kg, R_far_m, R_near_m})` closed form to **±1×10⁻⁴ relative** (I5 tolerance reconciliation: the Earth-Mars Shapiro absolute delay is ≈2×10⁻¹⁰ s; ±1e-6 relative = 2×10⁻¹⁶ s, which is below double-precision floor; ±1×10⁻⁴ relative = 2×10⁻¹⁴ s is the honest achievable target).

**Dependencies**: 1a (GL4), 2c (the non-zero `shapiroDelaySec` field — see below).

**Honest framing**: this is a *numerical-numerical* cross-validation and is **best-effort numeric** pending 1a and 2b correctness verification (M6). The v0.3.5 RK4 path already passes to 7.27e-10 relative against the closed form (per `project_upt.md` line 60); v0.5.0's GL4 + covariant-eikonal path targets ±1×10⁻⁴ relative — looser because the absolute Shapiro delay for Earth-Mars geometry (≈2×10⁻¹⁰ s) is near double-precision floor at tighter tolerances. The headline win is *not* precision improvement — it's that the covariant-eikonal path produces a non-zero number at all. Once 1a and 2c are verified correct, this framing can be upgraded to "validated"; until then, mark as "best-effort numeric" in code comments.

#### Task 2c — `evaluateBE37CovariantEikonalNumerical` actually computes `shapiroDelaySec`

**Files**:
- Modify: `src/numerical/be37-covariant-eikonal.ts` — replace the stub returning `0` with a real GL4-driven Shapiro integration on canonical (x, p) state.

**Current state (v0.4.6, per `project_upt.md` line 30)**:
> evaluateBE37CovariantEikonalNumerical ships the structural form g^μν ∇_μ ∇_ν S = 0 with eikonal residual=0 by construction (null wave-covector). … Full geodesic-integrated Shapiro cross-check is deferred to v0.5.0.

**v0.5.0 change**: the function now invokes `integrateGeodesicGL4` with null initial conditions on canonical `(x, p)` state, integrates from R_far to R_near and back, accumulates the coordinate-time-vs-flat-space-time delta, returns it as `shapiroDelaySec`. JSDoc updated to reflect this (no more "intentional stub returning literal zeros").

**Test approach**: covered by 2b (which calls this function).

**Dependencies**: 1a (GL4).

**Honest framing**: this changes a documented stub to a real implementation. The v0.4.6 JSDoc lie ("returns 0") becomes truth ("returns Shapiro delay"). Honest because we documented it as deferred at v0.4.0 ship time.

---

### Phase 3 — Bridge validation sweep (~7–8 tasks)

Driven by `docs/architecture/bridge-coverage-audit.md` §"Recommended next steps" (lines 127–135).

#### Task 3a — BE-51 structural sibling test

**Files**: Create `tests/bridges/be-51-gravitational-lensing-structural.test.ts`.

Current state per audit line 68: BE-51 has NUMERICAL coverage only (no separate AST/structural test). v0.5.0 adds a sibling structural file: asserts `formula_latex` regex, `dimensional_signature` round-trip, `status` pin, `tensor-product` / metric-layer AST kinds present. Mirrors BE-37's split (`be-37-shapiro-eikonal-structural.test.ts`).

**Dependencies**: none.

#### Task 3b — BE-52 structural sibling test

**Files**: Create `tests/bridges/be-52-perihelion-precession-structural.test.ts`.

Same pattern as 3a, mirrored on BE-52.

**Dependencies**: none.

#### Tasks 3c–3g — Physics-anchor tightening for catalog-only bridges

Per audit §"Recommended next steps":

- **3c — BE-17 broader numerical**: add `evaluateBE17SpinDensitySquared` test against textbook spin-polarized-neutron-star torsion density (audit recommendation #2).
- **3d — BE-20 ρ_Λ tightening**: tighten 2×-wide range to ±5% around Planck 2018 `5.96e-27 kg/m³` (audit recommendation #3).
- **3e — BE-45 physics anchor**: add the Bedroya-Vafa N_e < 137 Solar-System bound (audit recommendation #4).
- **3f — BE-46 physics anchor**: add Weinberg 1987 Λ_obs window probability (audit recommendation #4).
- **3g — BE-50 physics anchor**: add Cramer 1986 retrocausality bound (audit recommendation #4).

Each is a single new `expect(...)` block in the corresponding test file. Per-task: ~30 LoC test, no source changes, dependencies = none.

**Honest framing**: these tighten validation. They do NOT change evaluator behavior. If any expectation fails, that is a real new physics bug surfaced by tighter anchors — NOT a v0.5.0 regression.

#### Task 3h — Catalog-level integrity test

**Files**: Create `tests/bridges/catalog-integrity.test.ts`.

Asserts: 42 bridges present; every bridge has a numerical-evaluator test; every Schwarzschild-spacetime bridge (BE-37, BE-42, BE-51, BE-52) has a geodesic-cross-validation test. Per audit recommendation #5.

**Honest framing**: this is a *suite-level invariant*, not a single physics test. Codifies the v0.5.0 commitment that the bridge catalog has uniform coverage — future bridges must pass this gate.

**Dependencies**: 2a, 2b (Schwarzschild-spacetime geodesic-cross-validation requirement).

---

### Phase 4 — Release (3 tasks)

#### Task 4a — Version bump + CHANGELOG

`package.json` "0.4.6" → "0.5.0". `CHANGELOG.md` `[Unreleased]` → `[0.5.0]` with the four-phase summary.

#### Task 4b — Full suite + tsc gate

`npm run build` (tsc clean) + `npx vitest run` (full suite green). Target: ~1,485–1,520 passing (current v0.4.6 baseline 1,443 + ~40–80 new tests from Phase 1–3).

#### Task 4c — Tag → push → npm publish

```bash
git tag -a v0.5.0 -m "release: v0.5.0 — GR foundations (GL4 symplectic + curvature + bridge validation sweep)"
git push origin master --tags
npm publish --ignore-scripts   # Windows lesson: prepublish scripts hang on remote SSH
```

---

## 4. Honest framing

What v0.5.0 IS:
- The GR foundations release: symplectic integrator, bisection perihelion finder, curvature layer (Riemann + Ricci + Einstein + Bianchi).
- Activation of two v0.4.0 `it.skip` debts (BE-52 Mercury, BE-37 full Shapiro).
- Tightening of bridge-validation precision per the pre-flight audit.

What v0.5.0 is **NOT**:
- **Not a performance release.** Gauss-Legendre 4th-order is more expensive per step than RK4: 2 stages × ~20 Newton iterations each, versus 4 explicit stages for RK4. The win is *correct symplectic-form preservation for the non-separable geodesic Hamiltonian*, not speed. Mercury perihelion at 50k GL4 steps is slower than 50k RK4 steps; we pay that cost knowingly. Ruth-4 would have been cheaper but is NOT symplectic for the geodesic Hamiltonian (g^μν(x) mixes x and p — the Hamiltonian is non-separable).
- **Not a tensor-algebra refactor.** The existing v0.4.0 lowering pipeline is reused unchanged. RiemannTensorNode lowering walks the node directly (calling Christoffel + Γ-derivative at points) rather than AST-rewriting Riemann into thousands of pderiv nodes. Tree size stays bounded.
- **Not yet Einstein-field-equation work.** `einstein()` returns `G_μν`. Vacuum testing (`G_μν = 0` for Schwarzschild) IS in scope. **Matter-coupled** `G_μν = κ T_μν` requires a stress-energy AST kind (`stress-energy-tensor`?), matter Lagrangian primitives, and is **v0.6.0+ work**.
- **Not yet Friedmann/Raychaudhuri/cosmology bridges.** Those depend on `einstein()` + matter sources + cosmological metrics. v0.5.0 ships the LHS (`G_μν`); v0.6.0+ ships the RHS (`κ T_μν`) and writes the bridges.
- **Not yet threejs visualization.** The GL4 `GL4Snapshot` shape mirrors the v0.4.0 RK4 `GeodesicSnapshot` (τ + coordinates); future viz can consume both shapes without API churn. The canonical-p state is internal to the integrator; output snapshots expose x and optional v.
- **Not yet higher-order AD.** Riemann involves `∂Γ`; we evaluate `∂Γ` via `pderiv` finite-difference, NOT via forward-over-forward AD composition (which v0.4.0's interface supports structurally per §14.4). v0.5.0 chose finite-difference because Christoffel finite-differencing IS already the v0.4.0 'computed' strategy path; we reuse it without introducing nested-AD complexity. AD-on-curvature is a v0.6.0+ optimization.

---

## 5. Math conventions

### 5.1 Riemann index convention (Carroll Ch. 3)

`R^ρ_σμν` — 1 upper + 3 lower. Definition (S3 corrected; Carroll §3.4):
```
R^ρ_σμν = ∂_μ Γ^ρ_σν − ∂_ν Γ^ρ_σμ + Γ^ρ_λμ Γ^λ_σν − Γ^ρ_λν Γ^λ_σμ
```
Index-placement key: `σ` is the **second** lower slot in every Γ factor; `μ, ν` appear as the derivative/antisymmetry pair on the outer Γ slots (`σν`, `σμ`). The previous draft had `σ` and `ν` swapped in the Γ terms, which is incorrect per Carroll.

**Antisymmetries** (validator-asserted):
- `R^ρ_σμν = −R^ρ_σνμ` (last two lower indices)
- After lowering `ρ`: `R_ρσμν = −R_σρμν` (first two indices); `R_ρσμν = R_μνρσ` (pair-swap).

**Contractions** (used by `ricci`/`einstein`):
- Ricci: `R_μν = R^λ_μλν` (S2 corrected: trace on upper `λ` ↔ **first lower** slot, the `μ` position). Carroll convention; previous draft incorrectly used the second lower slot.
- Ricci scalar: `R = g^μν R_μν`.

### 5.2 Bianchi identity form (cyclic vs alternative)

**Chosen form** (cyclic on first three lower indices of `∇R`, all-lower convention):
```
∇_λ R_μνρσ + ∇_μ R_νλρσ + ∇_ν R_λμρσ = 0
```
This is the standard second Bianchi (per Carroll Eq 3.95). Alternative forms (e.g., `∇_λ R^ρ_σμν + ...` with one upper index, or the *first* Bianchi `R^ρ_[σμν] = 0` on the index ordering) are mathematically equivalent but bury the symmetry. Cyclic-all-lower is the textbook "look at me, I'm cyclic" form most readers recognize. For Schwarzschild we expect residual ≤ 1e-9 (bounded by Christoffel-pderiv numerical noise).

### 5.3 Gauss-Legendre 4th-order coefficients (2-stage GL; Hairer/Lubich/Wanner §II.1)

The 2-stage Gauss-Legendre method has quadrature nodes at the roots of the shifted Legendre polynomial `P₂(2t−1) = 0`:
```
c₁ = ½ − √3/6   ≈  0.2113248654051871
c₂ = ½ + √3/6   ≈  0.7886751345948129
```

Butcher tableau `a_{ij}`:
```
a₁₁ = ¼               ≈  0.25
a₁₂ = ¼ − √3/6        ≈  0.0669872981077807
a₂₁ = ¼ + √3/6        ≈  0.4330127018922193
a₂₂ = ¼               ≈  0.25

b₁ = b₂ = ½           (quadrature weights)
```

**Symplecticity condition** (Sanz-Serna 1988): verify `b_i a_{ij} + b_j a_{ji} = b_i b_j` for all `i, j`:
- `i=j=1`: `b₁ a₁₁ + b₁ a₁₁ = 2·½·¼ = ¼ = b₁ b₁ = ¼` ✓
- `i=1, j=2`: `b₁ a₁₂ + b₂ a₂₁ = ½·(¼−√3/6) + ½·(¼+√3/6) = ½·½ = ¼ = b₁ b₂` ✓
- `i=j=2`: by symmetry, same as `i=j=1` ✓

**Order conditions**: 4th-order accuracy verified by the standard order-conditions tree for Runge-Kutta; `Σ b_i = 1` ✓.

Test asserts numerical Butcher-tableau entries to ≤1e-15 against the above closed-form values.

**State variables**: `(x^μ, p_μ)` canonical pair where `p_μ = g_μν(x) v^ν` is covariant momentum. At integrator input, convert from 4-velocity `v^ν` via Legendre transform. At output, optionally recover `v^ν = g^μν p_μ`.

*(Yoshida 1990 Ruth-4 coefficients removed; Ruth-4 is NOT symplectic for the non-separable geodesic Hamiltonian — see §2 decision #2 rationale.)*

### 5.4 SI-units sanity (Schwarzschild gotcha v0.4.0 caught)

`g_tt = −(1 − r_s/r) c²` in SI coordinates. Christoffel `Γ^r_tt` carries an extra `c²` factor versus the c=1 textbook form. v0.4.0 tests confirmed this (per `project_upt.md` line 29). v0.5.0 inherits the SI-aware Christoffel; Riemann and Ricci inherit the units automatically (no extra `c²` factor needed at the curvature level — units flow through). Validator does NOT enforce SI vs geometric units; tests use SI consistently.

---

## 6. Test infrastructure

### Schwarzschild fixture re-use

`tests/fixtures/schwarzschild.ts` (already exists, used by v0.4.0) provides:
- Metric function `g(x): 4×4`
- Inverse metric `gInv(x): 4×4`
- Christoffel function `christoffelFn(x): 4×4×4`
- Schwarzschild radius computation
- Domain restriction `r ≥ 3·r_s`

v0.5.0 **adds**:
- Riemann function `riemannFn(x): 4×4×4×4` (analytic closed-form for Schwarzschild — used as a fixture-vs-numerical regression test in Task 1c).
- Ricci function `ricciFn(x): 4×4` (returns the zero tensor for vacuum — used as the assertion target in Task 1d).
- Bench/tests split: `bench/fixtures/schwarzschild.ts` keeps the *minimal* runtime fixture for benchmark hot loops; `tests/fixtures/schwarzschild.ts` is the *full* fixture with the analytic curvature tensors. Split is consistent with v0.4.6's pattern (per memory: "bench/fixtures/schwarzschild.ts vs tests/fixtures/schwarzschild.ts").

### Cycloid agreement targets

- v0.4.0 RK4: cycloid → 2.1e-16 relative (machine epsilon).
- v0.5.0 GL4: cycloid → ≤ 1e-13 relative (one order looser, reflecting GL4's per-step accuracy vs RK4's explicit stepping; GL4's advantage is symplecticity for long integrations, not per-step accuracy).

### Vacuum-Ricci-zero checks

- 1d test: `ricci(Schwarzschild) → 0` to ≤1e-12 per component.
- 1e test: `einstein(Schwarzschild) → 0` to ≤1e-11 per component.
- These are the **single strongest curvature-correctness assertions** in the suite — they encode that Schwarzschild IS a vacuum solution.

### de Sitter fixture (new)

`tests/fixtures/de-sitter.ts` (new file): constant-curvature metric for testing the non-zero-Ricci path. Used by 1d (Ricci scalar `R = 4Λ` matches BE-13) and 1e (Einstein tensor `G_μν = −Λ g_μν`).

---

## 7. Risk register

| # | Risk | Severity | Mitigation |
|---|---|---|---|
| R1 | **GL4 implicit iteration cost per step.** Mercury 2-orbit integration at 50k steps with Newton iteration may be slow on CI (2–5 minutes single run, higher than Ruth-4 would have been). | Medium | Restrict full-orbit tests to a single `describe` block that runs only when `GL4_LONG=1` env var set; default CI runs 1-orbit smoke test only. (Mirrors v0.4.0's perihelion-precession-deferred-cost-budget pattern.) Add R8 to register (Newton convergence risk; see below). |
| R2 | **Bisection precision floor.** `dr/dτ = g^{rν} p_ν ≈ 0` over a wide span near perihelion makes bracketing slow; double-precision floor on `dr/dτ` may be ~1e-14 of orbital speed, capping bisection precision. | Medium | Use polynomial interpolation (cubic Hermite) on cached snapshot data to avoid full re-integrations per bracket. Target 1e-10 × T_orbit. Δφ target ±5×10⁻⁴ relative is unaffected — bisection precision ≪ Δφ tolerance. |
| R3 | **Curvature lowering performance.** RiemannTensorNode lowering at a single point = 256 entries × (Γ-eval + ∂Γ-eval). Bianchi residual = additional `∇` layer → 1024 effective evaluations. May be slow. | Low | Acceptable for fixture-based testing. v0.6.0+ will need optimization for grid-field curvature (Friedmann/Raychaudhuri). v0.5.0 honest framing: "scales linearly in number of test points; not optimized for arrays." |
| R4 | **Bianchi residual lower bound from finite-difference noise.** `pderiv` introduces ~1e-9 relative noise per derivative; nested `∇` may compound to 1e-7 — masking real errors. | Medium | Use centered finite differences (already v0.4.0 default); document floor of 1e-9 in residual test. If 1e-9 proves too tight, relax to 1e-8 and document. |
| R5 | **AST size blow-up for curvature.** `einstein()` builds a composite tree including 2× `ricci()` (one for `R_μν`, one for trace `R`) which each contain 4× contractions of `RiemannTensorNode`. Pre-lowering tree size may grow to 100s of nodes. | Low | RiemannTensorNode lowering does NOT AST-rewrite into pderiv-of-Γ — it lowers directly. Tree size bounded by the helper composition (10s of nodes, not 1000s). v0.6.0+ may CSE-share the `R_μν` subtree between `ricci` and `einstein`. |
| R6 | **Phase ordering risk.** Phase 2c (the non-zero `shapiroDelaySec`) depends on 1a; if 1a slips, 2c slips, then 2b is blocked. | Low | Phase 2 is small; if 1a is late, 2b/2c slip to v0.5.1 — Phase 1 + Phase 3 still ship. (Graceful-degradation precedent from v0.4.0 §17.) |
| R7 | **`it.skip` re-skip temptation.** If 2a's Δφ doesn't reach ±5×10⁻⁴ relative, the temptation is to re-skip and defer to v0.6.0. | Medium | Explicit project-rule: 2a may NOT re-skip. If precision misses ±5×10⁻⁴ relative, the implementation is the bug — fix GL4 step count, bisection refinement, or initial conditions. |
| R8 | **GL4 Newton convergence failure.** For very large step sizes or near-horizon orbits, the Newton iteration for GL4 stage values may fail to converge within `newtonMaxIter=50`. | Medium | Use `h_max = T_orbit / 1000` as the maximum step size in practice (verified to converge for Mercury scale in Schwarzschild). If convergence fails at any step, halve `h` and re-attempt (one retry). Log a warning and short-circuit the test if convergence fails on retry — this signals a physical pathology (near-horizon), not a GL4 defect. |

---

## 8. Spec coverage

| § Requirement | Implementing task | Notes |
|---|---|---|
| Maximal scope (decision #1) | All of Phases 1–4 | |
| Gauss-Legendre 4th-order, canonical (x,p) state (decision #2) | Task 1a | GL4 Butcher tableau §5.3; Legendre transform at integrator boundary |
| Mixed curvature API (decision #3) | Tasks 1c (node) + 1d, 1e, 1f (helpers) | |
| Bottom-up sequencing (decision #4) | Phase 1 → 2 → 3 → 4 | Phase 3 mostly independent; Phase 4 last |
| BE-52 Mercury activation | Task 2a | Per `it.skip` line 96, `perihelion-precession.test.ts` |
| BE-37 full Shapiro activation | Tasks 2b + 2c | Per `it.skip` line 68, `covariant-derivative-preview.test.ts` |
| Bridge-coverage-audit follow-through | Tasks 3a–3h | See audit §"Recommended next steps" lines 127–135 |
| Version bump | Task 4a | |
| Tag + publish | Task 4c | `--ignore-scripts` per Windows lesson |
| Honest framing | §4 | Phrased as "not yet" for everything deferred |
| Math conventions locked | §5 | Carroll Ch. 3 + GL4 Butcher tableau (Hairer/Lubich/Wanner §II.1, Sanz-Serna 1988) + SI-units gotcha |
| Test infrastructure | §6 | Schwarzschild fixture extension + de Sitter fixture |
| Risk register | §7 | 7 risks; all mitigations documented |

---

## 9. Self-review section

**Self-review checklist** (post-Adam/Eve adversarial reconciliation 2026-05-18):

- [x] Does §3 detail each task's files, signatures, tests, and dependencies? YES — all tasks have files/signatures/tests/dependencies.
- [x] Are decisions traced to rationale or marked as user-locked? YES — GL4 choice, (x,p) state, Ricci correction, Riemann formula all traced to sources.
- [x] Does §4 honest framing flag every "not yet" feature? YES — updated to reflect GL4 cost model and tolerance relaxations.
- [x] Are §5 conventions cited to published sources (Carroll, Hairer/Lubich/Wanner, Sanz-Serna)? YES — updated §5.3 cites GL4 sources.
- [x] Does §7 risk register include mitigations, not just risks? YES — R8 added for Newton convergence.
- [x] Does §8 spec coverage table cover ALL design requirements? YES — updated for GL4.
- [x] Is the integrator truly symplectic for the geodesic Hamiltonian? YES — GL4 verified via Sanz-Serna symplecticity condition; Ruth-4 rejected because H = ½ g^μν(x) p_μ p_ν is non-separable.
- [x] Are Riemann and Ricci index conventions consistent with Carroll? YES — S2 (Ricci first-lower contraction) and S3 (Riemann Γ index placement) both corrected.
- [x] Are tolerances achievable in double precision? YES — Mercury ±5×10⁻⁴ relative and Shapiro ±1×10⁻⁴ relative both above double-precision floor.

---

## 10. Cross-validation record (pre-dispatch)

Adam (Gemini 2.5 Pro) + Eve (OpenAI o3) adversarial review **dispatched immediately after design save**. Findings will be folded into a v0.5.0-Review-Findings.md companion doc + v0.5.0-Design.md inline reconciliation post-review (mirroring v0.4.0 pattern).

**Reviewer focus areas (priority order):**
1. Algorithm correctness (GL4 Butcher tableau + tolerance claims). [Adam+Eve adversarial review complete — findings reconciled; see §11.]
2. Riemann index convention.
3. Bisection precision floor.
4. Bianchi identity form choice.
5. Phase dependency graph.
6. Honest framing accuracy (especially 2c stub→real claim).
7. Scope creep risk (anything that's actually v0.6.0+ work).
8. Test infrastructure (fixture re-use consistency with v0.4.6).
9. Anything else that would cause planning or execution to fail.

**Decision log (locked at brainstorming session 2026-05-18; updated post-Adam/Eve reconciliation 2026-05-18):**

| # | Decision | Chosen | Source |
|---|---|---|---|
| 1 | Scope | Maximal (all three blocks) | User pick |
| 2 | Symplectic algorithm | **Gauss-Legendre 4th-order** (2-stage implicit RK, symmetric Butcher tableau) | User lock post-Adam/Eve: Ruth-4 rejected — NOT symplectic for non-separable geodesic H = ½ g^μν(x) p_μ p_ν |
| 3 | State variables | **Canonical (x, p)** with Legendre transform at integrator boundary | User lock post-Adam/Eve: kicking v (not p) breaks symplecticity regardless of integrator |
| 4 | Curvature API | Mixed (RiemannTensorNode + ricci/einstein/bianchi helpers) | User pick (mirrors v0.4.0 ∇_μ-node + christoffel-helper precedent) |
| 5 | Sequencing | Bottom-up (Foundations → Activations → Validation → Release) | User pick (dependencies force this) |
| 6 | Riemann index convention | `R^ρ_σμν` = 1 upper + 3 lower (Carroll Ch. 3); Γ placement corrected (S3) | Convention pick + adversarial correction |
| 7 | Ricci contraction | `R_μν = R^λ_μλν` — upper λ vs FIRST lower (Carroll); corrected from SECOND lower (S2) | Adversarial correction |
| 8 | Riemann units | `dim: { L: -2 }` — not DIMENSIONLESS (I3) | Adversarial correction |
| 9 | Bianchi form | Cyclic all-lower (Carroll Eq 3.95); lower upper-ρ via metric before applying ∇ (I7) | Convention pick + adversarial correction |
| 10 | Mercury tolerance | ±5×10⁻⁴ relative (I4: tighter than ±1e-4 from v0.4.0, but ±1×10⁻⁴ relative is below CI reach) | Adversarial relaxation |
| 11 | Shapiro tolerance | ±1×10⁻⁴ relative (I5: ±1e-6 relative is below double-precision floor) | Adversarial relaxation |
| 12 | Bisection cost | Polynomial interpolation on snapshot cache, not full re-integration per bracket (I6) | Adversarial improvement |
| 13 | ricci() signature | `ricci(R: ExprNode): ExprNode` — no explicit contraction-label params (M2) | Adversarial simplification |
| 14 | bianchiResidual return | Full 5-index tensor + evaluateMax convenience; callers reduce (M4) | Adversarial improvement |
| 15 | Vacuum-Einstein scope | In scope (G_μν = 0 for Schwarzschild) | Honest framing — testable today |
| 16 | Matter-coupled Einstein | Out of scope (v0.6.0+) | Honest framing — needs T_μν AST |
| 17 | Higher-order AD for ∂Γ | Out of scope (reuse v0.4.0 finite-difference) | Honest framing — premature optimization |
| 18 | Friedmann/Raychaudhuri/cosmology bridges | Out of scope (v0.6.0+) | Honest framing — needs matter-coupled Einstein |
| 19 | threejs viz | Out of scope (still) | Roadmap — v0.6.0+ |

---

## 11. Adversarial Review — Findings & Reconciliation

**Review date:** 2026-05-18
**Reviewers:** Adam (Gemini 2.5 Pro), Eve (OpenAI o3)
**Total findings:** 19 (4 showstoppers, 9 important, 6 minor)

| F# | Severity | Source | Finding | Fix applied / Rejection |
|---|---|---|---|---|
| F1 | SHOWSTOPPER | Eve #1, Eve #2 | **Symplectic integrator wrong for non-separable H.** Ruth-4 (Yoshida drift-kick composition) is symplectic ONLY for separable Hamiltonians H = T(p) + V(q). The geodesic Hamiltonian H = ½ g^μν(x) p_μ p_ν is NOT separable because g^μν depends on x, coupling position and momentum. Ruth-4 has secular non-symplectic error for this system. | **FIXED (S1):** Replace Ruth-4 with Gauss-Legendre 4th-order (2-stage implicit RK). GL4 is symplectic for ANY smooth Hamiltonian via its symmetric Butcher tableau (Sanz-Serna 1988 condition `b_i a_{ij} + b_j a_{ji} = b_i b_j` verified). §3 Task 1a, §5.3 fully rewritten. |
| F2 | SHOWSTOPPER | Eve #1 (state vars) | **State variables (x, v) break symplecticity.** Kicking the coordinate velocity v instead of the canonical momentum p destroys the symplectic form regardless of which integrator is used. | **FIXED (S1 / Daniel decision):** State variables changed to canonical (x, p) throughout. Legendre transform p_μ = g_μν(x) v^ν at integrator construction; v^ν = g^μν p_μ at output. §3 Task 1a, §5.3, §10 decision log updated. |
| F3 | SHOWSTOPPER | Adam #1, Eve #7 | **Ricci contraction wrong slot.** Design said `R_σν = R^ρ_σρν` (contract upper ρ vs SECOND lower slot). Carroll convention is `R_μν = R^λ_μλν` (contract upper λ vs FIRST lower slot). These differ by a permutation of σ and ρ, producing the wrong Ricci tensor. | **FIXED (S2):** §5.1 Ricci contraction corrected to `R_μν = R^λ_μλν`. Task 1d signature simplified to `ricci(R): ExprNode` with Carroll convention internal (M2 subsumed). §3 Task 1d, §5.1 updated. |
| F4 | SHOWSTOPPER | Eve #5 | **Riemann formula has Γ indices permuted.** Previous draft: `R^ρ_σμν = ∂_μ Γ^ρ_νσ − ...` (σ and ν swapped in Γ factors). Carroll Ch. 3 gives: `R^ρ_σμν = ∂_μ Γ^ρ_σν − ∂_ν Γ^ρ_σμ + Γ^ρ_λμ Γ^λ_σν − Γ^ρ_λν Γ^λ_σμ`. | **FIXED (S3):** §5.1 Riemann definition corrected with σ in SECOND lower slot of every Γ factor. §3 Task 1c updated with correction note and pinning vitest (M7 subsumed). |
| F5 | N/A | — | S4: subsumed by F1/F2 (same root cause — incorrect integrator for non-separable H). | N/A |
| F6 | IMPORTANT | Eve #3 | **Energy-drift tolerance ≤1e-10 over 100 orbits.** With true symplectic GL4, this IS achievable (no secular energy drift). Previous concern was that Ruth-4's non-symplectic secular drift would violate this. | **KEPT (I1):** Tolerance ≤1e-10 retained in Task 1a test spec. No relaxation needed since GL4 is genuinely symplectic for the geodesic Hamiltonian. |
| F7 | IMPORTANT | Eve #4 | **Yoshida d₄=0 issue (Ruth-4 coefficient anomaly).** d₄=0 creates a degenerate drift-kick pattern for non-separable H. | **N/A (I2):** Moot — Ruth-4 replaced by GL4 which has a different two-stage scheme. §5.3 rewritten with GL4 Butcher tableau. |
| F8 | IMPORTANT | Eve #6 | **Riemann units are 1/L², not DIMENSIONLESS.** Riemann appears in the geodesic deviation equation `D²ξ^μ/dτ² = R^μ_νρσ u^ν ξ^ρ u^σ` — with u having units m/s and ξ in meters, R must have units 1/m². | **FIXED (I3):** `dim` set to `{ L: -2, M: 0, T: 0, I: 0, Theta: 0, N: 0, J: 0 }` in Task 1c validator spec and §5.1. Ricci and Einstein inherit consistent units. |
| F9 | IMPORTANT | Eve #8 | **Mercury Δφ tolerance ±1e-4 relative.** 2-orbit Δφ ≈ 2.1×10⁻⁴ rad; ±1e-4 relative = ±2.1×10⁻⁸ rad absolute (≈4 mas), below cross-validation reach at 50k steps even with true GL4. | **PARTIALLY RELAXED (I4):** Relaxed to ±5×10⁻⁴ relative (±1.05×10⁻⁷ rad absolute). Honest at 50k GL4 steps. Tighter precision deferred to v0.6.0+ (adaptive step-size or 5×10⁵+ steps). §3 Task 2a updated. |
| F10 | IMPORTANT | Eve #9 | **Shapiro tolerance ±1e-6 relative.** Earth-Mars Shapiro absolute delay ≈ 2×10⁻¹⁰ s; ±1e-6 relative = 2×10⁻¹⁶ s, below double-precision floor. | **FIXED (I5):** Relaxed to ±1×10⁻⁴ relative (2×10⁻¹⁴ s absolute — achievable above double-precision floor). §3 Task 2b updated. |
| F11 | IMPORTANT | Eve #10 | **Bisection re-integration cost.** O(N log ε) full GL4 integrations per bracket would be minutes per CI test run. | **FIXED (I6):** Bisection now uses cubic Hermite polynomial interpolation on cached snapshot data. Full GL4 re-integration only for the bracket-width-refinement step (sub-step at h/10 for the one coarse bracket identified). §3 Task 1b algorithm rewritten. |
| F12 | IMPORTANT | Adam #2 | **bianchiResidual operates on mixed R^ρ_σμν but requires all-lower R_μνρσ.** The Bianchi identity cyclic sum is over all-lower indices. The RiemannTensorNode is 1 upper + 3 lower; applying ∇ directly to the mixed form gives the wrong contraction. | **FIXED (I7):** bianchiResidual now lower-first via `lower()` helper (v0.3.0) before constructing the ∇ + cyclic sum tree. §3 Task 1f spec updated with I7 note. |
| F13 | IMPORTANT | — | I8: subsumed by I3 (Riemann units fix covers this). | N/A |
| F14 | IMPORTANT | — | I9: subsumed by I4 (Mercury tolerance reconciliation). | N/A |
| F15 | MINOR | Adam #3 | **Phase 1a loop description ambiguous on data dependency.** The Newton iteration coupling between stages 1 and 2 was not spelled out. | **FIXED (M1):** §3 Task 1a algorithm rewritten with explicit Newton iteration scheme: stage values `(X_i, P_i)` at iterate k fed forward to update `(X_j, P_j)` at iterate k+1. Data-flow direction is explicit. |
| F16 | MINOR | Adam #4 | **`ricci()` signature has extraneous parameters** (`contractUpper`, `contractLowerMu`). The Carroll convention is fixed; no caller needs to specify which slot to contract. | **FIXED (M2):** Signature simplified to `ricci(R: RiemannTensorNode): ExprNode`. Convention is internal (always first-lower per Carroll). §3 Task 1d updated. |
| F17 | MINOR | Adam #5 | **Phase 1b: no bracket-width check before bisecting.** If the snapshot grid is coarser than the perihelion feature width, bisection cannot proceed safely. | **FIXED (M3):** Task 1b step 2 adds explicit bracket-width check: if `|τ_{i+1} − τ_i| < 2h`, emit warning and refine the enclosing step at `h/10` before proceeding. |
| F18 | MINOR | Eve #11 | **bianchiResidual returns only scalar max; callers cannot inspect component-level residuals.** | **FIXED (M4):** `bianchiResidual` now returns full 5-index `evaluate()` method + convenience `evaluateMax()`. Callers can reduce however they need. §3 Task 1f signature updated. |
| F19 | MINOR | Eve #12 | **Yoshida sum-coefficient prose was incorrect.** `Σ dᵢ = 1` claim for d₄ = 0 was inconsistent (`w₁ + w₀ + w₁ + 0 ≠ 1`). | **N/A (M5):** Moot — Ruth-4 removed; GL4 uses a Butcher tableau where `Σ b_i = b₁ + b₂ = 1` ✓ with no anomalous zeroth coefficient. |
| F20 | MINOR | Eve #13 | **Phase 2c "real Shapiro delay" framing too confident before 1a/2b are verified.** | **FIXED (M6):** Task 2b honest framing updated: "best-effort numeric" framing added until 1a and 2c are verified; JSDoc instruction to mark as "best-effort numeric" in code comments. |
| F21 | MINOR | Eve #14 | **Missing one-line pinning vitest for Riemann/Ricci closed-form.** | **FIXED (M7):** Task 1c test approach adds pinned closed-form assertion: `R^t_rtr(r=6M, M=M☉) === 2GM☉/(6M☉)³` to ≤1e-10 relative. Cross-checks the corrected Riemann formula (S3) at a concrete coordinate. |
