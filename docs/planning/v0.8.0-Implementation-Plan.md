# v0.8.0 Implementation Plan — r1 (consume with Design r2 deltas)

> Consumes `v0.8.0-Design.md` **r2** — its §8 r2 deltas OVERRIDE task
> details here (operator name `composeEdges`; GW formulas/tolerances
> per r2-2/r2-3; adjudication precedence r2-4; named test pins r2-5;
> version adjudication r2-6). Baseline at HEAD (2026-06-11):
> **2126 passed / 0 failed / 5 skipped / 1 todo (202 files), tsc clean.**
> Every task: pre-execution verification gate (read the real source the
> task touches before writing tests), scoped vitest during TDD,
> full-suite only at the release gate. Honest-deviation notes in commit
> messages. One commit per phase minimum.

## Phase 1 — Graph core (T1, T2) — `src/composition/`

- **1.1** `quantity.ts`: `Quantity`, `RegimeAttributes`, `dimEquals`
  (gate: check `src/dimensional/algebra.ts` for an existing equality
  helper first — reuse, don't duplicate). Tests: attribute sparsity,
  dimEquals exactness.
- **1.2** `edge.ts`: `ValidityDomain`, `BridgeEdge`, typed error classes
  (`CompositionJunctionError`, `CompositionDimensionError`,
  `CompositionDomainError`). Tests: domain predicate behavior.
- **1.3** `compose.ts`: `compose()` per design D-1..D-5 +
  `QUANTITY_IDENTIFICATIONS` registry + `minConfidence`. Tests: junction
  matching by name and by identification; dimension-mismatch throws;
  min-confidence demotion; domain piping (second's domain checked on
  intermediate value); chained compose (3 edges).
- **1.4** `consistency.ts`: `consistencyRatio` (CT-2 helper). Tests:
  shared-source elimination on synthetic edges.
- **1.5** `index.ts` barrel + re-export from `src/index.ts` (`@public`
  tags; gate: read `src/index.ts` export conventions first).

## Phase 2 — Calibration edges + pre-registered targets (T3, T4)

- **2.1** `src/composition/edges/` — catalog-backed edges wrapping the
  EXISTING evaluators (gate: read each evaluator's exact signature):
  `be42Edge` (M→T_H, wraps `evaluateHawkingTemperature`), `be16Edge`
  (T→E_min, wraps `evaluateLandauerEnergy`), `be51Edge`, `be52Edge`,
  plus laws `lawSchwarzschildRadius` (M→r_s) and `be42ViaRsEdge`
  (r_s→T_H variant, documented as re-parameterization). Each carries a
  G-8 `ValidityDomain` (M>0; T>0; b>0 ∧ weak-field b≫r_s; 0≤e<1 ∧ a>0).
- **2.2** CT-1: `composeErasureCost = compose(be42Edge, be16Edge)` via
  the T_H≡T identification. Test pins closed form ℏc³ln2/(8πGM) at
  3 masses, relErr ≤1e−12, junction dim [Θ], output dim [energy],
  confidence = highly-speculative.
- **2.3** CT-1b: 3-edge law chain ≡ 2-edge ≡ closed form.
- **2.4** CT-2: ratio independent of M (3 decades), equals
  2a(1−e²)/(3πb).

## Phase 3 — GW170817 → BE-36 (T5)

- **3.1** Gate: read `src/bridges/equations/be-36-gw-speed-bound.ts`
  fully (encoded bound semantics, existing evaluator names).
- **3.2** `src/bridges/be36-gw170817-confrontation.ts`: observation
  record + `confrontBE36()` per design §4. Tests: reproduce published
  −3×10⁻¹⁵ / +7×10⁻¹⁶ to ≤5%; positive-side satisfies encoded ≤10⁻¹⁵;
  input validation.

## Phase 4 — Membership + adjudication (T6, P-4)

- **4.1** Gate: `grep -rn "bridges:" tests/` for tuple pins; enumerate
  every entry with `['unknown','unknown']`.
- **4.2** `src/bridges/membership.ts`: criterion + whole-catalog report
  fn. Tests: verdict mapping; catalog tally pins.
- **4.3** `src/bridges/rejected.ts`: `REJECTED_BRIDGE_ADJUDICATIONS`
  (BE-29 + any other upheld NOT-A-BRIDGE outcomes, with reasons +
  citations). Consistency test (rejected ∩ tuple-bridge = ∅).
- **4.4** Catalog edits: BE-42 tuple → `['gravity','quantum']` + notes
  addendum; per-entry adjudication of remaining unknowns (update tuple
  where clear, document where contested). Update any pinned tests in
  the same commit. Report doc:
  `docs/architecture/v0.8.0-catalog-adjudication.md`.

## Phase 5 — Supporting (T7–T11)

- **5.1** `npm i -D fast-check` (intentional lockfile change) + property
  tests (design §6). Gate: confirm vitest+fast-check interop with one
  minimal property first.
- **5.2** `.github/workflows/ci.yml`; locally execute each step it runs.
- **5.3** `scripts/emit-catalog-json.mjs` + schema + `catalog:json`
  script + round-trip test + committed `data/bridge-catalog.json`
  (requires `npm run build` first — gate: confirm dist/ freshness).
- **5.4** `CONTRIBUTING.md`.
- **5.5** Part-VI status-promotion protocol subsection (P-5).

## Phase 6 — Release gate

Full suite + `npx tsc --noEmit` + `npm run build` + `npm run smoke`.
All must be green before Phase 7.

## Phase 7 — Architecture-docs refresh (user-added gate)

Survey `docs/architecture/*.md` for stale claims (bridge counts,
versions, suite counts, "not implemented yet" claims overtaken by this
work); apply targeted fixes; add the adjudication report from 4.4.

## Phase 8 — Final documentation

CHANGELOG `[Unreleased]` block for the v0.8.0 work; todo.md update
(active-queue entry + improvement-plan status flips); push.
