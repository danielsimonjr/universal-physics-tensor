# Canonical-Only Discovery Baseline (v0.23.0)

> Generated 2026-06-18 by `upt map --source=canonical` and
> `upt discover --source=canonical` over the **canonical-equation L-layer
> alone** — the textbook ground-truth registry (`src/canonical/registry.ts`),
> with the 44-bridge catalog entirely excluded. This is the standard-physics
> *consistency baseline*: what the discovery funnel does when pointed at
> equations no one disputes. Pinned by `tests/composition/canonical-graph.test.ts`.
> NOTHING here is a status claim (Part-VI §XXVII-B): the funnel proposes; humans
> dispose.

> **Update — 66-law registry (2026-06-21).** The Adam+Eve audit grew the canonical
> L-layer **26 → 66 laws**. Re-running the same two commands on the larger registry:
> the linkage map is now **15 components over 66 edges** (anchored cluster still 16),
> and the discovery funnel is **120 candidates → 7 promising · 104 inert · 9
> magnitude-clash · 0 contradictory**. The load-bearing property **holds at the
> larger scale: `contradictory = 0`** — standard physics stays self-consistent under
> the engine even after ~2.5× more laws. The promising residue grew from 2 → 7
> (more clean-monomial pairs to coincidentally match) and `upt discover
> --source=canonical --derive` now emits **3** unadjudicated proposed equations (was
> 1). The v0.23.0 detail below is preserved as the original baseline. See
> `docs/research/bridges-vs-canonical-map.md` and `Part-XI-Proposed-Equations.md`.

## Why a canonical-only baseline

The discovery loop is normally run over the bridge catalog, where a `promising`
verdict is interesting *because* the bridge is speculative. Running it over the
**canonical L-layer instead** answers a different, calibrating question: when the
inputs are all established physics, does the funnel stay quiet, and is its
residual signal explicable? Two properties are load-bearing:

- **No contradiction must appear.** Standard physics is mutually consistent, so
  `contradictory = 0` is the pass condition. Any non-zero count would indict the
  engine, not the physics.
- **The promising/clash residue must be interpretable.** Whatever survives the
  funnel should be a recognisable dimensional coincidence or a real scale
  incompatibility — never a namespace or anchor artifact.

This run is also the regression surface for two fixes and one declared link
applied 2026-06-18 (see *Provenance* below); the numbers here are the
post-fix steady state.

## Linkage map (`upt map --source=canonical`)

```
11 components over 26 edges; 5 compose into chains
  ● cluster of 16   [ANCHORED to known physics]
       link hubs: compton-wavelength, mass, period, r, temperature
  ○ isolated (10)   — share no quantity with any other edge
```

The anchored cluster (16 of 26 edges) spans gravitation (Newton, Kepler,
Schwarzschild, Hawking, light-deflection, perihelion, pendulum), thermodynamics
(Stefan–Boltzmann, ideal-gas, Landauer, Jarzynski, Wien), the matter-wavelength
family (Compton, thermal-de-Broglie, de-Broglie), and Coulomb (joined via the
shared `r` length hub). The engine reports its link hubs as
`compton-wavelength, mass, period, r, temperature` — the payoff of unifying the
canonical variable names (previously `M`/`T`/`m_1`/`m_2` fragmented `mass` and
`temperature` into private nodes) and of declaring the compton↔de-Broglie link
(which folded `de-broglie-wavelength` onto the `compton-wavelength` hub). The 10
isolated edges share no symbol with any other equation: the three Planck units,
Bohr radius, Lorentz force, Planck–Einstein (`photon-energy`), Bekenstein–Hawking,
the Einstein field equation, Friedmann, and string-wave-speed.

## Discovery funnel (`upt discover --source=canonical`)

```
funnel:  33 candidates  →  2 promising  ·  26 inert  ·  5 magnitude-clash  ·  0 contradictory (falsified)
```

The 33 are the distinct same-dimension candidate pairs after the proposer
canonicalises `de-broglie-wavelength → compton-wavelength` (the declared link, so
that pair is never re-proposed).

### Promising (2) — flagged dimensional coincidences, NOT claims

| Pair | Dim | Score | Unlocks | Reading |
|---|---|---|---|---|
| `erasure-energy ≟ photon-energy` | [[energy]] | 7 | `photon-energy` | Landauer `E = k_B T ln2` vs Planck–Einstein `E = hν`. Identifying them sets a "Landauer photon" frequency `ν = k_B T ln2 / h` — links the information/thermo cluster to the quantum-EM photon. A clean dimensional coincidence worth a physicist's eye; no mechanism asserted. |
| `free-energy-difference ≟ photon-energy` | [[energy]] | 7 | `photon-energy` | Jarzynski `ΔF` vs `hν`, the same thermal-energy↔photon coincidence one rung up (ΔF shares Landauer's `k_B T` monomial). |

Both survive because they **merge two components** (thermo/information ↔ the
isolated Planck–Einstein edge) and **unlock `photon-energy`** — a quantity the
single-mass anchor cannot otherwise reach — while staying numerically consistent.
They are the honest residue of dimensional matching, not endorsements.

### Magnitude-clash (5) — all GENUINE scale incompatibilities

| Pair | Orders apart | Basis |
|---|---|---|
| `mass ≟ planck-mass` | ~38 | anchor-derived (`mass` at the M_⊙ anchor) |
| `planck-length ≟ bohr-radius` | ~24.5 | sourced (ℓ_P vs a₀) |
| `planck-length ≟ compton-wavelength` | ~23.2 | sourced (ℓ_P vs λ_C) |
| `radius ≟ bohr-radius` | ~13.4 | anchor-derived (Schwarzschild r at M_⊙) |
| `radius ≟ planck-length` | ~38 | anchor-derived |

Every clash here is a *true* scale gap, not an artifact: Planck-scale lengths
really are ~23–24 orders below atomic radii, and a solar-mass Schwarzschild
radius (~3 km) really is ~13 orders above the Bohr radius. The one prior **false**
clash in this slot — `compton-wavelength ≟ bohr-radius` at a spurious ~61 orders —
was an anchor artifact (the 1/mass Compton wavelength evaluated at M_⊙ → 10⁻⁷³ m)
and is now correctly `inert` after sourcing both atomic magnitudes.

### Subsuming (1, barred from promising)

`length ≟ planck-length` — a generic↔specialisation pair (tautological).

## Headline: zero contradictions

`contradictory = 0` across all 33 hypotheses. The L-layer is self-consistent
under the engine, which is the pass condition for the baseline. The funnel's
entire output is two flagged coincidences and five genuine scale gaps — exactly
the "quiet, interpretable" behaviour the calibration demands.

## Provenance — fixes folded into this baseline (2026-06-18)

This baseline reflects three changes made the same day (branch
`claude/upt-analysis-no-bridge-kduiw0`), each surfaced by an earlier run of this
very analysis:

1. **Magnitude-gate sourcing.** Added sourced CODATA representative values for
   `compton-wavelength` (2.43e-12 m) and `bohr-radius` (5.29e-11 m) so the gate
   compares real magnitudes instead of falling back to the M_⊙ anchor. Killed the
   false `compton ≟ bohr` clash; kept the genuine Planck-scale clashes.
2. **Canonical name unification.** `T → temperature`, `M → mass`, Newton
   `m_1`/`m_2` → `mass`/`secondary-mass` (not both `mass`: `buckinghamPi`
   requires unique governing names, and the two distinct masses are Newton's
   free mass-ratio). De-fragmented the map (14 → 11 components; anchored cluster
   8 → 16 edges) and removed the `mass ≟ m_1`/`m_2`, `temperature ≟ T` namespace
   artifacts.
3. **Declared compton↔de-Broglie link.** Registered
   `de-broglie-wavelength → compton-wavelength` in `QUANTITY_IDENTIFICATIONS`:
   the Compton wavelength is the de Broglie wavelength at the relativistic limit
   `p = mc`, so at the graph's quantity-KIND resolution they name one matter-
   wavelength node. Folded onto `compton-wavelength` (the anchor-determinable,
   sourced node) so the genuine `planck-length ≟ compton-wavelength` clash
   survives. Moves the correspondence from a re-discovered `promising` candidate
   to a declared link.

Net effect across the three: discovery candidates 66 → 33, promising 8 → 2,
false clashes 1 → 0, contradictions 0 → 0 throughout.
