# v0.9.0 Baseline — Phase 0 (sprint renumbered from "v0.7.2")

> 2026-06-11. Fulfills the plan's `v0.7.2-baseline.md` deliverable
> under the renumbered label (v0.8.0 design r2-6). Executed at
> post-v0.8.0 HEAD — materially later than the plan's `9c92d11`
> assumption; drift documented per task.

## Task 0.1 — Suite + build baseline

- Suite at sprint start: **2182 passed / 0 failed / 5 skipped / 1 todo
  (209 files)** — not the plan's 2103/200: the v0.8.0 release (+79) and
  its punch-list landed in between. tsc 6.0.3 strict clean; build clean.

## Task 0.2 — O-1 + O-6 callsite re-grep (Adam A-1 revised regex)

- **Plan-bug found:** the plan's comment filter
  (`grep -vE "^\s*(//|...)"`) cannot match grep output (lines start
  with `path:line:`, never whitespace) — it filtered nothing. Recounted
  with an awk content-part filter.
- Raw hits: **119**; non-comment: **80** across **24 files** (vs Adam
  A-1's ~109 raw / ~53 non-comment — drift from v0.8.0-era test growth
  (weyl-kerr-schild, foreach-multi-index) + the corrected filter).
- Category 2 (direct-indexed `src/`) matches the plan's six files
  exactly: gl4-integrator, weyl-lowering, perihelion-finder, killing,
  null-ic, be37-covariant-eikonal — **plus `src/dimensional/metric.ts`**
  (not in the plan's list; classified during Phase 3).

## Task 0.3 — PG callsite re-grep

- **17 non-comment hits across 4 files** (vs Eve E1.1's ~5): fixture
  definitions, the curvature test, the bench, **plus
  `tests/api/public-tag-vs-index-invariant.test.ts`** (PG named in the
  accepted-drift list — name-only, no call). Direct-index assertion
  sites enumerated in the curvature test lines 184–232 (+ the
  Kretschmann helper's TWO `as number[][]` casts at lines 84–85, which
  the plan missed — see Phase 1 notes).

## Task 0.4 — S-9 deferred-arm re-grep

- **Exactly 5 arms at lowering.ts:534, 546, 557, 569, 579** — matches
  Eve E1.2 with zero drift (lowering.ts untouched by v0.8.0).

## Task 0.5 — Bench baseline re-capture

- **Environment deviation:** this capture runs on the remote Linux
  container, NOT the Windows box that produced the v0.7.1 numbers, so
  the plan's 5%-drift gate against 3,490 hz is inapplicable. Absolute
  baselines re-anchored here; the Phase-5 Decision-#6 gate is
  ratio-based (same-machine before/after), which remains valid.
- PO-1 `solveGL4Stage` single-stage: **2,082 hz** (±0.39%).
- PO-1 100-stage batch: **20.92 hz** (±0.57%).

## Phase 1 execution notes (recorded here per Task 0.6 + honest-claude)

- `MetricFn` lives in `curvature-lowering-helpers.ts:44`, NOT
  `numerical/types.ts`; `NestedArray` lives in `types.ts:11`, NOT
  `lowering-utils.ts:11` (two plan/Adam-A-3 location errors; aliases
  landed where the types actually live).
- `flattenNA` (connection-lowering-helpers.ts:31) walks leaves with
  `for...of` — Float64Array-safe at runtime, confirming Decision #3
  Category 1 needs only the type widening.
- **TDD-RED catch:** the PG curvature test's Kretschmann helper had two
  `gFn(x) as number[][]` casts; against the flat layout these produce
  `undefined` lookups → NaN. The plan enumerated only the assertion
  callsites (lines 229-232). Both casts replaced with explicit
  unflattens (computeKretschmann + the local lowerRiemannFirstIndex
  still take `number[][]` — O-4 stays deferred).
