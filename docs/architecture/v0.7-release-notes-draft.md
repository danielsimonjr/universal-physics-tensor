# v0.7-Series GitHub Release Notes — Drafts

> Pre-staged release-notes text for the three tag-strategy options
> in `CHANGELOG.md` `[Unreleased]`. Mechanical prep — pick the
> option that matches the chosen tag(s), paste into the GitHub
> release UI when publish unblocks. **No edits needed beyond
> filling in the tag name + commit SHA.**

**Date prepared:** 2026-05-23.
**Branch HEAD at prep:** `d62e0e2` (run `git rev-parse HEAD` to
re-check before tagging — the branch may have advanced).

---

## Option 1 — Single `v0.7.0` rolling everything

**Title**: `v0.7.0 — Foundation Consolidation + Extension Surface (6 proposals shipped)`

**Body**:

```markdown
**The largest single UPT release to date.** Six v0.7-series proposals
shipped on top of the v0.6.1 hygiene sprint, plus a v0.7 hygiene
follow-up that closed 4 stale carry-forwards including a 3-release
Shapiro-residual investigation.

## Major additions

### v0.7-p3 — Typed `Cell` discriminated union
`Cell = LawCell | BridgeCell | EmergenceCell` discriminated union
with full `addCell` dispatch + `compose(laws, bridges, emergences,
config)` factory. **+7 public symbols.**

### v0.7-p2 — Sparse semantic catalog + flux rules
Three-rule registry (`'dimensional-consistency' | 'lbe-coordinate' |
'causality'`) with `_exhaustive: never` dispatch. New `UniversalTensor`
methods: `populatedCount`, `populatedCells`, `unpopulatedNeighborhoods`,
`fluxDiagnostics`. New catalog adapter (`catalogToCells`, `scanCatalog`,
`ingestCatalog`) for `BRIDGE_EQUATIONS` ingestion. **+10 public symbols.**

### v0.7-p1 — Intelligent Index Layer
`UniversalIndex<Axis>` with branded `UniversalIndexId`; `Axes` module
singleton (18 frozen references across scale/force/symmetry/information
axes); `LabeledTensor<L>` wrapper matching by `UniversalIndexId`
identity, not by string label. **+12 public symbols.**

### v0.8 Proposal 5 (ahead of target) — `RegimeType` Extension System
Extensible regime registry with provenance + per-axis convenience APIs.
Sibling-registry pattern for cell attachment (avoids P3 surface
breakage). `FluxRuleKind` extended with `'regime-consistency'` rule
(WARNING tier). **+16 public symbols.**

### v0.9 Proposal 8 (ahead of target) — Bridge Parameter Differentiation
`bridgeGradient(spec, engine, params)` thin wrapper over
`MathTSEngine.reverseGrad`. Four representative differentiable bridge
specs (BE-11, 37, 42, 52). Graceful degradation via
`EngineCapabilityError` when the optional `mathts-autograd` peer is
absent. **+7 public symbols.**

### v0.8+ Proposal 6 Phase A — Bridge Composition Research Track (docs-only)
Research-spec `Part-IX-Composition.md` defining numerical-cascade
composition + categorical layer, plus open-questions doc and Adam+Eve
review-findings doc. Phase B (calibration) deferred to v0.9α.

## v0.7 hygiene follow-up (4 stale carry-forwards closed)

- **PC-1.5 Shapiro residual investigation** — closed after 3 releases.
  Documented residual was 2.51e-4; actual at HEAD is **2.28e-8** (4
  orders of magnitude tighter). Side effect of v0.6.0's BR-2
  christoffelFn → Float64Array(64) refactor, never re-measured.
- **`lowering.ts` LOC target** — v0.6.1 missed 890 LOC budget by 13.
  Extracted tensor-partial-derivative + covariant-derivative arms
  into `derivative-lowering.ts` via thunk pattern. **903 → 597 LOC**
  (beat target by 293).
- **AS-3 `schwarzschildPin` helper** — v0.5.1 estimate "~65 invocation
  sites" was stale by 1 OOM (actual: 8). Helper overkill at that
  scale; closed WON'T-DO.
- **Vitest 4.1.4 async-bench reporter limitation** (v0.4.5) — fixed
  in vitest 4.1.7 (patched via dep bump). hz tables now print for
  async benches.

## v0.7 hygiene follow-up (BE-module exports + audit)

- 20 unused BE-module exports dropped (bucket-(a)); 40 `*Inputs`
  interfaces annotated `@internal` (bucket-(a')). BE-module exports
  triage fully closed.
- Bridge-physics audit `encoded_form` field added to
  `BridgeEquationEntry`; applied to BE-13, BE-47, BE-48 (the audit's
  identified encoding-reductions). Status-recalibration attempt
  surfaced 5 conflicts with deliberate test-pinned rationale —
  conflict-analysis doc shipped (`docs/architecture/v0.7-bridge-status-recalibration-analysis.md`)
  for editorial decision.

## Suite + build

**1675 → 2056 passed (+381 net new tests)**, 0 failed, 5 skipped
(3 PC-1.5 long-run + 2 optional-dep), 1 todo. Build clean (tsc
strict) throughout. `npm audit` 0 vulnerabilities.

## Documentation

- `docs/specification/Part-IX-Composition.md` (P6 Phase A)
- `docs/architecture/intelligent-index-tutorial.md` (P1)
- `docs/architecture/bridge-gradient-tutorial.md` (P8)
- `docs/architecture/v0.7-pc15-shapiro-floor.md` (PC-1.5 closure)
- `docs/architecture/v0.7-be-module-exports-audit.md`
- `docs/architecture/v0.7-bridge-status-recalibration-analysis.md`
- `docs/architecture/v0.7-near-horizon-kretschmann-design-note.md`
- `docs/architecture/v0.7-tensor-equation-node-design-note.md`

## Dep updates

- `@types/node` 24.12.2 → 24.12.4 (patch)
- `vitest` 4.1.4 → 4.1.7 (patch)
- `@danielsimonjr/mathts-tensor`, `@danielsimonjr/mathts-autograd` —
  optional peers at `^0.1.0` (unchanged)

## Adam+Eve adversarial reviews

Six proposals went through full review cycles (P3, P2-original,
P2-redraft, P1, P5, P8, P6-A) plus four hygiene-leftover audits.
Pattern this session: **4 of 4 hygiene-leftover audits found their
carry-forward numbers stale** (PC-1.5 4 OOM, AS-3 1 OOM, BE-module
40%, vitest reporter no longer present). Memorialized as a v0.7
execution lesson in `todo.md`.
```

---

## Option 2 — Three-tag split

### `v0.7.0 — Foundation Consolidation (P3 + P2 + P1)`

**Title**: `v0.7.0 — Foundation Consolidation`

**Body**: paste the Major-additions section above for P3 + P2 + P1
ONLY (omit P5/P8/P6). Add suite numbers 1675 → 1813 (the post-P1
checkpoint).

### `v0.8.0 — Extension Surface (P5 + P6 Phase A)`

**Title**: `v0.8.0 — Extension Surface Expansion`

**Body**: paste P5 + P6-A sections. Add suite numbers 1813 → 1835.

### `v0.9.0-alpha — Bridge Parameter AD (P8)`

**Title**: `v0.9.0-alpha — Bridge Parameter Differentiation`

**Body**: paste P8 section. **Alpha-tag rationale**: the optional
`mathts-autograd` peer is not auto-installed in CI; real-AD tests
are `describe.skipIf(true)`-marked until that's documented. Alpha
gives consumers a heads-up that the surface ships but the test
matrix is incomplete.

Suite numbers 1835 → 2056 (including v0.7 hygiene follow-up + BE-X re-encoding sprint + BE-53/54 catalog extensions + BRIDGE-PHYSICS-AUDIT-v2 E1 fix).

---

## Option 3 — Some other partitioning

User's choice; the per-proposal text above can be split any way.
The hygiene-follow-up content (PC-1.5, lowering.ts, dep bumps,
audit work) can attach to whichever tag closes the cycle.

---

## Pre-publish checklist (regardless of option chosen)

- [ ] `git rev-parse HEAD` — confirm tag points at the intended commit
- [ ] `npm audit` (expect 0 HIGH/CRITICAL)
- [ ] `npm outdated` — document any remaining outdated deps in the
      release body under "Dep updates"
- [ ] `npm test` — confirm 2056 passed / 0 failed / 5 skipped / 1 todo (200 files, 198 passed + 2 skipped). (Number drifted past initial pre-flight `1854` per Eve-E13 of BRIDGE-PHYSICS-AUDIT-v2; re-pin to HEAD before tag.)
- [ ] `npm run build` — confirm tsc clean
- [ ] Bump `package.json` version (`0.6.0` → chosen target)
- [ ] Commit version bump
- [ ] `git tag vX.Y.Z` + `git push origin vX.Y.Z`
- [ ] Create GitHub release using the relevant body above
- [ ] (Token-blocked) `npm publish --ignore-scripts --access public`
      once NPM_TOKEN is rotated. Tag/release can land independently.

## Why this doc exists

Per the session pragma "skip publish for now, continue with the
rest," this draft is pre-staged so the publish step (when it
happens) is paste-and-go rather than re-deriving the release
narrative.
