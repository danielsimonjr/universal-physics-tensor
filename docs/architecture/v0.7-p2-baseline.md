# v0.7 Proposal 2 — Phase 0 Baseline Capture

> Phase 0 deliverable per `docs/planning/v0.7-Proposal-2-Design.md`
> (post-Adam+Eve-redraft, commit `01632b5`). Establishes the ground-
> truth measurements that Phases 1-4 compare against and verifies that
> P3 (the prerequisite) is shipped at HEAD.

**Date:** 2026-05-23.
**HEAD at baseline:** `c5cab59` (docs(v0.7-p2): Eve re-review of redraft — READY for execution).
**Branch:** `claude/changelog-todo-sync-9PdMg`.

---

## Task 0.1 — P3 prerequisite gate

`src/core/cell.ts` exists at HEAD. Required exports verified present:

| Export | Verified? |
|---|---|
| `CellConfidence` (type) | ✓ |
| `CellBase` (interface) | ✓ |
| `LawCell` (interface) | ✓ |
| `BridgeCell` (interface) | ✓ |
| `EmergenceCell` (interface) | ✓ |
| `Cell` (union) | ✓ |
| `compose` (function) | ✓ |

`src/index.ts` re-exports all 6 type-only Cell symbols + `compose`
(at lines 39-46 + 47). P3 prerequisite **PASSES**.

---

## Task 0.2 — Suite baseline

`npm test`:

```
 Test Files  179 passed | 2 skipped (181)
      Tests  1697 passed | 1 skipped | 1 todo (1699)
```

- Passed: **1697**
- Failed: 0
- Skipped: 1 (intentional, optional-dep-gated)
- Todo: 1 (intentional)
- Test files: 179 passed + 2 skipped = 181 total

**Gate for Phases 1-4**: suite must stay ≥ 1697 passed, 0 failed.
Net-add only.

---

## Task 0.3 — Type-shape + empirical audit

All design citations re-verified at HEAD:

| Claim | File:line | Verified |
|---|---|---|
| `BridgeCell.source: TensorIndices` | `src/core/cell.ts:110` | ✓ |
| `BridgeCell.target: TensorIndices` | `src/core/cell.ts:111` | ✓ |
| `LawCell.scales: ReadonlyArray<PhysicalScale>` | `src/core/cell.ts:90` | ✓ |
| `EmergenceCell.indices: ReadonlyArray<TensorIndices>` | `src/core/cell.ts:131` | ✓ |
| `BridgeEquationEntry.dimensional_signature: string \| null` | `src/bridges/index.ts:140` | ✓ |
| `grep -cE "^\s+dimensional_signature: null" src/bridges/index.ts` | — | **0** ✓ (Eve-R1 lesson re-confirmed) |
| ``grep -cE "bridges: \[\`unknown\`," src/bridges/index.ts`` | — | **26** ✓ (Adam-V5 corrected) |
| `grep -cE "^\s+id: [0-9]+," src/bridges/index.ts` | — | **42** ✓ (catalog invariant) |
| `EXPECTED_DIMENSION_BY_BRIDGE` entry count | `src/dimensional/bridge-check.ts` | **40** entries |

**Risk §3 coverage**: `EXPECTED_DIMENSION_BY_BRIDGE` has 40 entries
covering bridge IDs in the catalog (42 total entries). Per the v0.6.0
maintenance work, 2 entries (BE-51 + BE-52) are codebase-only additions
not in the formal catalog count. The 26 `'unknown' → 'unknown'` bridges
are a subset that the dimensional-check covers via the `null` /
`'[dimensionless]'` path. Rule 3 (Causality) operates with the
`scale-unspecified-skip` info diagnostic for these (per Decision #5).

---

## Task 0.4 — Public-surface baseline

| Measure | Value |
|---|---|
| `src/index.ts` `^export` count | 35 |
| `tests/api/__snapshots__/public-surface.test.ts.snap` LOC | 74 |
| Snapshot last modified | post-v0.7-p3 (commit `978c11d`) |

Phase 4a will regenerate this snapshot after adding new P2 exports
(per Decision #9's public-surface bucket policy).

---

## Sprint baseline summary

| Metric | Current (HEAD `c5cab59`) | v0.7-p2 final target |
|---|---:|---:|
| Tests passed | 1697 | ≥ 1697 + N (N = net-new in Phases 1-4) |
| Tests failed | 0 | 0 |
| Test files | 179 | 179 + 3-5 new |
| `src/index.ts` exports | 35 | 35 + ~6 (per Decision #9 buckets) |
| `src/core/tensor.ts` LOC | 571 | 571 + ~80 (`fluxDiagnostics`, `populatedCells`, `unpopulatedNeighborhoods`) |
| `npm audit` HIGH/CRITICAL | 0 | 0 |

---

## Plan-time concerns inherited from Eve re-review (R15-R19)

These ride along during Phase 1+ implementation:

- **Eve-R15 (MEDIUM)** — `populatedCount()` vs `populatedCells()`
  semantics: the former returns `tensorData.size` (key count, post
  fan-out per Decision #6); the latter dedupes to `Cell` objects.
  These will disagree on multi-scale `LawCell` inputs. Phase 2
  Task 2.4's cross-method consistency test will surface this if
  it's not pinned in the task spec. **Action**: Phase 2 task
  descriptions must spell out the semantic difference and ship a
  test that asserts BOTH counts simultaneously.
- **Eve-R16 (LOW)** — Phase 0 Task 0.3 grep against
  `EXPECTED_DIMENSION_BY_BRIDGE` was mis-shaped (the constant is
  `Map<number, Dimension>`, not a `'from→to'` record). Phase 0
  re-shaped the grep to count entries via `'\[[0-9]+,'` pattern;
  reported as 40 above.
- **Eve-R17 (LOW)** — Decision #7's "legacy discriminator" phrasing
  overstates the existing `' -> '` infix as already-branching; it's
  just a key-format choice in the constructor at `tensor.ts:402`.
  No design rework needed; honest wording fix at Phase 4a docs pass.
- **Eve-R18 (LOW)** — `info`-tier silence policy at `addCell` time:
  Phase 2 task spec must commit. Default: emit once per `addCell`
  call (matches the warning/error tier policy).
- **Eve-R19 (LOW)** — Two diagnostic report shapes
  (`CatalogIngestionReport.errors` vs `FluxReport`). Phase 3 + 4a
  must either harmonize or document the separation explicitly.

---

**Phase 0 complete.** No design rework needed. All 17 prior findings
remain resolved. Phase 1 (Flux-rule scaffolding) is unblocked.
