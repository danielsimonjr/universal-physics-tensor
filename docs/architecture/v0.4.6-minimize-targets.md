# v0.4.6 Minimize/Simplify Targets

**Generated**: 2026-05-17
**Source**: post-v0.4.5 deep audit using RLM + honesty discipline
**Scope**: per Daniel — unreachable code + lies in comments + type holes + obvious algorithmic simplifications. Physics correctness deferred to v0.5.0.

---

## Summary

| Category | HIGH | MEDIUM | LOW | Total |
|---|---|---|---|---|
| Unreachable code | 2 | 3 | 1 | 6 |
| Lies in comments | 2 | 5 | 2 | 9 |
| Type-safety holes | 3 | 4 | 2 | 9 |
| Algorithmic simplifications | 2 | 4 | 2 | 8 |
| **Total** | **9** | **16** | **7** | **32** |

---

## Category 1: Unreachable Code

### UC-1 — `lowering.ts` line 471-472: partial computed unconditionally but the comment is the only thing explaining why — and the comment is wrong (HIGH)

**File**: `src/numerical/lowering.ts`, lines 462-493

**Finding**: The block comment at line 470 says:
> "(partial is also needed by the 'supplied' path below, so we compute it unconditionally and use the early return only for 'computed'.)"

This is CORRECT logic — but the code structure implies `partial` is computed before the `strategy === 'zero'` early-return check. Read the actual flow:

1. Line 457: `if (strategy === 'zero') { return zeroTensor(...); }` — early return for `'zero'`
2. Lines 473-486: `partial` is computed (unconditionally at this point)
3. Line 491: `if (strategy === 'computed') { return partial; }` — early return for `'computed'`
4. Lines 500-502: `if (ofFreeIndices.length === 0) { return partial; }` — early return if scalar operand

**The actually unreachable case**: after the `strategy === 'zero'` and `strategy === 'computed'` early returns, `strategy` at line 518 can ONLY be `'supplied'`. The cast `strategy as 'zero' | 'supplied'` at line 518 includes `'zero'` as a possibility, but `'zero'` is unreachable here — it already returned at line 458-459. This is a **dead type arm in the cast**, not a runtime error, but it creates a misleading type at the `getMetricDerivFlat` call site.

**Fix**: Change line 518 from `strategy as 'zero' | 'supplied'` to `'supplied'` (literal, no cast needed). This is the only value that can reach that line.

**Severity**: MEDIUM — the cast works at runtime (the `'zero'` branch of `getMetricDerivFlat` just returns zeros and is never reached), but it misrepresents what the code can actually do.

---

### UC-2 — `lowering.ts` lines 482-486: `else` branch in partial-computation block is dead (HIGH)

**File**: `src/numerical/lowering.ts`, lines 474-486

**Finding**: The code computing `partial`:
```ts
if (ofExpr.kind === 'tensor-symbol' || ofExpr.kind === 'metric-tensor') {
  // build pdNode and lowerNode(pdNode, ...)
} else {
  // Scalar or other: partial derivative is zero.
  const outShape = [...ofTensor.shape, N];
  partial = zeroTensor(outShape, engine);
}
```

The `else` branch is dead. `validateCovariantDerivative` (in `connection-validators.ts`) calls `validateChild(node.of)`, and `connection-validators.ts` only validates `CovariantDerivativeNode` which requires `of` to be a child that passes `validateChild`. The `of` field in a covariant-derivative node is, per the v0.4.0 design, always a tensor-symbol or metric-tensor (any other `of` would have failed dimensional validation before reaching the lowering phase — the validator walk in `validator.ts` line 554-555 throws for unknown kinds, and partial-derivative child resolution only accepts tensor-symbol, metric-tensor, kronecker-delta, and tensor-partial-derivative). A covariant-derivative with a kronecker-delta or pderiv `of` would also fail the `ofFreeIndices.length === 0` early return at line 500.

**Proof by elimination**: the `case 'covariant-derivative'` arm begins only after `validate(node)` passes (see `evaluateNumerical` in `index.ts` line 80). A validated covariant-derivative with `of.kind !== 'tensor-symbol' && of.kind !== 'metric-tensor'` would be a covariant-derivative of a scalar, where `ofFreeIndices.length === 0`, which exits at line 500-502. The `else` branch below line 482 can never fire.

**Fix**: Remove the `else` branch (lines 482-486). Replace with an assertion:
```ts
// of is always tensor-symbol or metric-tensor here (validated upstream)
partial = lowerNode(pdNode, inputs, engine);
```
Or restructure the if/else to throw: `throw new NumericalBackendError('unreachable: covariant-derivative of must be tensor-symbol or metric-tensor')`.

**Severity**: HIGH — dead code that conceals the actual invariant. Removing it makes the constraint explicit.

---

### UC-3 — `tensor.ts` lines 213-215: `Array.from(merged.entries())` copies before deleting (MEDIUM)

**File**: `src/dimensional/tensor.ts`, lines 213-215

**Finding**:
```ts
for (const [label, counts] of Array.from(merged.entries())) {
  if (counts.upper === 0 && counts.lower === 0) merged.delete(label);
}
```

The `Array.from(merged.entries())` snapshot is taken to allow mutation during iteration. However, in Step B (lines 191-208), when a label is fully contracted (`pairs === counts.upper === counts.lower`), `merged.set(label, { upper: 0, lower: 0 })` is called. Labels that are NOT contracted still have non-zero counts. Labels that ARE contracted have counts set to `{ upper: 0, lower: 0 }`.

This means Step C will always delete exactly the labels that Step B set to zeros — so Step C is equivalent to just deleting all labels whose final count is `{upper: 0, lower: 0}`. The snapshot is NOT needed for correctness here because we are iterating and deleting, not modifying counts. You CAN delete from a Map during `for...of` iteration over a freshly obtained entries view in modern JS, but the `Array.from` snapshot is needed to avoid the iterator-invalidation issue.

VERDICT: The `Array.from` is correct but it allocates an unnecessary array. This belongs under **Category 4** (algorithmic simplification). See AS-5 below.

**Severity**: LOW — not unreachable, moved to algorithmic category.

---

### UC-4 — `float64-engine.ts` line 196: `forEachIndex` called with `contractSizes` that could be empty (MEDIUM)

**File**: `src/numerical/float64-engine.ts`, lines 575-583

**Finding**: The nested `forEachIndex(contractSizes, ...)` at line 576 is called with `contractSizes` which is `spec.contractions.map(...)`. When there are no contractions (pure outer product), `contractSizes = []`. The `forEachIndex` function at line 355 handles `shape.length === 0` by calling `visit([])` once. So the scalar contraction value `1` is returned correctly by `let product = 1; ... acc += product`.

This is **correct** but the empty-contractions case is unusual and the path through `forEachIndex` with `[]` is technically fine. Not actually unreachable.

VERDICT: AMBIGUOUS (see Honesty Notes). Not flagged.

---

### UC-5 — `be37-covariant-eikonal.ts`: function accepts inputs but never uses `M_kg`, `R_far_m`, `R_near_m` for the actual computation (MEDIUM)

**File**: `src/numerical/be37-covariant-eikonal.ts`, lines 97-141

**Finding**: All three input parameters are validated (lines 100-119) and destructured, but `eikonalResidual = 0` is a literal constant that does not depend on them. The function says it "encodes the eikonal as ∇_μ ∇^μ S", but it actually returns a hardcoded 0. `shapiroDelaySec = 0` is also a stub constant.

The inputs are ONLY used for the domain guards (the range checks). The comment in the JSDoc says this is an "intentional v0.4.0 stub", which is accurate, but the function SIGNATURE implies it does meaningful computation when it does not.

**Fix**: This is a **lie in the comment/docstring** (actually the function body), not unreachable code. See LC-1 in Category 2.

**Severity**: Moved to Category 2 (LC-1).

---

### UC-6 — `connection-validators.ts` lines 94-98: validateChild called on gLower/gInverse but result discarded (MEDIUM)

**File**: `src/dimensional/connection-validators.ts`, lines 91-98

**Finding**: The comment at lines 94-97 says:
> "gLower and gInverse are validated via validateChild ONLY for their dim sanity-check; their free indices are deliberately discarded — the Christoffel contractions inside ∇_μ consume them internally. (We don't even need to call validateChild on them; their dims are structurally known and any malformed metric would have failed the signature checks above.)"

This is an explicit in-code acknowledgment that the `validateChild` call on `gLower`/`gInverse` is unnecessary. The signature checks (`indices[0].variance !== 'lower'` etc.) at lines 71-88 do verify the structural shape. The `validateChild` calls at lines 91-92 are never actually made — read lines 91-92:

```ts
const ofResult = validateChild(node.of);
const wrtResult = validateChild(node.wrt);
```

These call `validateChild` on `of` and `wrt` only, NOT on `gLower`/`gInverse`. The comment at lines 94-97 says they "are validated via validateChild" but that call never happens. The comment's parenthetical "(We don't even need to call validateChild on them...)" contradicts the first sentence.

**Fix**: The comment is confusing. Fix: delete the first sentence; keep only "gLower and gInverse are NOT validated via validateChild — their dims are structurally fixed and the signature checks above already guard the shape."

**Severity**: MEDIUM (comment fix only, no behavioral change).

---

## Category 2: Lies in Comments

### LC-1 — `be37-covariant-eikonal.ts`: function docstring claims it "encodes the eikonal" but it hardcodes 0 (HIGH)

**File**: `src/numerical/be37-covariant-eikonal.ts`, lines 78-93

**Misleading comment** (function-level JSDoc):
> "Encodes the eikonal as ∇_μ ∇^μ S using Task 2's CovariantDerivativeNode and Task 12's lowering path."

**What the code actually does**: lines 131-138:
```ts
const eikonalResidual = 0;
const shapiroDelaySec = 0;
return { eikonalResidual, shapiroDelaySec };
```

The function does NOT encode the eikonal using CovariantDerivativeNode or the lowering path. It returns two hardcoded zeros after running domain guards. The file-level module docstring (lines 1-27) DOES say "the eikonal residual is 0 by construction" and "shapiroDelaySec = 0 (intentional v0.4.0 stub)", which is honest. But the function-level JSDoc at lines 78-93 implies active computation via the connection layer, which is false.

**Fix**: First paragraph of the function JSDoc (line 79): change "Encodes the eikonal as ∇_μ ∇^μ S using Task 2's CovariantDerivativeNode and Task 12's lowering path." to "Returns the eikonal residual (exactly 0 by construction for a null ray) and a stub Shapiro delay (0 in v0.4.0). Does NOT use the covariant-derivative or lowering infrastructure — see file-level docstring for rationale."

**Severity**: HIGH — the existing comment misrepresents what the function does and implies computation that does not occur.

---

### LC-2 — `mathts-engine.ts` lines 3-4 and 33: claims MathTSEngine "becomes UPT's default in v0.4.0" — it IS the default in v0.4.5 (HIGH)

**File**: `src/numerical/mathts-engine.ts`, lines 3-4 and 33

**Misleading comments**:
> Line 3-4: "The second v0.3.5 engine; becomes UPT's default in v0.4.0 (v0.3.5-Design.md §12)."
> Line 33: "the second v0.3.5 engine; becomes UPT's default in v0.4.0."

**What the code actually does**: v0.4.0 shipped and MathTSEngine IS now the default (when the peer deps are present). The current codebase is v0.4.5. The future-tense "becomes" is a lie — it became the default in v0.4.0, which is past. The correct tense is "became UPT's default in v0.4.0" or better "is UPT's default when both @danielsimonjr/mathts-tensor and @danielsimonjr/mathts-autograd are installed."

Also, `engine-registry.ts` line 4 says "The second v0.3.5 engine; becomes UPT's default in v0.4.0" — same lie.

**Fix**: Change to past tense: "Became UPT's default in v0.4.0 when both optional peers are installed; see engine-registry.ts."

**Severity**: HIGH — future-tense comment that already happened. Misleads readers about the current state.

---

### LC-3 — `connection-lowering-helpers.ts` lines 25-29: the comment about `flattenNA` vs `flattenNestedArray` is misleading about the "duplicate" situation (MEDIUM)

**File**: `src/numerical/connection-lowering-helpers.ts`, lines 24-29

**Misleading comment**:
> "Canonical implementation. `flattenNestedArray` in lowering.ts is NOT a duplicate — it delegates here and adds an expectedSize invariant check (throws NumericalBackendError if the flattened count mismatches). This function deliberately omits that check for callers that do not know the expected count ahead of time."

**What the code actually does**: The comment is internally consistent but the "NOT a duplicate" framing implies the concern has been raised and settled. The concern is valid: `pderiv.ts` has its OWN `flattenToNumbers` (line 15-24) which does the SAME thing as `flattenNA` — a third independent flatten implementation. The comment only addresses the `lowering.ts` wrapper, ignoring the identical duplicate in `pderiv.ts`.

Cross-reference: `pderiv.ts:flattenToNumbers` is functionally identical to `connection-lowering-helpers.ts:flattenNA` — both walk nested arrays recursively and collect numbers. Neither is exported. The comment in `connection-lowering-helpers.ts` claims "Canonical implementation" but there are at least THREE flatten implementations in the numerical module (flattenNA, flattenNestedArray-wrapper, flattenToNumbers). The comment's claim of "canonical" is false.

**Fix**: In `connection-lowering-helpers.ts` line 25, note that `pderiv.ts` has its own `flattenToNumbers` which is a true duplicate that should be consolidated to `flattenNA`. Or consolidate them in v0.4.6. See also AS-1 in Category 4.

**Severity**: MEDIUM — comment says "canonical" when there are duplicate implementations; also points to AS-1 (consolidation opportunity).

---

### LC-4 — `lowering.ts` lines 291-292: comment says `of` is "a tensor-symbol or metric-tensor" but the code accepts more (MEDIUM)

**File**: `src/numerical/lowering.ts`, lines 290-292

**Misleading comment**:
> "v0.3.5/v0.4.0 scope: `of` is a tensor-symbol or metric-tensor."

**What the code actually does**: The `tensor-partial-derivative` case in `lowerNode` begins by checking `of.kind === 'metric-tensor'` (lines 297-338) and then `of.kind !== 'tensor-symbol'` (line 341 throws). So only tensor-symbol and metric-tensor are supported — the comment is accurate in what it says the scope IS. However, the comment immediately above at lines 290-293 is in the `case 'tensor-partial-derivative'` arm of `lowerNode`, yet this same arm is also reached by the `lowerContractable` dispatcher (line 171). The `operandIndices()` function at lines 63-75 also says "v0.3.5: `of` is a tensor-symbol or metric-tensor". These comments say the SCOPE is intentionally limited, which IS honest.

Actually this is fine. VERDICT: NOT a lie. Removing this candidate.

---

### LC-5 — `lowering.ts` lines 104-111: comment says this is "finding #1 of the v0.3.5 adversarial review" — stale reference (MEDIUM)

**File**: `src/numerical/lowering.ts`, lines 104-111

**Misleading comment**:
> "CRITICAL (finding #1 of the v0.3.5 adversarial review): this function does NOT decide which indices contract."

**What the code actually does**: The finding reference is to a specific review event. This is fine as historical context but it is now orphaned — v0.4.5 has shipped and the finding is stale archaeology. The word "CRITICAL" in a now-settled, already-implemented fix is misleading to new readers who may think the issue is still outstanding.

Similarly, line 249 says "computeContraction is the single authority on WHICH indices contract (variance-aware, implicit-metric rule — finding #1)" — again referencing a historical review finding number.

**Fix**: Remove "CRITICAL (finding #1 of the v0.3.5 adversarial review):" prefix. Replace with "NOTE: this function does NOT decide which indices contract — see computeContraction() for that."

**Severity**: MEDIUM — stale "CRITICAL" label that no longer signals an active concern.

---

### LC-6 — `float64-engine.ts` line 375: says "v0.3.5's default engine" — no longer true in v0.4.0+ (MEDIUM)

**File**: `src/numerical/float64-engine.ts`, line 374-375

**Misleading comment** (class-level JSDoc):
> "The pure-TypeScript, zero-dependency `TensorEngine` — v0.3.5's default engine and the correctness baseline for the conformance suite."

**What the code actually does**: Float64ReferenceEngine is the FALLBACK engine in v0.4.0+, not the default. When both optional peer deps are present, MathTSEngine is the default (engine-registry.ts lines 84-88). The comment "v0.3.5's default engine" was accurate when written but is now false for v0.4.0+.

**Fix**: Change to "The pure-TypeScript, zero-dependency `TensorEngine` — the correctness baseline and fallback engine (default when @danielsimonjr/mathts-tensor is not installed; see engine-registry.ts)."

**Severity**: MEDIUM — users reading the docstring get the wrong impression of which engine is active.

---

### LC-7 — `connection-validators.ts` lines 2-3: says "Per-kind validation for v0.4.0 connection-layer AST nodes" but the spec doc it references doesn't yet exist (LOW)

**File**: `src/dimensional/connection-validators.ts`, lines 9-10

**Misleading comment**:
> "Per docs/specification/Part-IX-Connection-Layer.md (to be written in Task 18) and v0.4.0-Design.md §4 + §7."

**What the code actually does**: `Part-IX-Connection-Layer.md` was marked "(to be written in Task 18)". v0.4.5 is shipped. Either the spec was written (and the parenthetical is stale) or it was not written (and the file reference is a dead pointer). The phrase "to be written" is a future-tense promise from the past.

**Fix**: Verify whether `docs/specification/Part-IX-Connection-Layer.md` exists. If it does, remove the parenthetical. If it does not, change to "see v0.4.0-Design.md §4 + §7 (Part-IX spec not yet written)."

**Severity**: LOW — doc cross-reference maintenance, no behavioral impact.

---

### LC-8 — `index.ts` line 99 comment has a stale version reference in the re-export block (LOW)

**File**: `src/numerical/index.ts`, line 99 area (the main `src/index.ts`)

**Misleading comment**: `src/index.ts` line 99:
> "// v0.3.5 numerical-contraction backend. See docs/planning/v0.3.5-Design.md."

But the actual re-export block also includes v0.4.0 additions (lines 112-116: `DuplicateCoordinateWarning`, `EngineCapabilityError`, `hasAutogradSupport`, `evaluateBE37CovariantEikonalNumerical`). The section is no longer purely "v0.3.5".

**Fix**: Change to "// Numerical-contraction backend (v0.3.5+). See docs/planning/v0.3.5-Design.md and v0.4.0-Implementation-Plan.md."

**Severity**: LOW — minor version staleness in a comment.

---

### LC-9 — `connection-lowering-helpers.ts` lines 339-341: `getMetricDerivFlat` says key format is `${metricName}/${coordLabel}_${mu}` but pderiv.ts `pderivSymbolic` uses `${symbolName}/${coordLabel}` (no `_${mu}`) (MEDIUM)

**File**: `src/numerical/connection-lowering-helpers.ts`, lines 339-341

**Misleading comment**:
> "Key format: `${metricName}/μ_${mu}` (e.g. 'g/μ_0' for ∂_0 g)."

**What the code actually does**: Line 354: `const key = \`${metricName}/${coordLabel}_${mu}\`;` — so the key is `g/μ_0`. But in `lowering.ts` (line 319), the metric-pderiv 'supplied' path builds the key as `${mNode.name}/${coordLabel}_${mu}` — same format. However, `pderivSymbolic` in `pderiv.ts` line 108 uses `${symbolName}/${coordLabel}` with NO `_${mu}` suffix — these are different key schemas for different contexts. The comment in `getMetricDerivFlat` uses `μ_0` literally, where `μ` should be the coordLabel string, not the Greek letter. This is a documentation error in the example.

**Fix**: Change the example from `'g/μ_0'` to `'g/<coordLabel>_0'` (where `<coordLabel>` is the actual label string like `'x'`, `'t'` etc.) to avoid implying the literal Greek letter μ is used as a key.

**Severity**: MEDIUM — the example is confusing; using the Greek letter μ as a key literal would be valid but unexpected. The actual key depends on whatever `coordLabel` string the caller passes.

---

## Category 3: Type-Safety Holes

### TS-1 — `mathts-engine.ts` lines 119, 132, 159, 171: `as any` casts for the optional autograd dep (HIGH)

**File**: `src/numerical/mathts-engine.ts`

**Locations**:
- Line 119: `let autograd: any;` (used in forwardGrad)
- Line 132: `await autograd.forwardGrad(fn as any, xMathts as any)`
- Line 159: `let autograd: any;` (used in reverseGrad)
- Line 171: `await autograd.reverseGrad(fn as any, xMathts as any, ctMathts as any)`

**Risk**: `autograd` is declared as `any`, so all method calls on it are unchecked. If `@danielsimonjr/mathts-autograd` changes its API, these calls will fail at runtime with no TypeScript guard. The `fn as any` casts further suppress the function signature mismatch check.

The `// eslint-disable-next-line` + `// @ts-ignore` comments show awareness of the issue. The file imports `Tensor` from `mathts-tensor` (which has types), but `mathts-autograd` is optional with no bundled declarations.

**Proposed tighter type**: Define a local interface for the expected autograd shape:
```ts
interface MathTSAutograd {
  forwardGrad(fn: (x: Tensor) => Tensor, x: Tensor): Promise<{ value: Tensor; jacobian: Tensor }>;
  reverseGrad(fn: (x: Tensor) => Tensor, x: Tensor, ct?: Tensor): Promise<{ value: Tensor; gradient: Tensor }>;
}
```
Then cast the dynamic import result to `MathTSAutograd` (one explicit `as unknown as MathTSAutograd` cast at the import site, tighter than `any` cascading to 4+ call sites).

**Severity**: HIGH — `any` casts suppress type-checking at the boundary where the optional dep's API is consumed. A breaking change in mathts-autograd would produce runtime errors with no compile-time signal.

---

### TS-2 — `lowering.ts` line 418: `CovariantDerivativeNode.of` and `.wrt` are `unknown` — cast to `ExprNode` without a guard (HIGH)

**File**: `src/numerical/lowering.ts`, lines 407-408

**Locations**:
- Line 407: `const covNode = node as CovariantDerivativeNode;`
- Line 408: `const ofExpr = covNode.of as ExprNode;`
- Line 479: `wrt: covNode.wrt as ExprNode,`
- Line 452: `const strategy = (covNode.gLower as MetricTensorNode).derivativeStrategy ?? 'computed';`

**Risk**: `CovariantDerivativeNode.of` and `.wrt` are typed as `unknown` (see `connection-validators.ts` line 36-37 — `readonly of: unknown; readonly wrt: unknown`) because the validator module cannot import `ExprNode` (module cycle). The cast `covNode.of as ExprNode` is a type assertion without a runtime check. If a malformed AST is constructed directly (bypassing the validator), `ofExpr.kind` will produce a runtime error instead of a typed NumericalBackendError.

Similarly, `covNode.gLower as MetricTensorNode` (line 452) is an unchecked cast. If `gLower` is not a MetricTensorNode at runtime, the `.derivativeStrategy` access silently returns `undefined` and the `?? 'computed'` default kicks in — no error, but wrong behavior.

**Proposed fix**: Add a minimal runtime guard at the lowering case entry:
```ts
if (typeof (covNode.of as { kind?: unknown }).kind !== 'string') {
  throw new NumericalBackendError('lowering: CovariantDerivativeNode.of must have a kind field');
}
```
Or narrow `gLower` explicitly: if `covNode.gLower.kind !== 'metric-tensor'` throw.

**Severity**: HIGH — unchecked `unknown → ExprNode` casts are the exact scenario type-safety is designed to prevent. Any direct AST construction bypassing the validator will silently produce wrong results.

---

### TS-3 — `float64-engine.ts` lines 391-429: AD dispatch via duck-typing `'tangent' in a` and `'tape' in a` — fragile property-existence checks (HIGH)

**File**: `src/numerical/float64-engine.ts`, lines 391-429

**Locations**: `add()`, `sub()`, `mul()`, `scale()` all dispatch to AD paths via:
```ts
if ('tangent' in a && 'tangent' in b) { /* dual path */ }
if ('tape' in a && 'tape' in b) { /* tape path */ }
```

**Risk**: These checks rely on a string property name being present, which is not a type-level guarantee. There is no TypeScript discriminant for this — `EngineTensor` is an opaque interface with only `shape: ReadonlyArray<number>`. The casts `a as unknown as EngineDualTensor` are unchecked. If any external code passes an `EngineTensor` that happens to have a `tangent` property for another reason (e.g., a future engine's tensor), it would incorrectly enter the dual-number path.

`EngineDualTensor` and `EngineTapedTensor` are `@internal` private classes, so the risk is low in practice. But the dispatch mechanism is fundamentally un-typed.

**Proposed fix**: A minimal discriminant — e.g., a symbol property on `EngineDualTensor` and `EngineTapedTensor`, or an `instanceof` check (which works since both classes are defined in the same module). Change `'tangent' in a` to `a instanceof EngineDualTensor` (within the module where the class is defined, this is both typesafe and clear).

**Severity**: HIGH — the current dispatch survives only because both AD tensor types are private. The lack of typed dispatch is a latent fragility.

---

### TS-4 — `lowering.ts` line 418: `ofIndices` is accessed via a structural cast without a type guard (MEDIUM)

**File**: `src/numerical/lowering.ts`, lines 418-419

**Location**:
```ts
const ofIndices = (ofExpr as { indices?: ReadonlyArray<{ label: string; variance: string }> }).indices;
```

**Risk**: The cast `ofExpr as { indices?: ... }` is a structural lie — `ofExpr` could be an `op` node or `integral` node (both lack `.indices`), in which case `ofIndices` is correctly `undefined`. But `variance: string` is too wide — the actual type is `'upper' | 'lower'`. If a future ExprNode kind provides `.indices` with different variance values, this cast silently passes through.

**Fix**: Change `variance: string` to `variance: 'upper' | 'lower'` in the cast to tighten the type.

**Severity**: MEDIUM — practical risk is low (the ExprNode union is sealed), but the cast is wider than necessary.

---

### TS-5 — `validator.ts` line 339: `probeCtx` violations array is separate from `ctx.violations` — silently discards errors (MEDIUM)

**File**: `src/dimensional/validator.ts`, lines 339-343

**Location**:
```ts
const probeCtx: InferContext = {
  path: joinPath(ctx.path, 'args[1]'),
  violations: [],
  freeIndices: new Map(),
};
const probed = infer(expNode, probeCtx);
if (probed !== null && probed !== undefined && probeCtx.violations.length === 0) {
  actualDim = probed;
}
```

**Risk**: The `probeCtx.violations` array is intentionally throwaway (the comment at line 336 says "Use a throwaway local violation channel so a deeper inference error doesn't double-report"). But the check `probeCtx.violations.length === 0` means that if inference SUCCEEDS (`probed !== null`) but with warnings, those warnings are silently dropped. In v0.3.5+ warnings can be `severity: 'warning'` violations, so `probeCtx.violations.length > 0` does NOT necessarily mean inference failed.

**Fix**: Either (a) propagate `probeCtx.violations` to `ctx.violations` after the probe, or (b) check `okFromViolations(probeCtx.violations)` (the internal function at line 586) instead of `.length === 0`.

**Severity**: MEDIUM — warning-severity violations from the probe exponent are silently discarded. In practice, `'^'` exponents are simple numeric symbols so this is low-risk, but the pattern is subtly wrong.

---

### TS-6 — `bridge-check.ts`: `EXPECTED_DIMENSION_BY_BRIDGE` is exported but never imported internally or externally (MEDIUM)

**File**: `src/dimensional/bridge-check.ts`

**Finding**: Per the unused-analysis.md, `EXPECTED_DIMENSION_BY_BRIDGE` is exported but has no import anywhere in the codebase. It is a large constant map keyed by bridge ID. Exporting unused public API increases the surface that must be maintained.

**Fix**: Either (a) keep it exported with a `@public` JSDoc if it's intended for downstream consumer use, or (b) mark it `@internal` in the JSDoc, or (c) remove the `export` keyword if it's truly not needed.

**Severity**: MEDIUM — public API surface inflation. Not a type error per se but a maintainability issue.

---

### TS-7 — `tensor-engine.ts` line 140: `isEinsumSpec` uses `unknown` runtime guard but is exported as `@internal` — not called by the lowering pass (LOW)

**File**: `src/numerical/tensor-engine.ts`, lines 140-159

**Finding**: Per unused-analysis.md, `isEinsumSpec` is exported but not imported anywhere. The JSDoc says `@internal`. The function exists to guard the lowering→engine boundary, but `lowering.ts` builds `EinsumSpec` objects directly (via `buildEinsumSpec`) and never validates them against `isEinsumSpec`. If the guard was intended to be called before `engine.einsum()`, it is currently dead.

**Fix**: Either (a) call `isEinsumSpec(spec)` inside `float64-engine.ts`'s `einsum()` as an invariant check (at the cost of a tiny runtime overhead), or (b) remove the export if the guard is only conceptually useful.

**Severity**: LOW — dead export; note to verify intended use case.

---

### TS-8 — `metric-validators.ts`: Three exported result interfaces are never imported (LOW)

**File**: `src/dimensional/metric-validators.ts`

**Finding**: Per unused-analysis.md, `MetricTensorValidationResult`, `KroneckerDeltaValidationResult`, and `PartialDerivativeValidationResult` are exported but never imported by any other file. These are the result types of `validateMetricTensor`, `validateKroneckerDelta`, and `validatePartialDerivative` — but all callers use the return value anonymously (TypeScript infers the type).

**Fix**: If these are intended as part of the public surface for downstream consumers who want to type their own validators, add `@public` to their JSDoc. If they are internal, remove the `export` keyword.

**Severity**: LOW — type-only, no runtime impact. Reduces public API surface if removed.

---

## Category 4: Algorithmic Simplifications

### AS-1 — THREE independent `flattenNestedArray` implementations in the numerical module (HIGH)

**Files**:
1. `src/numerical/connection-lowering-helpers.ts` line 31: `flattenNA(data: NestedArray): number[]`
2. `src/numerical/lowering.ts` line 92: `flattenNestedArray(data, expectedSize)` (wrapper over flattenNA)
3. `src/numerical/pderiv.ts` line 15: `flattenToNumbers(data: NestedArray): number[]`

**Finding**: `flattenToNumbers` in `pderiv.ts` is a LOCAL private function that is functionally identical to `flattenNA` in `connection-lowering-helpers.ts`. Both recursively walk a `NestedArray` and collect numbers in row-major order. Neither is exported from `pderiv.ts`.

`pderiv.ts` could simply import `flattenNA` from `connection-lowering-helpers.ts` and delete `flattenToNumbers`. This eliminates one of three implementations.

**Proof of equivalence**:
- `flattenNA`: `if (typeof data === 'number') return [data]; const out = []; walk(data); return out;`
- `flattenToNumbers`: `if (typeof data === 'number') return [data]; const out = []; walk(data); return out;`
Same algorithm, different name.

**Fix**: In `pderiv.ts`, remove `flattenToNumbers` (lines 15-24). Import `flattenNA` from `'./connection-lowering-helpers.js'`. Replace 3 usages at lines 43, 88, 89 with `flattenNA`.

**Severity**: HIGH — duplicate function with no distinguishing behavior. Maintaining two copies risks divergence.

---

### AS-2 — `float64-engine.ts` and `connection-lowering-helpers.ts`: TWO independent `sameShape` functions (MEDIUM)

**Files**:
1. `src/numerical/float64-engine.ts` line 335: `function sameShape(...)`
2. `src/numerical/connection-lowering-helpers.ts` line 129: `function sameShape(...)`

**Finding**: Both functions are identical:
```ts
return a.length === b.length && a.every((v, i) => v === b[i]);
```

`float64-engine.ts` uses it in `elementwise()` (line 342). `connection-lowering-helpers.ts` uses it in `tensorAdd` and `tensorAddScaled`. Both are private module-level functions.

**Fix**: This would require either (a) extracting a shared utility module (introduces a new file, minor overhead), or (b) accepting the duplication since the functions are private and the modules serve different concerns. Option (b) is acceptable given the simplicity of the function. Mark as LOW if not extracted.

**Severity**: MEDIUM — duplication is minor, but if the shape comparison ever changes (e.g., to handle broadcast shapes), having two copies creates a maintenance divergence risk.

---

### AS-3 — `float64-engine.ts`: TWO independent `flatIndex` + stride utility functions duplicate those in `connection-lowering-helpers.ts` (MEDIUM)

**Files**:
1. `src/numerical/float64-engine.ts` lines 267-279, 367-371: `rowMajorStrides`, `flatIndex`
2. `src/numerical/connection-lowering-helpers.ts` lines 298-312: `buildStrides`, `flatIndex`

**Finding**: Both modules implement the same row-major stride computation and flat-index calculation:
- `Float64Tensor.rowMajorStrides`: `for k from length-1 to 0, strides[k] = acc, acc *= shape[k]`
- `buildStrides`: identical algorithm, different variable names

Both `flatIndex` functions are also identical: `let f = 0; for k: f += idx[k] * strides[k]; return f`.

**Fix**: Extract to a shared `src/numerical/strides.ts` utility module (or similar), or — simpler — import from `float64-engine.ts` if the dependency direction is acceptable. Given that `connection-lowering-helpers.ts` should not depend on `float64-engine.ts` (would create a coupling concern), a tiny shared utility file is cleaner.

**Severity**: MEDIUM — 4 duplicate functions across 2 files. Risk of divergence if stride convention changes.

---

### AS-4 — `connection-lowering-helpers.ts` lines 257-290: `forEachMultiIndex` allocates a fresh `number[]` on every `visit` call via `[...idx]` (MEDIUM)

**File**: `src/numerical/connection-lowering-helpers.ts`, lines 264, 319-328

**Finding**: `forEachMultiIndex` calls `visit([...idx])` at line 264 — spreading the `idx` array on every iteration. For a 4D Christoffel computation this is called N^4 = 256 times (N=4). Each spread allocates a new array.

Compare to `float64-engine.ts`'s `forEachIndex` which calls `visit(idx)` at line 359 — passing the SAME array reference to the visitor. The `float64-engine.ts` version is more efficient (visitor must not mutate), and the visitor pattern there does NOT mutate the idx.

The `contractChristoffelWithOperand` visitor at line 264 does not mutate `outIdx` (it calls `outIdx.slice(0, rank)` and `tIdx[freeIdxPos] = lam` on the SLICE, not on outIdx). So the spread in `forEachMultiIndex` is unnecessary.

**Fix**: Change `visit([...idx])` to `visit(idx)` in `forEachMultiIndex`. Add a comment: "Visitor must not mutate the passed idx array." The visitor in `contractChristoffelWithOperand` uses `.slice()` already, so it is safe.

**Severity**: MEDIUM — eliminates N^4 array allocations per Christoffel contraction. In N=4 spacetime: 256 avoided allocations per covariant-derivative lowering call.

---

### AS-5 — `tensor.ts` lines 213-215: `Array.from(merged.entries())` for safe Map iteration during deletion (LOW)

**File**: `src/dimensional/tensor.ts`, lines 213-215

**Finding**:
```ts
for (const [label, counts] of Array.from(merged.entries())) {
  if (counts.upper === 0 && counts.lower === 0) merged.delete(label);
}
```

The `Array.from` snapshot is taken because you cannot delete from a Map while iterating its `entries()`. However, an alternative is to collect the keys to delete first, then delete in a separate loop — avoiding the full entries array allocation:

```ts
const toDelete: string[] = [];
for (const [label, counts] of merged) {
  if (counts.upper === 0 && counts.lower === 0) toDelete.push(label);
}
for (const label of toDelete) merged.delete(label);
```

This avoids allocating the full entries tuple array (just a string array of deleted keys, which is typically small — only contracted labels).

**Severity**: LOW — minor allocation optimization in a function called per validated tensor-product. In typical bridge equations with 2-4 contractions, this is not a bottleneck.

---

### AS-6 — `geodesic-integrator.ts` lines 122-132: inner RK4 loop recomputes `G = christoffelFn(x)` at each of 4 stages with a fully nested array return (MEDIUM)

**File**: `src/numerical/geodesic-integrator.ts`, lines 113-132

**Finding**: `geodesicRHS` is called 4 times per RK4 step. Each call invokes `christoffelFn(x)` which must return a `readonly (readonly (readonly number[])[])[]` — a fully nested 4x4x4 array structure. The inner triple-loop then accesses `G[mu][nu][rho]`.

For a flat array representation, each `G[mu]` and `G[mu][nu]` lookup follows object references through two levels of array indirection. This is 64 nested-array accesses per `geodesicRHS` call, 256 per RK4 step.

**However**: This is an algorithmic interface concern — changing the `christoffelFn` signature to accept a flat `[64]` array would be a breaking API change. VERDICT: Cannot simplify without breaking the public interface. Do NOT flag for v0.4.6.

**Revised Severity**: LOW — noted but out of scope for v0.4.6 without a breaking change. Deferred to v0.5.0 API review.

---

### AS-7 — `float64-engine.ts` lines 562-586: `operandFlatIndex` closure re-walks `spec.free` and `spec.contractions` on every element (MEDIUM)

**File**: `src/numerical/float64-engine.ts`, lines 562-584

**Finding**: The `operandFlatIndex` closure at line 562 is called once per output element per operand. It iterates `spec.free` and `spec.contractions` to set `idx` entries. For a 4x4 contraction with 2 operands and 3 free axes, this closure is called 64 × 2 = 128 times, each iterating the spec arrays.

The spec arrays are small in practice (≤ 8 entries), but the spec structure is constant — it could be precomputed into per-operand lookup tables before the `forEachIndex` loops.

**Fix**: Before the `forEachIndex(outShape, ...)` loop, build per-operand mappings:
```ts
// freeAxesByOp[o] = array of {varIdx, axis} pairs
// contractAxesByOp[o] = array of {contractorIdx, axis} pairs
```
Then inside `operandFlatIndex`, iterate the precomputed small arrays instead of the spec.

**Severity**: MEDIUM — reduces iteration cost inside the innermost loop of the einsum. For 4D spacetime tensor contractions, the improvement could be 2-5x for medium-rank tensors.

---

### AS-8 — `connection-lowering-helpers.ts` lines 162-185: `getMetricDeriv(mu)` is called 3 times per (mu, nu) pair in Christoffel computation, fetching the same `dmu`/`dnu` arrays repeatedly (MEDIUM)

**File**: `src/numerical/connection-lowering-helpers.ts`, lines 162-184

**Finding**: The Christoffel computation loops over `(alpha, mu, nu, rho)`. Inside the `rho` loop:
```ts
const dmu = getMetricDeriv(mu);  // called once per (alpha,mu,nu) iteration
const dnu = getMetricDeriv(nu);  // called once per (alpha,mu,nu) iteration
for (let rho = 0; rho < N; rho++) {
  const drho = getMetricDeriv(rho);  // called N times per (alpha,mu,nu) iteration
```

For strategy='supplied', `getMetricDerivFlat` calls `flattenNA` on the stored `NestedArray` each time. For N=4: `dmu` and `dnu` are computed once per (alpha,mu,nu) triple — that is N^3 = 64 times each. `drho` is computed N^4 = 256 times total (N per (alpha,mu,nu) combo).

**But**: `dmu` and `dnu` are already hoisted OUT of the rho loop (they are at the mu/nu level). Only `drho` = `getMetricDeriv(rho)` is inside the rho loop and is called N^3 times. Hoisting `drho` outside the alpha loop (precompute `dgrid[mu]` for all mu before the alpha loop) would reduce from N^3 calls to N calls.

**Fix**: Precompute all N metric derivative arrays before the triple loop:
```ts
const dMetric = Array.from({ length: N }, (_, mu) => getMetricDeriv(mu));
```
Then replace `getMetricDeriv(mu)`, `getMetricDeriv(nu)`, `getMetricDeriv(rho)` with `dMetric[mu]`, `dMetric[nu]`, `dMetric[rho]`.

For strategy='supplied', this reduces `flattenNA` calls from N^2 + N^2 + N^3 to N total.

**Severity**: MEDIUM — for N=4 with 'supplied' strategy: reduces from ~96 `flattenNA` calls to 4. The function's O(N^4) behavior remains but the constant factor shrinks.

---

## v0.4.6 Task Count Estimate

- **HIGH severity**: 9 findings → ~9 tasks (UC-2, TS-1, TS-2, TS-3, AS-1 are the critical ones)
- **MEDIUM severity**: 16 findings → ~12 tasks (some are single-line fixes, some are module-level refactors)
- **LOW (optional)**: 7 findings → ~5 tasks
- **Total v0.4.6 estimated tasks**: 21-26 (HIGH+MEDIUM) or 26-31 (with LOW)

Scope assessment: **MEDIUM** release. The HIGH+MEDIUM fixes are bounded, none require new APIs or breaking changes. AS-1 (flattener consolidation) and TS-1 (autograd `any` typing) are the most impactful individual tasks.

---

## Honesty Notes

### AMBIGUOUS findings (couldn't prove, did not flag)
- UC-4: `forEachIndex` with empty `contractSizes`. The path through `shape.length === 0` calling `visit([])` once IS a handled case. Not unreachable — correctly handled.
- AS-6: RK4 christoffelFn interface. Changing the nested-array return to a flat array would require a breaking API change. Deferred, not flagged as v0.4.6 scope.

### VERIFIED non-findings (candidates examined and rejected)
- `validator.ts` try/catch at lines 519-524 around `computeContraction`: the `throw err` in the catch is NOT dead — it re-throws IndexLabelCollisionError and VarianceMismatchError, which are expected.
- `metric.ts` raise()/lower() `RaiseLowerInvalidLabelError`: NOT dead — used by both raise() and lower() for the two distinct error conditions.
- `lowering.ts` `op '/'` guard at line 208: `node.args.length === 0` — this IS reachable from a malformed unvalidated AST (the validate() check at the top of evaluateNumerical doesn't run in the direct lowerNode path when called from tests).
- All 40 `be-11` through `be-50` files listed as "unused": these are public library code — they are not imported internally but exist as consumer-facing bridge modules. NOT dead code — intentional standalone exports.
- `resetEngineForTesting` in engine-registry.ts: per unused-analysis.md, this is not imported by source but is imported by test files (the scanner may have missed test imports). Verified as test infrastructure; not dead.

### Cross-checks performed
- Grep for `as any` / `as unknown as`: confirmed all `as unknown as` casts are in `float64-engine.ts` AD dispatch; no plain `as any` in non-test source
- Grep for `// TODO|// FIXME|// HACK`: zero matches in source — all deferred items use prose comments
- Grep for `isActiveStatus`: confirmed exported from bridges/index.ts but not imported anywhere — confirmed as public API (intended for downstream consumers who want to filter the BRIDGE_EQUATIONS array)
- Grep for flattenNA/flattenToNumbers: confirmed 3 independent implementations
- Grep for sameShape: confirmed 2 independent implementations
- Grep for forEachIndex/forEachMultiIndex: confirmed separate implementations per module; the `[...idx]` spread difference in connection-lowering-helpers vs float64-engine is real
- Grep for strategy-narrowing in lowering.ts: confirmed `strategy as 'zero' | 'supplied'` at line 518 is reached only after the `'zero'` early return, making the `'zero'` arm of the cast type-unreachable
