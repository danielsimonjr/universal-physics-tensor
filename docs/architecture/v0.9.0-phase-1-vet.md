# v0.9.0 Phase-2 Mid-Cycle Vet — Phase 1 (PG pilot)

> 2026-06-11. Single-reviewer vet (Adam role with Eve's empirical axes
> folded in — honest deviation from the plan's serial Adam→Eve pair).
> Verdict: **YELLOW** — core migration verified correct; one HIGH fixed
> before Phase 3 per the plan's gate rule.

## Findings + dispositions

- **H-1 (FIXED).** `bench/painleve-gullstrand-pipeline.bench.ts:72,82`
  passed the now-flat `PG_G_INV(X)` straight into `computeKretschmann`
  (`number[][]` parameter) — silent NaN; the bench "ran green" because
  benches assert nothing (the plan's own red-team axis, line 364).
  Empirically reproduced (K = NaN vs 1.5766e-19 unflattened, matching
  closed-form to 0.15%). Fixed with an `unflatten4x4` shim; O-4
  (`computeKretschmann` flat migration) stays deferred.
- **M-1 (MITIGATED).** No typecheck gate covers `tests/`/`bench/`
  (`tsconfig.json` excludes them; vitest transforms without checking).
  Also falsifies the Task 1.2 commit-message claim that "tsc FAILS on
  the consumer side" — tsc never saw those files; breakage surfaced at
  TDD-RED only. **Mitigation:** new `tsconfig.tests.json` whole-repo
  noEmit check, adopted as a *diff-gate* for Phase 3+ (the first run
  surfaced **71 preexisting error lines** in legacy tests/bench —
  baselined at `v0.9.0-tsc-tests-baseline.txt`; the gate is "no NEW
  errors", since fixing the legacy ones is out of sprint scope —
  recorded as G-7-class debt).
- **M-2 (ACCEPTED, on Phase-3 checklist).** Widened `NestedArray`
  type-admits `Float64Array` at fail-loud runtime-reject sites
  (`Float64ReferenceEngine.fromNested`, `bianchiResidual` walk,
  `MathTSEngine.fromNested` unverified vs optional peer). No
  Float64Array flows there today.
- **L-1 (noted).** R-1b covers the write-side transpose class only
  (read-side uniform transpose undetectable on a symmetric metric);
  plan commit-template g_{μν}/g^{μν} nit.

## Verified-correct

PG Float64Array slots one-to-one with the docstring matrices (zeros
included); g·g⁻¹ = I by hand (T-r block det = −1) and numerically;
all actually-flowing consumption paths flattenNA-mediated
(Float64Array-safe `for...of`); R-1b value/sign/precision sane;
tests/numerical 236/0; tests/api 9/9; PG bench non-degenerate and now
semantically valid post-H-1.
