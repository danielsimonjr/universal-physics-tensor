# UPT v0.7.1 — Phase 0 Baseline + Per-Candidate Verification

> Phase 0 deliverable per `docs/planning/v0.7.1-Design.md` Phase 0
> ("Baseline capture + per-candidate verification recon"). Runs
> the per-candidate empirical re-grep BEFORE Phases 1-6 open per
> the v0.7 stale-carry-forward execution lesson.

**Date:** 2026-05-24.
**HEAD at baseline:** `f11eee1` (consolidated design committed).
**Branch:** `claude/changelog-todo-sync-9PdMg`.

---

## Task 0.1 — Suite + build baseline

```
Test Files  198 passed | 2 skipped (200)
     Tests  2056 passed | 5 skipped | 1 todo (2062)
```

```
> universal-physics-tensor@0.6.0 build
> tsc
(clean — zero errors)
```

**Gate for Phases 1-6**: suite must stay ≥ 2056 passed, 0 failed.
Net-add only.

---

## Task 0.2 — Per-candidate verification

### MINIMIZE axis

| Candidate | Brainstorm claim | HEAD reality | Drift |
|---|---|---|---|
| **M-1** 7 v0.7 primitives `@public`-tagged absent from src/index.ts | 7 modules | **5 modules** (tensor-trace, friedmann-equation, rg-flow, gauge-field, klein-gordon-equation) | field-equation-helpers + validator-registry have NO `@public` tags — correctly internal; NOT re-export candidates. **2-of-7 drift.** |
| **M-3** ~37 BE-NN *Inputs interfaces with export keyword | ~37 | **45** | +8 drift (more leverage; same direction) |
| **M-4** *_LHS export count | ~35-45 | **33** | within band |
| **M-5** validateBE*Dimensions export count | 8 | **30** | brainstorm counted only "BE-NN-formatted" names; actual includes TEE, FLM variants. Will use the broader 30-set for the @internal pass. |

### SIMPLIFY axis

| Candidate | Brainstorm claim | HEAD reality | Drift |
|---|---|---|---|
| **S-5** `_dimensionOf`/`_requireValue`/`_flattenNestedArray` at curvature-lowering-helpers.ts:599-621 | confirmed at those lines | **confirmed lines 599/604/613** | no drift |
| **S-8** rg-flow.ts bypasses field-equation-helpers | no import | **confirmed: no import of field-equation-helpers** | no drift (Adam-MEDIUM #2 from BRIDGE-PHYSICS-AUDIT v2 holds) |

### OPTIMIZE axis

| Candidate | Brainstorm claim | HEAD reality | Drift |
|---|---|---|---|
| **O-1** schwarzschildGInverseFn returns nested array (no Float64Array migration) | confirmed | **confirmed: returns `number[][]`** | no drift; BR-2-class win recoverable |

### Adjustments to v0.7.1 design from this verification

- **Phase 1 Task 1.2 (M-1)**: re-export **5 modules**, not 7. field-equation-helpers + validator-registry stay `@internal` (correct design — those are extension-mechanism internals, not the consumer-facing API).
- **Phase 2 Task 2.1 (M-3)**: target **45 interfaces** for export-drop pass, not 37.
- **Phase 2 Task 2.2 (M-4 + M-5)**: target **33 + 30 = 63 symbols** for the @internal annotation pass.
- Other candidates pass verification cleanly — no further design changes.

---

## Task 0.3 — Cross-axis dependency check

Per Decision #1 of the design doc:

- **M-1 → Phase A simplify**: Phase A simplify (Phase 3) uses `field-equation-helpers` from the `_be-helpers.ts` extraction; it does NOT depend on M-1's re-exports (the 5 primitives M-1 surfaces are not consumed by Phase 3's BE-NN triple-extraction). **Cross-link unnecessary**: Phase 1 (M-1) and Phase 3 (simplify) can land in either order.
- **S-8 BE-39 ↔ BRIDGE-PHYSICS-AUDIT v2 Adam-MEDIUM #2**: confirmed parallel finding. Phase 3 Task 3.3 closes both simultaneously.
- **O-1 → S-10**: confirmed — O-1 lands first, S-10 (DEFERRED per Decision #5 anyway) would see the post-migration shape.

Cross-axis assumptions hold. Phases 1+2 can launch in parallel (file-disjoint).

---

## Task 0.4 — Sprint-baseline gate

| Measure | Baseline | v0.7.1 final target |
|---|---:|---:|
| Tests passed | 2056 | ≥ 2056 + N (N = net-new from Phase 1+5 gate tests) |
| Tests failed | 0 | 0 |
| Test files | 198 + 2 skipped | 198 + 2 skipped + 1-2 new |
| `src/index.ts` re-exports | ~60 | 60 + 5 (M-1 re-exports) |
| Catalog entries | 44 | 44 (no new bridges this sprint) |
| `npm audit` HIGH/CRITICAL | 0 | 0 |

---

**Phase 0 complete.** Two minor drifts found (M-1: 7→5; M-3: 37→45 — both same-direction, sprint scope expands marginally); design adjustments noted in §"Adjustments to v0.7.1 design from this verification" above. Phases 1+2 unblocked for parallel dispatch.
