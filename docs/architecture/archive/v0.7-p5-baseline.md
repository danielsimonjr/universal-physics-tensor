# UPT v0.8 Proposal 5 — Baseline + Adam+Eve Reconciliation

> Phase 0 deliverable per `docs/planning/v0.7-Proposal-5-Design.md`.

**Date:** 2026-05-23.
**HEAD at baseline:** `095eef0` (feat(v0.7-p1): Phase 4 — single-bridge demo + surface + tutorial + CHANGELOG).
**Branch:** `claude/changelog-todo-sync-9PdMg`.
**Tag note:** P5 design says v0.8 (sequenced after v0.7 Foundation Consolidation). User executed in v0.7 session ahead of schedule; tag strategy is theirs.

## Task 0.1 — Suite baseline

```
Test Files  186 passed | 2 skipped (188)
     Tests  1813 passed | 1 skipped | 1 todo (1815)
```

**Gate**: ≥ 1813 passed, 0 failed.

## Task 0.2 — Dependency check (P3/P2/P1 readiness)

- **P3 (`Cell` union)**: ✓ `src/core/cell.ts:90,110,130,144` exports `LawCell`, `BridgeCell`, `EmergenceCell`, `Cell`.
- **P1 (`AxisName`)**: ✓ `src/core/universal-index.ts:35-41` — six members (`scale`, `force`, `symmetry`, `information`, `dimension`, `topology`).
- **P2 (flux rules)**: ✓ `src/core/flux-rules.ts:99-127` — `FluxRuleKind`, `FluxRule`, `runRules` dispatcher; `src/core/tensor.ts:145,178,414` — `fluxRules` field on `UniversalTensor`.

## Task 0.3 — Re-export site verification (Adam-H1 patch)

The P5 design said "verify `src/core/index.ts` exports" — but no such file exists. The actual public re-export site is `src/index.ts` (44 exports at HEAD). All P1/P2/P3 surface accounted for there. **Fix applied**: substitute `src/core/index.ts` → `src/index.ts` throughout our Phase-0/4 task spec.

## Adam findings (architecture)

- **H1 RESOLVED**: Task-0.3 path patched (above).
- **M1 — legacy-adapter round-trip strips unknown fields**: `src/core/tensor.ts:385-406` (addCell) uses `cellToBridge` (`src/core/cell.ts:200+`) which maps only the legacy `BridgeEquation` field set. Any new `regimes?` field on `BridgeCell` will not survive the round-trip into the legacy `bridgeEquations: Map` storage. **Resolution**: P5 Phase 4 ships `regimes?` as a sibling registry (keyed by cell id) rather than as a `BridgeCell` field. Avoids breaking the P3 surface AND avoids the adapter round-trip problem.
- **M2 — LawCell has plural arrays**: `src/core/cell.ts:90-100` shows `LawCell.scales: ReadonlyArray<PhysicalScale>` (plural), not the design's "single-valued regime." **Resolution**: regime registry stores `regimes: ReadonlyArray<RegimeValueBase>` per cell (matches the plural-axes shape).
- **L1**: Decision #1 framing acknowledged; ship without rephrase.

## Eve findings (empirical)

- **H1 RESOLVED**: 6 axes (not 5) confirmed; all P5 code/tests use 6.
- **M1 — terminology**: design uses `*Entry` but actual is `*Cell`. **Resolution**: implementation uses `*Cell` throughout.
- **M2 — FluxRuleKind closed union**: adding `'regime-consistency'` requires explicit union extension at `src/core/flux-rules.ts:99-103`. **Resolution**: extend the union in Phase 4 with full `_exhaustive: never` update everywhere it dispatches.
- **M3 — audit-doc-generator shape**: out of session scope (Phase 5 deferred).
- **L1 — defineRegime idempotency**: pinned in Phase 1 spec — same-spec re-import is a no-op (idempotent); spec mismatch on same axis+tag throws.

**Phase 0 deliverable complete.** Phase 1 (registry plumbing) unblocked.
