# v0.7 Proposal 3 — Phase 0 Baseline Capture

> Phase 0 deliverable per `docs/planning/v0.7-Proposal-3-Design.md`
> (post-Adam+Eve revision). Establishes the ground-truth measurements
> that Phases 1-2 compare against.

**Date:** 2026-05-23.
**HEAD at baseline:** `695d915` (docs(v0.7-p3): Eve red-team findings + design reconciliation).
**Branch:** `claude/changelog-todo-sync-9PdMg`.

---

## Task 0.1 — Suite baseline

`npm test` output:

```
Test Files  177 passed | 2 skipped (179)
     Tests  1675 passed | 1 skipped | 1 todo (1677)
  Duration  22.90 s
```

- **Passed:** 1675
- **Failed:** 0
- **Skipped:** 1 (intentional — gated by optional dep absence)
- **Todo:** 1 (intentional)
- **Test files:** 177 passed + 2 skipped (mathts-engine env-gated) = 179 total

**Gate for Phases 1-2:** suite must stay ≥ 1675 passed, 0 failed.
Net-add of new tests in Phase 1 (`tests/core/cell.test.ts`) increases
the passing count; no existing test may be deleted (per Eve-R4 silent-
deletion guard).

---

## Task 0.2 — Type-shape audit

Every load-bearing citation from `v0.7-Proposal-3-Design.md` verified
at HEAD:

| Claim | File:line | Verified |
|---|---|---|
| `"strict": true` | `tsconfig.json:12` | ✓ |
| `PhysicalLaw.confidence: number` | `src/core/types.ts:108` | ✓ |
| `BridgeEquation.confidence: number` | `src/core/types.ts:143` | ✓ |
| `EmergentPhenomenon.confidence: number` | `src/core/types.ts:172` | ✓ |
| `BridgeEquationStatus` union | `src/bridges/index.ts:42-46` | ✓ |
| `ExprNode` `kind` discriminator | `src/dimensional/validator.ts:65-69` | ✓ |
| `_exhaustive: never` pattern | `src/dimensional/validator.ts:636` | ✓ |
| `UniversalTensor.addLaw` | `src/core/tensor.ts:76` | ✓ |
| `UniversalTensor.addBridge` | `src/core/tensor.ts:118` | ✓ |
| `UniversalTensor.addEmergence` | `src/core/tensor.ts:226` | ✓ |
| `tensor.ts` line count | 441 LOC | ✓ |
| `PhysicalScale`, `Force`, `Symmetry` exports | `src/core/types.ts:10/19/31` | ✓ |
| `TensorIndices` export | `src/core/types.ts:76` | ✓ |
| `TensorConfig` interface | `src/core/types.ts:53-71` | ✓ |
| `src/core/cell.ts` absent | (file does not exist) | ✓ |

**No divergence found.** All architectural decisions reference real
code shapes. Design proceeds to Phase 1 without rework.

---

## Task 0.3 — Public-surface baseline

| Measure | Value |
|---|---|
| `src/index.ts` LOC | 232 |
| `^export` statements in `src/index.ts` | 33 |
| `tests/api/__snapshots__/public-surface.test.ts.snap` exists | Yes (73 LOC) |
| Snapshot mtime | 2026-05-23 03:22:56 (pre-sprint state) |

Phase 2 Task 2.3 will regenerate the snapshot after adding 6 new
exports (`Cell`, `LawCell`, `BridgeCell`, `EmergenceCell`, `CellBase`,
`compose`). Comparing pre- and post-snapshots gives an explicit
"what was added" audit (per Eve-R4 + Eve-R6).

---

## Task 0.4 — Consumer-import census

Sites referencing `PhysicalLaw`, `BridgeEquation`, or
`EmergentPhenomenon` by name. Each becomes a known migration target
for the v0.7.x follow-up sprint that adds `@deprecated` markers + the
consumer migration to `Cell` types (deferred from this sprint per
Eve-R2).

| Site | Lines | Role |
|---|---|---|
| `src/core/types.ts` | 88 (PhysicalLaw decl), 126 (BridgeEquation decl), 155 (EmergentPhenomenon decl) | Declaration site — keeps the interfaces, will get `@deprecated` in a follow-up |
| `src/core/tensor.ts` | 17-19 (multi-line import), 26-28 (private field types), 76 (addLaw param), 118 (addBridge param), 226 (addEmergence param) | Class implementation — consumes by name as parameter + field types |
| `src/index.ts` | 29-31 (multi-line re-export) | Public-surface re-export |
| `tests/tensor.test.ts` | 3 (import), 5/15/50/205/228 (literals) | Test fixtures using the old shape with `confidence: number` |
| `tests/api/public-surface.test.ts` | 135-136 (allowed-list) | API-contract test pinning the names as part of the surface |
| `examples/basic-usage.ts` | 12 (import), 23/37/51 (literals) | Shipping example using the old shape |

**Total: 4 consumer sites + 1 declaration site + 1 surface-test site.**
Eve's census (5 minus 1 acknowledged false-positive at
`tests/bridges/_helpers.ts:27`, which imports `BridgeEquationEntry`
not `BridgeEquation`) was correct. My initial single-line grep
under-counted because the imports are multi-line.

---

## Sprint baseline summary

| Metric | Current (master HEAD) | v0.7-p3 final target |
|---|---:|---:|
| Tests passed | 1675 | ≥ 1675 + N (N = net-new tests in Phase 1+2) |
| Tests failed | 0 | 0 |
| Test files | 179 | 179 + 2 (new: `cell.test.ts`, `addCell.test.ts`) |
| `src/core/cell.ts` LOC | (absent) | ~120-150 (estimated) |
| `src/index.ts` exports | 33 | 39 (+6 for the new types + `compose`) |
| `src/core/tensor.ts` LOC | 441 | 441 + ~30 (`addCell` + the three `cellToX` adapters) |
| `npm audit` HIGH/CRITICAL | 0 | 0 |

---

**Phase 0 complete.** No design rework needed; no pre-existing issues
surfaced. Phase 1 (Cell type + `cell.ts`) is unblocked.
