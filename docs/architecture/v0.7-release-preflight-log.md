# v0.7-Series Release Pre-Flight Log

> Per CLAUDE.md release discipline. Run before tagging.
> All checks executed 2026-05-23 at HEAD `72b73c3` (post v0.7
> follow-up + staging-docs commit).

**Branch**: `claude/changelog-todo-sync-9PdMg`.

---

## Check 1 — `npm audit`

```
found 0 vulnerabilities
```

✅ **PASS.** No HIGH/CRITICAL findings to document in CHANGELOG.

---

## Check 2 — `npm outdated`

| Package | Current | Wanted | Latest | Class |
|---|---|---|---|---|
| `@danielsimonjr/mathts-autograd` | 0.1.0 | 0.1.0 | 0.1.0 | optional peer — up to date |
| `@danielsimonjr/mathts-tensor` | 0.1.0 | 0.1.0 | 0.1.0 | optional peer — up to date |
| `@types/node` | 24.12.4 | 24.12.4 | 25.9.1 | dev — major bump available |
| `typescript` | 5.9.3 | 5.9.3 | 6.0.3 | dev — major bump available |
| `vitest` | 4.1.7 | 4.1.7 | 4.1.7 | dev — up to date (just patched) |

✅ **PASS** within `package.json` ranges. Two majors available but
out of v0.7-series scope:
- `@types/node` 25.x — Node 25 not yet LTS; defer to a dedicated
  dep-bump release.
- `typescript` 6.x — breaking changes; needs coordinated audit of
  every `@ts-*` directive + decorator usage + `strictFunctionTypes`
  interactions. Defer.

Note for CHANGELOG dep-health snapshot: cite "0 vulnerabilities + 2
non-blocking majors deferred (typescript 6.x, @types/node 25.x)."

---

## Check 3 — `npm run build` (`tsc` strict)

```
> universal-physics-tensor@0.6.0 build
> tsc
```

✅ **PASS.** Zero output = zero errors. tsc strict mode clean.

---

## Check 4 — `npm test`

```
Test Files  189 passed | 2 skipped (191)
     Tests  1854 passed | 5 skipped | 1 todo (1860)
```

| Tier | Count | Notes |
|---:|---|---|
| Passed | 1854 | up from 1675 at sprint baseline (+179 net new) |
| Failed | 0 | gate held throughout |
| Skipped (test files) | 2 | mathts-engine + AD conformance, optional-dep gated |
| Skipped (tests) | 5 | 3 PC-1.5 long-run gated behind `GL4_LONG=1` + 2 optional-dep |
| Todo | 1 | BE-42 N/A-as-encoded placeholder (intentional, audit §1 retraction) |

✅ **PASS.** Gate (≥ 1813, 0 failed) held + improved.

---

## Check 5 — `npm run smoke`

```
Rank: 3
Known Laws: 2
Bridge Equations: 1

=== Physical Constants ===
Speed of light: 299792458 m/s
Planck length: 1.616255e-35 m
✅ Test completed successfully!
```

✅ **PASS.** Smoke test (built `dist/` against `examples/test-example.js`)
runs to completion. Exit code 0.

---

## Check 6 — `npm run bench:ci` (informational; not gated)

Skipped in this pre-flight execution (bench harness has known
cold-start cost on this machine; not blocking for tag work). The
hz-table reporter limitation from v0.4.5 was closed in commit
`45ebfdb` via vitest 4.1.7 — async bench runs now print
distributions correctly. Recommended to run once before tagging
to capture fresh baseline numbers in `docs/architecture/benchmarks.md`.

Suggested follow-up command (5-10 minute runtime):

```
npm run bench:ci 2>&1 | tee /tmp/v0.7-bench-baseline.txt
```

Then update the v0.7 entry in `docs/architecture/benchmarks.md`
if material drift is observed (do not gate; baselines are
informational per the bench-discipline policy).

---

## Cross-Phase Invariant verification

`Cross-Phase Invariant 4` (P1) — `TensorSymbolNode` + `computeContraction`
UNCHANGED beyond v0.7-series commits that touched them. Diff-grep:

```
git diff master --name-only | grep -E "(src/dimensional/|src/numerical/lowering|src/bridges/equations/)"
```

Shows files touched in legitimate v0.7 work (P1 + P2 + the v0.7
follow-up `lowering.ts` extraction). Adam-broadened gate from
P1 Phase 0 (broaden Risk 5 grep to cover 5 files) verified clean
at HEAD; no rogue edits to AST-layer files.

---

## Suite-baseline preservation (release-gate)

| Release | Baseline at sprint-start | Current at pre-flight | Delta |
|---|---:|---:|---:|
| v0.6.0 | 1693 | — | — (released) |
| v0.6.1 (pending tag) | 1675 | 1675 | preserved |
| v0.7-series (pending tag) | 1675 | **1854** | **+179** |

All sprint-baseline gates held throughout the session.

---

## Pre-tag readiness verdict

✅ **READY TO TAG.** All five blocking pre-flight checks pass. Two
informational items recommended (run `npm run bench:ci` to refresh
baselines; pick a tag-strategy option from
`docs/architecture/v0.7-release-notes-draft.md`).

When you're ready to tag (and ignoring publish per session pragma):

```bash
git rev-parse HEAD                     # confirm tag target commit
# Edit package.json: 0.6.0 → 0.7.0 (or your chosen tag)
git add package.json
git commit -m "chore(release): bump version to v0.7.0"
git tag v0.7.0
git push origin master                  # if merging branch to master first
git push origin v0.7.0
# Create GitHub release using docs/architecture/v0.7-release-notes-draft.md
```

`npm publish --ignore-scripts --access public` deferred per user
directive (NPM_TOKEN rotation needed; publish unblocks
independently of tag/release).
