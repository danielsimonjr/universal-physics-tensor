## 2024-06-30 - Metric Closure Evaluation Hoisting in GL4 Updates
**Learning:** In the GL4 integrator stage and momentum updates (`updateFromStages` and `updateMomentumFromStages`), evaluating metric closures (like `gInverseFn` and `dgInverseFn`) inside the coordinate iteration loop (`mu`) resulted in redundant evaluations. Because the inputs (`stageX[i]`) only depend on the stage `i` and not on the coordinate axis `mu`, computing them `dim` times instead of once per stage causes unnecessary overhead, especially for expensive metric closure functions.
**Action:** Always hoist metric closure evaluations out of coordinate or nested iteration loops when their inputs are invariant across those inner loops to minimize redundant computations.

## 2024-05-24 - GL4 Stage Solver Memory Optimization
**Learning:** The nested loops in the GL4 stage solver (`solveGL4Stage` in `src/numerical/gl4-integrator.ts`) were recalculating identical quantities and doing redundant nested iterations inside the `i` loop, which executes twice per Picard iteration. By pre-allocating state buffers, reducing calls to metric inverse and derivative to exactly twice per Picard iteration, and unrolling/hoisting the intermediate stages (dx and dp terms) out of the `i` loop, we can make the computation substantially faster (measured a 5x speedup in the core routine logic with isolated testing).
**Action:** Always identify redundant evaluations and computations happening inside hot loops (like RK4/GL4 integrators or Picard solver inner loops) and precompute/hoist them. Pre-allocating and re-using `Float64Array` buffers avoids allocation pressure and GC pauses inside hot loops.
## 2024-05-18 - TypedArray to Array Conversion Overhead
**Learning:** In V8 (Node.js), converting a TypedArray (like `Float64Array`) to a standard Array using `Array.from()` carries massive allocation overhead. This can significantly degrade performance when used inside tight numerical integration loops (like RK4 or GL4 integrators).
**Action:** Always prefer manual `for` loops to populate standard Arrays from TypedArrays in performance-critical sections.

## 2024-07-03 - Array.from Overhead in Nested Allocations
**Learning:** V8's `Array.from()` carries significant allocation overhead not just for TypedArray conversions (as noted previously), but also when used to initialize nested multi-dimensional native Arrays (e.g., `Array.from({length}, () => ...)`). In numerical tensor assembly pipelines (like `connection-lowering-helpers.ts` or `lowering.ts`), this creates unnecessary allocation pressure compared to manual explicit loops `for (let i=0; i<N; i++) out[i] = ...`.
**Action:** Always pre-allocate arrays natively with `new Array(size)` and populate them using manual `for` loops in tensor assembly and numerical code.

## 2024-07-11 - flattenNA Recursion and Iterator Overhead
**Learning:** V8/Node.js incurs noticeable overhead for iterator protocol `for...of` loops, particularly when combined with deep recursion (e.g. flattening physics tensors). Replacing the recursive `walk` step in `flattenNA` with an explicit stack and `for (let i = len - 1; i >= 0; i--)` traversal avoids function call depth limits and iterator allocations, while adding explicit pre-allocated `new Array(len)` fast paths for common 1D and 2D physics shapes delivers up to ~1.7x performance improvement across tensor assembly workloads.
**Action:** When walking or flattening heavily nested structures in numerical backends, prefer explicit iterative stacks over deep recursion, and replace `for...of` loops over raw arrays with standard `for` loop traversal.
## 2026-07-13 - Algebraic Factoring of Tensor Loops in Integrator
**Learning:** In deep nested loops for tensor contractions (e.g. `updateMomentumFromStages` calculating `dg * P * P`), straightforward implementation often repeats multiplications of outer loop terms. Algebraically refactoring the sum by factoring out invariant array accesses (`stageP[i][nu]`) from the innermost loop reduces the number of multiplications dramatically (e.g., from ~64 to ~20 per outer loop execution).
**Action:** Always algebraically expand and re-factor multi-dimensional tensor contractions in performance-critical inner loops to minimize total arithmetic operations, moving partial sums out of the innermost loops.
## 2024-05-24 - TypedArray map optimization
**Learning:** V8's Array.prototype.map and TypedArray.prototype.map methods have significant overhead compared to manual `for` loops that write to pre-allocated arrays (`new Array(len)` or `new Float64Array(len)`). This overhead becomes a bottleneck in hot inner loops like finite difference stencils (e.g., `pderivNumericalFn`). We also learned that `Array.isArray()` checks will fail if we change a standard `Array` to a `Float64Array`, so we must be careful with types even if allocating standard Arrays is slightly slower than typed arrays.
**Action:** Always prefer manual `for` loops over `.map` in hot mathematical/numerical routines, explicitly pre-allocating the result array (`new Array(len)` or typed equivalents where valid). Avoid changing array types if the return signature or tests expect standard `Array`.

## 2024-07-17 - Algebraic Factoring in Tensor Integrators
**Learning:** In hot path mathematical loops like `geodesicRHS` in numerical integrators (e.g., RK4 evaluating Christoffel accelerations), naïve deeply nested loops computing contractions (like `G[16 * mu + 4 * nu + rho] * v[nu] * v[rho]`) perform massive numbers of redundant additions and multiplications.
**Action:** Always hoist invariant index offsets (`16 * mu`) and factor out multiplicands (`v[nu]`) that are invariant to the innermost loop index (`rho`). This reduces mathematical operations dramatically with no cost to readability.
## 2024-09-25 - Algebraic Factoring and Array Allocation Hoisting in Tensor Contractions
**Learning:** In tight tensor contraction loops like `contractChristoffelWithOperand`, placing array slice operations (`outIdx.slice()`) and complex index arithmetic (`flatIndex()`) inside the innermost loop can dominate the execution time due to the constant re-allocations and redundant computations. By hoisting these operations outside the innermost loop and tracking the flat index manually with a precalculated stride (`tFlat += tStride`), we can significantly speed up the contraction operation. Similarly, in multi-dimensional loops like `computeChristoffelTensor`, adding early bailout checks (`if (g !== 0)`) and hoisting index offsets (like `alpha * N * N`) outside their inner loops minimizes redundant operations.
**Action:** Always hoist array allocations and redundant index arithmetic out of tight loops. Manually track flat indices using precalculated strides for the innermost loops instead of recalculating full index projections. Utilize early bailout checks to prevent propagating operations when dealing with sparse or diagonal tensors.

## 2024-05-14 - Loop Unrolling and `.slice()` in V8
**Learning:** For small, fixed-iteration hot loops (like the 2 stages in the GL4 integrator), manually unrolling the loops and assigning array lookups to scalar variables produces measurable performance improvements (e.g. eliminating overhead of function calls/iterator arrays). However, attempting to replace a native `Array.prototype.slice()` operation with a manual `for` loop array copy *decreased* performance, because V8 optimizes the `slice()` method very well for copying small JS arrays (like 4D tensors).
**Action:** Always manually unroll small fixed-iteration outer loops in heavily nested paths. Leave `.slice()` alone for shallow array cloning unless benchmarking explicitly dictates otherwise.
## 2026-08-04 - Array Initialization and Allocation Pattern Overhead
**Learning:** V8 array initialization with integers (`0`) triggers `PACKED_SMI_ELEMENTS`. Subsequent assignment with floating-point values forces an internal re-allocation to `PACKED_DOUBLE_ELEMENTS`. Furthermore, fully pre-allocating complex multidimensional arrays simply to fill them with zeroes before immediately looping over them again to overwrite with calculated values introduces redundant `O(N^4)` iteration overhead.
**Action:** When initializing arrays explicitly intended for floating-point mathematical operations, use `0.0` instead of `0`. When populating multidimensional arrays, allocate the nested inner arrays immediately before assigning calculated values within the same calculation loop to avoid redundant double-iteration.
## 2026-08-12 - Adjacent Loop Fusion in Picard Iteration
**Learning:** In the `solveGL4Stage` function of the GL4 numerical integrator, there were two identical outer `for` loops precomputing values for `stage0` and `stage1` sequentially. Combining these adjacent loops over the same dimensions into a single outer block allows intermediate values (like the combined index `mu_dim_nu`) to be shared, avoiding duplicated calculation overhead across the two stages.
**Action:** When a function computes identical structural patterns for multiple targets (e.g., stages in an RK integrator), fuse the outer loops and unroll the targets inside the innermost loop to share index math and minimize iteration bounds.

## 2026-08-14 - Cross-Iteration Algorithmic Redundancy in Iterative Solvers
**Learning:** In the GL4 implicit stage solver and update, the state updates (`x += h * stageDx` and `p += h * stageDp`) require evaluating closures (`gInverseFn` and `dgInverseFn`) and executing deep $O(N^3)$ tensor contractions to find the derivatives `xDot` and `pDot` given the converged `stageX` and `stageP` from Picard iteration. Because the implicit stages $k_i$ (which equal the `stageDx` and `stageDp`) are explicitly computed during the inner Picard loop to test for convergence, recomputing them outside the loop is computationally redundant. However, you must carefully preserve mathematical correctness: e.g., if a `-0.5` coefficient was present in the momentum update but not in the raw `dp0` array computation, omitting the `-0.5` causes silent physical divergence. Second, if mutating `x` and `p` with the recovered vectors, ensure `x` and `p` are cloned (via `.slice()`) if the loop contains adaptive-halving retries to prevent retry corruption.
**Action:** In numerical integrators and fixed-point solvers, export the fully converged derivative arrays directly from the inner loop to skip re-evaluating computationally intensive mathematical closures for the final step update. When applying these updates manually without helper functions, strictly preserve state-safety cloning rules and equation coefficients.## 2024-08-17 - Hoisting Array Dereferences and Loop Unrolling in Tensor Assembly
**Learning:** In deeply nested numerical tensor assemblies like `computeWeylTensor` (e.g., $O(N^4)$ nested loops), simple array dereferences (like `g[sigma][mu]` or `RicMixed[rho][nu]`) become massive bottlenecks due to constant array pointer lookups inside the innermost loop. Additionally, when bounds are small and fixed (e.g., $N=4$ in relativity), the overhead of inner `for` loops exceeds the cost of the math itself. By hoisting invariant array references (like `const Ric_sigma = Ric[sigma]`) to the outermost possible loop and manually unrolling the innermost dimension, the V8 JIT can avoid vast amounts of pointer chasing.
**Action:** When working with fixed-size nested tensor formulas (especially $N=4$ spacetime tensors), aggressively hoist intermediate sub-array variables out of inner loops and manually unroll the innermost axis where feasible. Ensure floating-point literal initialization (`0.0`) is used to prevent V8 array element transitions.

## 2024-05-18 - Full Loop Unrolling in Fixed-Dimension Tensor Contractions
**Learning:** For extremely hot execution paths involving small tensors in physics frameworks (like `geodesicRHS` and `solveGL4Stage` where spacetime dimension is often fixed at 4), even with array access hoisting and invariant index factoring, standard nested `for` loops impose significant control flow and lookup overhead. By fully manually unrolling operations like 4x4 matrix-vector multiplication in a flat representation (e.g. tracking `offset` explicitly instead of dynamic indexing `mu*16 + nu*4 + rho`), execution time can be cut in half without losing performance benefits of zero-check (`if (g !== 0)`) bailout branches.
**Action:** When a critical integration or tensor loop frequently processes tensors of a small fixed dimension (`dim === 4`), create a dedicated unrolled code path that completely expands the inner loop combinations and explicitly caches array elements into scalar variables.

## 2026-08-21 - V8 Property Access and Loop Hoisting
**Learning:** In tight iterative integrators like GL4, dereferencing elements from multi-dimensional arrays inside the loop structure (e.g. `dxStageArr[0][mu]`) incurs significant V8 lookup overhead. Furthermore, repeated floating-point arithmetic (e.g. `subH * b0`) combined with an explicit loop across a standard, almost-universally-constant dimensionality (like `dim === 4` for relativity) consumes measurable CPU time. Unrolling these loops and hoisting operations mathematically guarantees identical correctness while speeding up execution by ~10% (e.g. from 24.19hz to 25.98hz on a test benchmark).
**Action:** When a loop modifies state using fixed multi-dimensional target buffers, extract inner nested arrays into local flat variables before the loop (e.g. `const dxStageArr0 = dxStageArr[0]`). Manually pre-calculate algebraic constants before array operations and provide explicit unrolled blocks for the most-common dimensionality (e.g. `if (dim === 4) { newX[0] += ... }`).
## 2024-09-25 - V8 Native Array Allocation and Loop Unrolling
**Learning:** In heavily nested multi-dimensional tensor construction (e.g. `computeWeylTensor`), explicitly unrolling the innermost fixed loop and populating a bracket-notation native array literal (e.g., `const arr = [val0, val1, val2, val3]`) is significantly faster than declaring a size-4 array (`new Array(4)`) and assigning elements by index (`arr[0] = ...`) within a `for` loop. V8 is able to optimally allocate and populate literal structures faster.
**Action:** For small, fixed dimension arrays built inside heavily executed numerical loops, hoist all variable lookups and unconditionally populate an array literal via `[...]` syntax instead of assigning iteratively by index.

## 2024-09-25 - Object/Array Literal Returns in Tight Loops
**Learning:** In V8, returning an object comprising newly allocated native multi-dimensional JS arrays via a manual `for` loop assignment structure requires significant overhead compared to returning an array literal. By completely unrolling the construction of returning multidimensional state variables (`[[stageX_1_1, ...], ...]`) in a heavily used numerical integrator function like `solveGL4Stage`, the JIT engine natively allocates these elements far more quickly. When $N=4$ dimensions are used across hundreds of stages per second, eliminating loop-based JS array instantiations reduces function iteration time by approximately 10%.
**Action:** When returning objects consisting of multiple small, fixed-dimension multi-dimensional arrays from heavily executed inner blocks (such as Picard solvers), explicitly define the object using native array literal blocks (e.g. `stageX: [[...], [...]]`) rather than utilizing pre-allocations and `for`-loops.
## 2024-10-27 - Closure Garbage Collection in Einsum Operations
**Learning:** In the `einsum` implementation of `Float64ReferenceEngine`, dynamically allocating arrays (like `ops.map(() => [])`) and especially creating `new Array<number>` inside the `operandFlatIndex` closure creates massive garbage collection pressure. Because this closure is invoked repeatedly within deep `forEachIndex` loops (e.g. millions of times for large tensor contractions), the memory allocator cannot keep up.
**Action:** Always pre-allocate tracking arrays and caches (e.g., `opIdxCache`) outside of hot loops and closures. Initialize multi-dimensional structures via unrolled `for` loops rather than chained `.map()` calls to bypass JS iterator overhead and avoid unnecessary memory transitions in tight numerical paths.

## 2024-11-20 - Factoring and Loop Unrolling inside Tensor Creation
**Learning:** In deeply nested algebraic tensor assembly (e.g. `computeWeylTensor` in `src/numerical/weyl-lowering.ts`), the combination of multi-dimensional arrays, closures, array method loops, and invariant mathematics can severely degrade performance. We learned that returning fully explicit native multi-dimensional array literals from functions `toNested4x4` bypasses array instantiation overhead. Furthermore, fully unrolling algebraic calculations with invariant constants hoisted prior to fixed assignment (like `R_rho_sigma_mu[0] + prefactor1 * R_sig_0 ...`) prevents `O(N^4)` repeated multiplication overhead. These loop unrolling and algebraic factorizations combine to give ~25% benchmark improvements.
**Action:** When building fixed 4x4 coordinate tensors (like Weyl, Riemann, Ricci index lowering/raising operations), unroll loop boundaries entirely if logic permits (e.g., `raiseRicciFirstIndex`). Inside multi-nested coordinate loops, identify invariant tensor slices (`R_rho = R[rho]`), invariant multiplication combinations (`prefactor1 = -0.5 * delta`), calculate them directly in the outermost scope possible, and assign the terminal innermost slice as an unrolled bracket-notation array literal `[a, b, c, d]`.

## 2024-11-20 - V8 Array Destructuring and Math Method Overhead in Hot Loops
**Learning:** In the `solveGL4Stage` Picard iteration solver, using array destructuring syntax for ping-pong buffer swaps (`[X, Xnew] = [Xnew, X]`) introduces hidden temporary array allocations that increase garbage collection overhead in hot loops. Additionally, calling `Math.max` and `Math.abs` inside the innermost convergence check loop incurs function invocation overhead. Replacing array destructuring with explicit temporary variable swaps and unrolling the dimensional loop (for `dim === 4`) with inline conditionals (`absD = d < 0 ? -d : d`) completely bypasses these overheads, leading to a measurable performance improvement (e.g., `solveGL4Stage` runs faster).
**Action:** In heavily executed integration loops, always prefer explicit temporary variables for swapping object references instead of array destructuring. Unroll heavily-called convergence checks for fixed dimensions (`dim === 4`) and use inline arithmetic conditionals instead of standard `Math` methods to eliminate invocation overhead.

## 2024-11-20 - Math Method Overhead in Hot Bisection Loops
**Learning:** In the `bisectCubic` function within `src/numerical/perihelion-finder.ts`, using `Math.abs(fs)` and `Math.sign(fs) === Math.sign(fLo)` repeatedly within its hot bisection loop (which can run up to 60 times per call) introduces measurable function invocation overhead. Replacing `Math.abs` with an inline ternary operator (`fs < 0 ? -fs : fs`) and `Math.sign` equality with a boolean comparison `(fs < 0) === (fLo < 0)` yields a ~25% performance improvement on tight micro-benchmarks by avoiding V8 engine method calls.
**Action:** When writing high-frequency mathematical loops (e.g., iterative root finders or integration convergence checks), prefer inline conditionals and boolean logic for simple operations like absolute value or sign comparison over `Math.*` methods to minimize invocation overhead.

## 2026-08-30 - V8 Array Initialization Fast Paths
**Learning:** For small, fixed-size multi-dimensional array creation in hot paths (like unflattening a 16-element Float64Array into a 4x4 array in `toNested4x4`), manually allocating arrays in a loop (`const row = new Array(4)`) adds significant overhead due to object allocation tracking. Returning an explicitly unrolled nested array literal (e.g., `[[m[0], m[1], ...], ...]`) leverages V8's fast-path literal allocation, cutting execution time by nearly 50% in tight loops.
**Action:** When creating small, fixed-size matrices from flat buffers in heavily accessed numerical routines, prefer explicit unrolled array literals `[[...], ...]` over iterative `new Array()` allocation.

## 2024-11-20 - Redundant Code Calculations inside loops
**Learning:** We had unneeded and unreachable mathematical assignments remaining in `computeWeylTensor` running at least 64 times inside hot loops that didn't do anything because the variables `term3_0` through `term3_3` weren't referenced outside the inner block. Worse, we found critical math expressions using variables that weren't even properly defined `RS_delta_rho_mu` inside an inner loop instead of evaluating `(RS * delta_rho_mu)`.
**Action:** When inspecting hot loops inside tensor math, explicitly track the lifecycle of intermediate calculations. If you find variables instantiated and modified that are completely untouched by the rest of the block (like `term3_0` etc.), delete them entirely to free up CPU operations. When factoring logic, test everything since fixing syntax typos might mask logic changes.

## 2026-09-02 - State Management in Hot Loops
**Learning:** In numerical retry/halving loops, continuous array cloning (e.g., `x.slice()`) causes garbage collection pauses. Pre-allocating persistent buffers outside the loop and using double-buffering (pointer swapping) after successful iterations preserves state safely without mutating the original arrays prematurely, avoiding massive array allocation overhead.
**Action:** When tracking and updating multi-dimensional state across steps or retries in a tight loop, pre-allocate working arrays once and swap array references (double-buffer) upon step success instead of cloning arrays on every attempt.

## 2024-12-19 - Deep Nested Array Literal Allocation
**Learning:** When generating multi-level nested structures like 4x4x4x4 arrays (e.g. in `nestedZeros4` for initializing intermediate storage during numerical integration loops), explicitly unrolling the matrix structure out into a fully formed literal (`[[[[0.0, ...]]]]`) for known fixed shapes (like N=4) takes advantage of V8's fast literal compilation path, significantly bypassing the dynamic overhead incurred by 4 levels of `new Array(N)` and `for` loop construction routines.
**Action:** For pre-allocation wrappers generating deep structures matching small physical bounds (e.g., a 4D spatial array), check for fixed boundary configurations and statically define deep array literals containing type-secure elements (like `0.0`) to avoid costly dynamic construction and instantiation tracing.
## 2024-06-03 - Nested Closure Overheads in Iterators
**Learning:** In the `einsum` implementation of `Float64ReferenceEngine`, the previous usage of the `forEachIndex` utility with nested callback closures (`(freeVals) => { ... }`) combined with an internal closure `operandFlatIndex` created massive garbage collection pressure and function-call invocation overhead in the hottest possible loops for tensor contractions.
**Action:** Replace custom higher-order iteration callbacks like `forEachIndex` with flattened, explicit "odometer" style manual `for` loops in hot tensor code paths. Pre-allocate multi-dimensional tracking indices and inline any index-resolution logic originally inside closures to avoid dynamic allocations.
