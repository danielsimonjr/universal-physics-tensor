/**
 * GridField — placeholder. Task 10 replaces this with the full definition
 * (shape + spacing + data + boundary).
 *
 * @module numerical/grid-field
 */
export interface GridField {
  readonly shape: ReadonlyArray<number>;
}
